
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local ContentProvider = game:GetService("ContentProvider")
local SoundService = game:GetService("SoundService")

local LocalPlayer = Players.LocalPlayer
local lplr = LocalPlayer
local SERVER_JOB_ID = game.JobId ~= "" and game.JobId or "Studio_Local_Server"


local SpoofedDisplayName = nil


local ScriptStartTime = os.time()


local MessageInput = nil
local SendChatMessage = nil
local PendingMessageMutations = {}
local CreateMessageCard = nil


local LastSentMessageText = ""
local ConsecutiveSpamCount = 0
local LastMessageSendTime = 0




local PFP_SAVE_KEY = "Kronos_UserPFP_AssetId"
local DefaultPfpId = "110388265928893"

local CurrentPfpId = (function()
    if readfile and isfile and isfile(PFP_SAVE_KEY .. ".txt") then
        local success, saved = pcall(function() return readfile(PFP_SAVE_KEY .. ".txt") end)
        if success and saved and saved ~= "" then return saved end
    end
    return DefaultPfpId
end)()

local function SavePfpIdLocally(assetId)
    CurrentPfpId = tostring(assetId)
    if writefile then
        pcall(function() writefile(PFP_SAVE_KEY .. ".txt", CurrentPfpId) end)
    end
end

local BoyPfpList = {
    "110388265928893", "95859643981980", "130155182109980", "13256391094", "7141979877", "126321955109144",
    "105811627876448", "117842977352398", "72892670694166", "83103469598025", "75074160606432", "102436713076194",
    "73360959903200", "8044287275", "75575644706346", "135418144122968", "108362888723956", "72208791847173",
    "126308173151046", "12589564218", "79957717292980", "9167461968", "14958453101", "15812656372",
    "17742534348", "16512222623", "16512186702", "15139830823", "113690915196801", "13815759727",
    "17209732396", "9465573085", "6828147162", "8798642997", "6931482888", "11465013207",
    "6151184293", "11792088810", "11792051524", "12129851981", "7766253892", "11810744690",
    "12129866999", "11792081695", "9812014649", "11566642562", "11566643877", "11566645628",
    "15489228483", "139913462267814", "127087398265711", "15741130603", "15914059452", "135351963625525",
    "116451771061047", "128665410481682", "80274332831847", "82099431184126", "133311251800519", "13120458633",
    "115774065375902", "70802874073501", "13589962584", "14640595544", "13399057339", "10246136967",
    "133023108030460", "119497955225507", "16167788172", "12972724276", "18955458678", "105883736105171", "77630895008923", "118862079408453", "106469663951907", "106301311535154", "80256028266444", "80138975757326", "88153948399084", "97074936820513", "122133569226021", "125791648012678", "81831086505070", "89843619861520"
}

local GirlPfpList = {
    "12020075855", "11830086459", "11511962080", "7777672203", "11830084792", "7332178758",
    "7430248978", "7766251418", "9093346004", "9093348980", "9093350889", "6784635852",
    "12557398956", "11830087451", "10305162859", "13706350387", "14436945404", "11830083821",
    "14436758178", "13135537390", "14428713593", "14436954850", "13135491186", "14428738204",
    "14428743609", "14436960020", "14758917533", "14484307859", "14437087525", "14436744290",
    "13835626125", "15317009082", "136596648757339", "81108881402205", "76266870536490", "16348401915",
    "117499002290555", "123333305454544", "114201242358456", "14847097309", "14688664923", "16348395223",
    "16348391354", "16348399148", "13843648183", "8705803091", "14549632840", "14111174579",
    "9052821925", "18516850087", "1756451716", "4958082006", "10395117329", "15023167771",
    "115457404558211", "14677866904", "106604782438383", "101003324274756", "99824761297039", "79899054463161",
    "132498447517240", "14365830804", "14133573570", "84575585234733", "14740456559"
}


local function GetFormattedDisplayName(displayName, isClient)
    if isClient then
        displayName = SpoofedDisplayName or displayName or ""
    else
        displayName = displayName or ""
    end
   
    if string.len(displayName) > 15 then
        return string.sub(displayName, 1, 14) .. "..."
    end
    return displayName
end


local function TruncateText(text, maxChars)
    text = text or ""
    if string.len(text) > maxChars then
        return string.sub(text, 1, maxChars) .. "..."
    end
    return text
end




local SOUNDS = {
    "rbxassetid://8503531171",
    "rbxassetid://88058206720285",
}

IntroScreenGui = Instance.new("ScreenGui")
IntroScreenGui.Name = "KronosIntroGui"
IntroScreenGui.ResetOnSpawn = false
IntroScreenGui.IgnoreGuiInset = true
IntroScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

IntroBackground = Instance.new("Frame")
IntroBackground.Name = "Background"
IntroBackground.Size = UDim2.new(1, 0, 1, 0)
IntroBackground.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
IntroBackground.BackgroundTransparency = 0.4
IntroBackground.BorderSizePixel = 0
IntroBackground.Parent = IntroScreenGui

IntroMainCard = Instance.new("Frame")
IntroMainCard.Name = "MainCard"
IntroMainCard.Size = UDim2.new(0, 480, 0, 180)
IntroMainCard.Position = UDim2.new(0.5, -240, 0.5, -90)
IntroMainCard.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
IntroMainCard.BorderSizePixel = 0
IntroMainCard.Parent = IntroBackground

CardCorner = Instance.new("UICorner")
CardCorner.CornerRadius = UDim.new(0, 12)
CardCorner.Parent = IntroMainCard

CardStroke = Instance.new("UIStroke")
CardStroke.Thickness = 3
CardStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
CardStroke.Color = Color3.fromRGB(255, 255, 255)
CardStroke.Parent = IntroMainCard

BorderGradientIntro = Instance.new("UIGradient")
BorderGradientIntro.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(15, 0, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0)),
})
BorderGradientIntro.Parent = CardStroke

local rotationSpeed = 120
local borderConnection
borderConnection = RunService.RenderStepped:Connect(function(deltaTime)
    if not CardStroke or not CardStroke.Parent then
        if borderConnection then borderConnection:Disconnect() end
        return
    end
    BorderGradientIntro.Rotation = (BorderGradientIntro.Rotation + (rotationSpeed * deltaTime)) % 360
end)

IntroTitle = Instance.new("TextLabel")
IntroTitle.Name = "Title"
IntroTitle.Size = UDim2.new(1, 0, 0, 35)
IntroTitle.Position = UDim2.new(0, 0, 0, 15)
IntroTitle.BackgroundTransparency = 1
IntroTitle.Text = "  KRONOS CHAT  "
IntroTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
IntroTitle.Font = Enum.Font.GothamBold
IntroTitle.TextSize = 22
IntroTitle.Parent = IntroMainCard

WelcomeLabel = Instance.new("TextLabel")
WelcomeLabel.Name = "WelcomeLabel"
WelcomeLabel.Size = UDim2.new(1, 0, 0, 20)
WelcomeLabel.Position = UDim2.new(0, 0, 0, 48)
WelcomeLabel.BackgroundTransparency = 1
WelcomeLabel.Text = "Welcome, " .. GetFormattedDisplayName(LocalPlayer.DisplayName, true)
WelcomeLabel.TextColor3 = Color3.fromRGB(180, 180, 190)
WelcomeLabel.Font = Enum.Font.Gotham
WelcomeLabel.TextSize = 12
WelcomeLabel.Parent = IntroMainCard

StatusLabel = Instance.new("TextLabel")
StatusLabel.Name = "StatusLabel"
StatusLabel.Size = UDim2.new(0, 320, 0, 20)
StatusLabel.Position = UDim2.new(0, 30, 0, 88)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "> Initializing core systems..."
StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.Font = Enum.Font.Code
StatusLabel.TextSize = 11
StatusLabel.Parent = IntroMainCard

SubStatusLabel = Instance.new("TextLabel")
SubStatusLabel.Name = "SubStatusLabel"
SubStatusLabel.Size = UDim2.new(0, 320, 0, 20)
SubStatusLabel.Position = UDim2.new(0, 30, 0, 106)
SubStatusLabel.BackgroundTransparency = 1
SubStatusLabel.Text = "Preparing interface..."
SubStatusLabel.TextColor3 = Color3.fromRGB(130, 130, 140)
SubStatusLabel.TextXAlignment = Enum.TextXAlignment.Left
SubStatusLabel.Font = Enum.Font.Gotham
SubStatusLabel.TextSize = 10
SubStatusLabel.Parent = IntroMainCard

BarBackground = Instance.new("Frame")
BarBackground.Name = "BarBackground"
BarBackground.Size = UDim2.new(0, 320, 0, 5)
BarBackground.Position = UDim2.new(0, 30, 0, 140)
BarBackground.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
BarBackground.BorderSizePixel = 0
BarBackground.Parent = IntroMainCard

BarCorner = Instance.new("UICorner")
BarCorner.CornerRadius = UDim.new(1, 0)
BarCorner.Parent = BarBackground

BarFill = Instance.new("Frame")
BarFill.Name = "BarFill"
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BorderSizePixel = 0
BarFill.Parent = BarBackground

FillCorner = Instance.new("UICorner")
FillCorner.CornerRadius = UDim.new(1, 0)
FillCorner.Parent = BarFill

local loadColorSequence = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 230, 0)),
    ColorSequenceKeypoint.new(0.9, Color3.fromRGB(0, 255, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 255, 0)),
})

local function getColorFromProgress(progress, sequence)
    if progress <= 0 then return sequence.Keypoints[1].Color end
    if progress >= 1 then return sequence.Keypoints[#sequence.Keypoints].Color end

    local lowerIndex = 1
    for i = 1, #sequence.Keypoints - 1 do
        if progress >= sequence.Keypoints[i].Time then
            lowerIndex = i
        end
    end

    local upperIndex = lowerIndex + 1
    local lower = sequence.Keypoints[lowerIndex]
    local upper = sequence.Keypoints[upperIndex]

    local ratio = (progress - lower.Time) / (upper.Time - lower.Time)
    return lower.Color:Lerp(upper.Color, ratio)
end

PercentCircle = Instance.new("Frame")
PercentCircle.Name = "PercentCircle"
PercentCircle.Size = UDim2.new(0, 56, 0, 56)
PercentCircle.Position = UDim2.new(0, 385, 0, 89)
PercentCircle.BackgroundColor3 = Color3.fromRGB(22, 22, 28)
PercentCircle.BorderSizePixel = 0
PercentCircle.Parent = IntroMainCard

CircleCorner = Instance.new("UICorner")
CircleCorner.CornerRadius = UDim.new(1, 0)
CircleCorner.Parent = PercentCircle

CircleStroke = Instance.new("UIStroke")
CircleStroke.Thickness = 2
CircleStroke.Color = Color3.fromRGB(40, 40, 50)
CircleStroke.Parent = PercentCircle

PercentText = Instance.new("TextLabel")
PercentText.Name = "PercentText"
PercentText.Size = UDim2.new(1, 0, 1, 0)
PercentText.BackgroundTransparency = 1
PercentText.Text = "0%"
PercentText.TextColor3 = Color3.fromRGB(255, 255, 255)
PercentText.Font = Enum.Font.GothamBold
PercentText.TextSize = 14
PercentText.Parent = PercentCircle

IntroSound = Instance.new("Sound")
IntroSound.Volume = 0.5
IntroSound.PlayOnRemove = false
IntroSound.Parent = IntroScreenGui
IntroSound.SoundId = SOUNDS[math.random(1, #SOUNDS)]

local loadingStages = {
    { progress = 0.15, status = "> Over 110+ Custom Stickers", sub = "Express yourself with a massive collection..." },
    { progress = 0.35, status = "> High-Priority User Privacy", sub = "Chat logs are securely cleared every 20 minutes..." },
    { progress = 0.55, status = "> Cross-Server Messaging", sub = "Connect and chat with friends across different servers..." },
    { progress = 0.75, status = "> Reliable & Secure Sharing", sub = "Seamless script and link sharing & copying..." },
    { progress = 0.85, status = "> Built-in Feedback System", sub = "Check out the About tab to share your thoughts..." },
    { progress = 0.92, status = "> Anti Spamming & Anti-Handspam", sub = "Features to prevent Trollers from ruining your experience" },
    { progress = 0.97, status = "> Exclusive Tags & Roles", sub = "Certain Tags / Roles grant Exclusive Privileges. Collect them all!" },
    { progress = 1.00, status = "> All Systems Ready!", sub = "Launching interface..." }
}

local ScreenGui = nil

local function runIntro()
    task.spawn(function()
        pcall(function()
            ContentProvider:PreloadAsync({ IntroSound })
        end)
    end)

    IntroSound:Play()

    local duration = 5.0
    local elapsed = 0
    local currentStageIndex = 1

    while elapsed < duration do
        task.wait(0.03)
        elapsed = elapsed + 0.03
        local rawPercent = math.min(elapsed / duration, 1)

        local t = rawPercent
        local progress = t * t * (3 - 2 * t)

        PercentText.Text = math.floor(progress * 100) .. "%"
        BarFill.Size = UDim2.new(progress, 0, 1, 0)

        local success, computedColor = pcall(function()
            return getColorFromProgress(progress, loadColorSequence)
        end)
        if success then
            BarFill.BackgroundColor3 = computedColor
        end

        if currentStageIndex <= #loadingStages and progress >= loadingStages[currentStageIndex].progress then
            local stage = loadingStages[currentStageIndex]
            StatusLabel.Text = stage.status
            SubStatusLabel.Text = stage.sub

            local pulseTween = TweenService:Create(
                CardStroke,
                TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, 0, true),
                { Thickness = 5 }
            )
            pulseTween:Play()

            currentStageIndex = currentStageIndex + 1
        end
    end

    task.wait(0.3)

    local fadeInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    local fadeBg = TweenService:Create(IntroBackground, fadeInfo, { BackgroundTransparency = 1 })
    local fadeCard = TweenService:Create(IntroMainCard, fadeInfo, { BackgroundTransparency = 1 })
    local fadeStroke = TweenService:Create(CardStroke, fadeInfo, { Transparency = 1 })
    local fadeTitle = TweenService:Create(IntroTitle, fadeInfo, { TextTransparency = 1 })
    local fadeWelcome = TweenService:Create(WelcomeLabel, fadeInfo, { TextTransparency = 1 })
    local fadeStatus = TweenService:Create(StatusLabel, fadeInfo, { TextTransparency = 1 })
    local fadeSubStatus = TweenService:Create(SubStatusLabel, fadeInfo, { TextTransparency = 1 })
    local fadeBarBg = TweenService:Create(BarBackground, fadeInfo, { BackgroundTransparency = 1 })
    local fadeBarFill = TweenService:Create(BarFill, fadeInfo, { BackgroundTransparency = 1 })
    local fadeCircle = TweenService:Create(PercentCircle, fadeInfo, { BackgroundTransparency = 1 })
    local fadeCircleStroke = TweenService:Create(CircleStroke, fadeInfo, { Transparency = 1 })
    local fadePercentText = TweenService:Create(PercentText, fadeInfo, { TextTransparency = 1 })

    fadeBg:Play()
    fadeCard:Play()
    fadeStroke:Play()
    fadeTitle:Play()
    fadeWelcome:Play()
    fadeStatus:Play()
    fadeSubStatus:Play()
    fadeBarBg:Play()
    fadeBarFill:Play()
    fadeCircle:Play()
    fadeCircleStroke:Play()
    fadePercentText:Play()

    fadeBg.Completed:Wait()
    IntroScreenGui:Destroy()

    if ScreenGui then
        ScreenGui.Enabled = true
    end
end

task.spawn(runIntro)




local customRPName = "乂乂 ᴋʀᴏɴᴏꜱ V ᴄʜᴀᴛ 乂乂"

local RPBioFontMap = {
    A = "Α", B = "Ⴆ", C = "ƈ", D = "ԃ", E = "ҽ", F = "ϝ", G = "ɠ", H = "ԋ",
    I = "ι", J = "ʝ", K = "ƙ", L = "ʅ", M = "ɱ", N = "α", O = "σ", P = "ρ",
    Q = "ϙ", R = "ɾ", S = "ʂ", T = "ƚ", U = "υ", V = "ʋ", W = "ɯ", X = "x",
    Y = "ყ", Z = "ȥ",
}

local function FormatRPBioDisplayName(displayName)
    displayName = tostring(displayName or "")
    return (displayName:gsub("%a", function(letter)
        return RPBioFontMap[string.upper(letter)] or letter
    end))
end

local customRPBio = FormatRPBioDisplayName(lplr.DisplayName)

local function applyRPProfile()
    local reFolder = ReplicatedStorage:WaitForChild("RE", 10)
    if not reFolder then return end

    local rpRemote = reFolder:WaitForChild("1RPNam1eTex1t", 5)
                  or reFolder:FindFirstChild("RPNameText")
                  or reFolder:FindFirstChild("RPName")

    if rpRemote then
        if not lplr.Character or not lplr.Character:FindFirstChild("HumanoidRootPart") then
            lplr.CharacterAdded:Wait()
            task.wait(1)
        end

        task.spawn(function()
            for i = 1, 5 do
                pcall(function()
                    rpRemote:FireServer("RolePlayName", customRPName)
                    rpRemote:FireServer("RolePlayBio", customRPBio)
                end)
                task.wait(0.5)
            end
        end)
    end
end

task.spawn(applyRPProfile)
lplr.CharacterAdded:Connect(function()
    task.wait(1.5)
    applyRPProfile()
end)


task.spawn(function()
    local reFolder = ReplicatedStorage:WaitForChild("RE", 10)
    if reFolder then
        local colorRemote = reFolder:WaitForChild("1RPNam1eColo1r", 5) or reFolder:FindFirstChild("RPNameColor")
        if colorRemote then
            local frequency = 1.6
            local brightRed = Color3.fromRGB(255, 0, 0)
            local pureBlack = Color3.fromRGB(0, 0, 0)
            local startTime = tick()

            while true do
                local elapsed = tick() - startTime
                local factor = (math.sin(elapsed * frequency) + 1) / 2
                local lerpedNameColor = brightRed:Lerp(pureBlack, factor)
                local lerpedBioColor = pureBlack:Lerp(brightRed, factor)

                pcall(function()
                    colorRemote:FireServer("PickingRPNameColor", lerpedNameColor)
                    colorRemote:FireServer("PickingRPBioColor", lerpedBioColor)
                end)
                task.wait(0.1)
            end
        end
    end
end)




local GLOBAL_FIREBASE_URL = "https://grimcouncilcommunicator-default-rtdb.asia-southeast1.firebasedatabase.app/"
local GLOBAL_MESSAGES_ENDPOINT = GLOBAL_FIREBASE_URL .. "global_chat/messages.json"

local SERVER_FIREBASE_URL = "https://gcserverchat-default-rtdb.asia-southeast1.firebasedatabase.app/"
local SERVER_MESSAGES_ENDPOINT = SERVER_FIREBASE_URL .. "server_chat/" .. SERVER_JOB_ID .. ".json"

local REQUEST_FIREBASE_URL = "https://gctagreq-default-rtdb.asia-southeast1.firebasedatabase.app/"
local REQUEST_ENDPOINT = REQUEST_FIREBASE_URL .. "requests.json"

local GITHUB_ROLE_LIST_URL = "https://raw.githubusercontent.com/basementfox98/KroCha/refs/heads/main/AudioIDduplicate.json"
local GITHUB_RANK_STYLES_URL = nil 

local JOIN_SOUND_ID = "rbxassetid://7460825623"
local NOTIFICATION_SOUND_ID = "rbxassetid://107978604605265"
local JoinEventId = LocalPlayer.UserId .. "_" .. tostring(os.time()) .. "_" .. tostring(math.random(100000, 999999))

local ActiveTab = "GLOBAL"




local baseStickers = {
    "rbxthumb://type=Asset&id=125983689093696&w=420&h=420",
    "rbxthumb://type=Asset&id=75506307767417&w=420&h=420",
    "rbxthumb://type=Asset&id=116836682474524&w=420&h=420",
    "rbxthumb://type=Asset&id=122120065074662&w=420&h=420",
    "rbxthumb://type=Asset&id=78357547416322&w=420&h=420",
    "rbxthumb://type=Asset&id=91482996346477&w=420&h=420",
    "rbxthumb://type=Asset&id=102568342129627&w=420&h=420",
    "rbxthumb://type=Asset&id=95097775871116&w=420&h=420",
    "rbxthumb://type=Asset&id=112745447456329&w=420&h=420",
    "rbxthumb://type=Asset&id=106977485921472&w=420&h=420",
    "rbxthumb://type=Asset&id=121310984106237&w=420&h=420",
    "rbxthumb://type=Asset&id=105883736105171&w=420&h=420",
    "rbxthumb://type=Asset&id=72805970059033&w=420&h=420",
    "rbxthumb://type=Asset&id=103116965662563&w=420&h=420",
    "rbxthumb://type=Asset&id=81834588444558&w=420&h=420",
    "rbxthumb://type=Asset&id=107564048134950&w=420&h=420",
    "rbxthumb://type=Asset&id=79650418792128&w=420&h=420",
    "rbxthumb://type=Asset&id=139765908340185&w=420&h=420",
    "rbxthumb://type=Asset&id=126499608563370&w=420&h=420",
    "rbxthumb://type=Asset&id=85967915168146&w=420&h=420",
    "rbxthumb://type=Asset&id=11484353402&w=420&h=420",
    "rbxthumb://type=Asset&id=128534124653098&w=420&h=420",
    "rbxthumb://type=Asset&id=77449075537615&w=420&h=420",
    "rbxthumb://type=Asset&id=90254884478832&w=420&h=420",
    "rbxthumb://type=Asset&id=123559079959222&w=420&h=420",
    "rbxthumb://type=Asset&id=9253566749&w=420&h=420",
    "rbxthumb://type=Asset&id=127674638208277&w=420&h=420",
    "rbxthumb://type=Asset&id=107949395268622&w=420&h=420",
    "rbxthumb://type=Asset&id=134008967452916&w=420&h=420",
    "rbxthumb://type=Asset&id=137895709516187&w=420&h=420",
    "rbxthumb://type=Asset&id=109694830487207&w=420&h=420",
    "rbxthumb://type=Asset&id=108796531608917&w=420&h=420",
    "rbxthumb://type=Asset&id=80436863872204&w=420&h=420",
    "rbxthumb://type=Asset&id=72997216799063&w=420&h=420",
    "rbxthumb://type=Asset&id=112896137773535&w=420&h=420",
    "rbxthumb://type=Asset&id=11600511955&w=420&h=420",
    "rbxthumb://type=Asset&id=124145396344946&w=420&h=420",
    "rbxthumb://type=Asset&id=124306223039740&w=420&h=420",
    "rbxthumb://type=Asset&id=130026784817133&w=420&h=420",
    "rbxthumb://type=Asset&id=78581194773132&w=420&h=420",
    "rbxthumb://type=Asset&id=100722574482118&w=420&h=420",
    "rbxthumb://type=Asset&id=137640259267057&w=420&h=420",
    "rbxthumb://type=Asset&id=113969372822195&w=420&h=420",
    "rbxthumb://type=Asset&id=140367998030901&w=420&h=420",
    "rbxthumb://type=Asset&id=120378246672337&w=420&h=420",
    "rbxthumb://type=Asset&id=116132850112023&w=420&h=420",
    "rbxthumb://type=Asset&id=87553190789546&w=420&h=420",
    "rbxthumb://type=Asset&id=9724585364&w=420&h=420",
    "rbxthumb://type=Asset&id=124047412639555&w=420&h=420",
    "rbxthumb://type=Asset&id=106808393141208&w=420&h=420",
    "rbxthumb://type=Asset&id=78606018239829&w=420&h=420",
    "rbxthumb://type=Asset&id=112484785272486&w=420&h=420",
    "rbxthumb://type=Asset&id=12727213926&w=420&h=420",
    "rbxthumb://type=Asset&id=98756307893472&w=420&h=420",
    "rbxthumb://type=Asset&id=78558780997877&w=420&h=420",
    "rbxthumb://type=Asset&id=126996930963081&w=420&h=420",
    "rbxthumb://type=Asset&id=115062400395026&w=420&h=420",
    "rbxthumb://type=Asset&id=90286953844894&w=420&h=420",
    "rbxthumb://type=Asset&id=73924170108274&w=420&h=420",
    "rbxthumb://type=Asset&id=111616336841381&w=420&h=420",
    "rbxthumb://type=Asset&id=112758420253845&w=420&h=420",
    "rbxthumb://type=Asset&id=126835535148309&w=420&h=420",
    "rbxthumb://type=Asset&id=93292021532883&w=420&h=420",
    "rbxthumb://type=Asset&id=130819442854007&w=420&h=420",
    "rbxthumb://type=Asset&id=138527421116380&w=420&h=420",
    "rbxthumb://type=Asset&id=107542712869273&w=420&h=420",
    "rbxthumb://type=Asset&id=107563628196607&w=420&h=420",
    "rbxthumb://type=Asset&id=9077479347&w=420&h=420",
    "rbxthumb://type=Asset&id=108283809544243&w=420&h=420",
    "rbxthumb://type=Asset&id=125437592584677&w=420&h=420",
    "rbxthumb://type=Asset&id=88450780656334&w=420&h=420",
    "rbxthumb://type=Asset&id=76757365751988&w=420&h=420",
    "rbxthumb://type=Asset&id=128965568153551&w=420&h=420",
    "rbxthumb://type=Asset&id=81437059666138&w=420&h=420",
    "rbxthumb://type=Asset&id=134676873059671&w=420&h=420",
    "rbxthumb://type=Asset&id=16780417621&w=420&h=420",
    "rbxthumb://type=Asset&id=81071566386630&w=420&h=420",
    "rbxthumb://type=Asset&id=100703525294618&w=420&h=420",
    "rbxthumb://type=Asset&id=86278563388796&w=420&h=420",
    "rbxthumb://type=Asset&id=87239554144904&w=420&h=420",
    "rbxthumb://type=Asset&id=129607195776931&w=420&h=420",
    "rbxthumb://type=Asset&id=99467189295335&w=420&h=420",
    "rbxthumb://type=Asset&id=106503865025496&w=420&h=420",
    "rbxthumb://type=Asset&id=71112430314329&w=420&h=420",
    "rbxthumb://type=Asset&id=98946997706325&w=420&h=420",
    "rbxthumb://type=Asset&id=95770046851932&w=420&h=420",
    "rbxthumb://type=Asset&id=73107616141016&w=420&h=420",
    "rbxthumb://type=Asset&id=117686693787446&w=420&h=420",
    "rbxthumb://type=Asset&id=15613479805&w=420&h=420",
    "rbxthumb://type=Asset&id=97531898393056&w=420&h=420",
    "rbxthumb://type=Asset&id=116963906560222&w=420&h=420",
    "rbxthumb://type=Asset&id=124962139890506&w=420&h=420",
    "rbxthumb://type=Asset&id=97109606724496&w=420&h=420",
    "rbxthumb://type=Asset&id=95541335353452&w=420&h=420",
    "rbxthumb://type=Asset&id=74143981281416&w=420&h=420",
    "rbxthumb://type=Asset&id=87427671653674&w=420&h=420",
    "rbxthumb://type=Asset&id=137157157388660&w=420&h=420",
    "rbxthumb://type=Asset&id=134368064124456&w=420&h=420",
    "rbxthumb://type=Asset&id=115496076924304&w=420&h=420",
    "rbxthumb://type=Asset&id=102923210652647&w=420&h=420",
    "rbxthumb://type=Asset&id=9703752867&w=420&h=420",
    "rbxthumb://type=Asset&id=17285946660&w=420&h=420",
    "rbxthumb://type=Asset&id=115766338835906&w=420&h=420",
    "rbxthumb://type=Asset&id=71036015366121&w=420&h=420",
    "rbxthumb://type=Asset&id=87693756574279&w=420&h=420",
    "rbxthumb://type=Asset&id=107639548994590&w=420&h=420",
    "rbxthumb://type=Asset&id=123881607288205&w=420&h=420",
    "rbxthumb://type=Asset&id=105612301516352&w=420&h=420",
    "rbxthumb://type=Asset&id=117799232246450&w=420&h=420",
    "rbxthumb://type=Asset&id=137162029636088&w=420&h=420",
    "rbxthumb://type=Asset&id=96967420548620&w=420&h=420",
    "rbxthumb://type=Asset&id=119307188585682&w=420&h=420",
    "rbxthumb://type=Asset&id=109467891240625&w=420&h=420",
    "rbxthumb://type=Asset&id=126255767092322&w=420&h=420",
    "rbxthumb://type=Asset&id=12604237076&w=420&h=420",
    "rbxthumb://type=Asset&id=91059306531671&w=420&h=420",
    "rbxthumb://type=Asset&id=186505196&w=420&h=420",
    "rbxthumb://type=Asset&id=113540856419753&w=420&h=420",
    "rbxthumb://type=Asset&id=77982313163011&w=420&h=420",
    "rbxthumb://type=Asset&id=136413255954959&w=420&h=420",
    "rbxthumb://type=Asset&id=97878152712841&w=420&h=420",
    "rbxthumb://type=Asset&id=10851678031&w=420&h=420",
    "rbxthumb://type=Asset&id=107712985832036&w=420&h=420",
    "rbxthumb://type=Asset&id=89724301020249&w=420&h=420",
    "rbxthumb://type=Asset&id=15228440984&w=420&h=420",
    "rbxthumb://type=Asset&id=123774516974893&w=420&h=420",
    "rbxthumb://type=Asset&id=101322904869821&w=420&h=420",
    "rbxthumb://type=Asset&id=124967996816135&w=420&h=420",
    "rbxthumb://type=Asset&id=96486531955234&w=420&h=420",
    "rbxthumb://type=Asset&id=129040222524985&w=420&h=420",
    "rbxthumb://type=Asset&id=110742598347676&w=420&h=420",
    "rbxthumb://type=Asset&id=6892957089&w=420&h=420",
    "rbxthumb://type=Asset&id=86402817297314&w=420&h=420",
    "rbxthumb://type=Asset&id=11123209169&w=420&h=420",
    "rbxthumb://type=Asset&id=122820594186443&w=420&h=420",
    "rbxthumb://type=Asset&id=105698532231334&w=420&h=420",
    "rbxthumb://type=Asset&id=114620995901131&w=420&h=420",
    "rbxthumb://type=Asset&id=85407192167743&w=420&h=420",
    "rbxthumb://type=Asset&id=98702128731326&w=420&h=420",
    "rbxthumb://type=Asset&id=102113722816584&w=420&h=420",
    "rbxthumb://type=Asset&id=83606808239727&w=420&h=420",
    "rbxthumb://type=Asset&id=94283114531364&w=420&h=420",
    "rbxthumb://type=Asset&id=9589322578&w=420&h=420",
    "rbxthumb://type=Asset&id=76312255766028&w=420&h=420",
    "rbxthumb://type=Asset&id=83060513543110&w=420&h=420",
    "rbxthumb://type=Asset&id=136887113512942&w=420&h=420",
    "rbxthumb://type=Asset&id=135700976643537&w=420&h=420",
    "rbxthumb://type=Asset&id=106330502110486&w=420&h=420",
    "rbxthumb://type=Asset&id=75030286063678&w=420&h=420",
    "rbxthumb://type=Asset&id=14750467200&w=420&h=420",
    "rbxthumb://type=Asset&id=91965007204396&w=420&h=420",
    "rbxthumb://type=Asset&id=128748009633926&w=420&h=420",
    "rbxthumb://type=Asset&id=169900042&w=420&h=420",
    "rbxthumb://type=Asset&id=72793460663497&w=420&h=420",
    "rbxthumb://type=Asset&id=140484578433196&w=420&h=420",
    "rbxthumb://type=Asset&id=112907096812107&w=420&h=420",
    "rbxthumb://type=Asset&id=92850410471479&w=420&h=420",
    "rbxthumb://type=Asset&id=130605412870116&w=420&h=420",
    "rbxthumb://type=Asset&id=75588875972100&w=420&h=420",
    "rbxthumb://type=Asset&id=134202774319269&w=420&h=420",
    "rbxthumb://type=Asset&id=84718406053016&w=420&h=420",
    "rbxthumb://type=Asset&id=96930545429149&w=420&h=420",
    "rbxthumb://type=Asset&id=17343896730&w=420&h=420",
    "rbxthumb://type=Asset&id=71442089462191&w=420&h=420",
    "rbxthumb://type=Asset&id=99802803824359&w=420&h=420",
    "rbxthumb://type=Asset&id=100982607210447&w=420&h=420",
    "rbxthumb://type=Asset&id=6158261961&w=420&h=420",
    "rbxthumb://type=Asset&id=99340040246135&w=420&h=420",
    "rbxthumb://type=Asset&id=29347007&w=420&h=420",
    "rbxthumb://type=Asset&id=133801475498371&w=420&h=420",
    "rbxthumb://type=Asset&id=15757126814&w=420&h=420",
    "rbxthumb://type=Asset&id=129493127513840&w=420&h=420",
    "rbxthumb://type=Asset&id=134238049027906&w=420&h=420",
    "rbxthumb://type=Asset&id=90357041968118&w=420&h=420",
    "rbxthumb://type=Asset&id=116671713835756&w=420&h=420",
    "rbxthumb://type=Asset&id=360554424&w=420&h=420",
    "rbxthumb://type=Asset&id=72982331023306&w=420&h=420",
    "rbxthumb://type=Asset&id=77115548103922&w=420&h=420",
    "rbxthumb://type=Asset&id=6892957751&w=420&h=420",
    "rbxthumb://type=Asset&id=95112780943766&w=420&h=420",
    
    "rbxthumb://type=Asset&id=126155452969559&w=420&h=420",
    "rbxthumb://type=Asset&id=76528918733148&w=420&h=420",
    "rbxthumb://type=Asset&id=139746534721570&w=420&h=420",
    "rbxthumb://type=Asset&id=107882158860216&w=420&h=420",
    "rbxthumb://type=Asset&id=114738142020573&w=420&h=420",
    "rbxthumb://type=Asset&id=100644268219896&w=420&h=420",
    "rbxthumb://type=Asset&id=82732486060449&w=420&h=420",
    "rbxthumb://type=Asset&id=89860739826332&w=420&h=420",
    "rbxthumb://type=Asset&id=85611228914039&w=420&h=420",
    "rbxthumb://type=Asset&id=126857592936719&w=420&h=420",
    "rbxthumb://type=Asset&id=120233374498907&w=420&h=420",
    "rbxthumb://type=Asset&id=138611043437875&w=420&h=420",
    "rbxthumb://type=Asset&id=33230128&w=420&h=420",
    "rbxthumb://type=Asset&id=104379643177016&w=420&h=420",
    "rbxthumb://type=Asset&id=139529399352583&w=420&h=420",
    "rbxthumb://type=Asset&id=33200310&w=420&h=420",
    "rbxthumb://type=Asset&id=33200394&w=420&h=420",
    "rbxthumb://type=Asset&id=250852269&w=420&h=420",
    "rbxthumb://type=Asset&id=146447101&w=420&h=420",
    "rbxthumb://type=Asset&id=36061967&w=420&h=420",
    "rbxthumb://type=Asset&id=31325913&w=420&h=420",
    "rbxthumb://type=Asset&id=15805629980&w=420&h=420",
    "rbxthumb://type=Asset&id=16022747924&w=420&h=420",
    "rbxthumb://type=Asset&id=72687512455219&w=420&h=420",
    "rbxthumb://type=Asset&id=15995320422&w=420&h=420",
    "rbxthumb://type=Asset&id=81506012118866&w=420&h=420",
    "rbxthumb://type=Asset&id=97857646756275&w=420&h=420",
    "rbxthumb://type=Asset&id=77414899898218&w=420&h=420",
    "rbxthumb://type=Asset&id=2606484655&w=420&h=420",
    "rbxthumb://type=Asset&id=947554872&w=420&h=420",
    "rbxthumb://type=Asset&id=7077501904&w=420&h=420",
    "rbxthumb://type=Asset&id=22743612&w=420&h=420",
    "rbxthumb://type=Asset&id=125942884957668&w=420&h=420",
    "rbxthumb://type=Asset&id=131728403495824&w=420&h=420",
    "rbxthumb://type=Asset&id=213127981&w=420&h=420",
    "rbxthumb://type=Asset&id=74300261807862&w=420&h=420",
    "rbxthumb://type=Asset&id=15908931002&w=420&h=420",
    "rbxthumb://type=Asset&id=18932008974&w=420&h=420",
    "rbxthumb://type=Asset&id=126392350195389&w=420&h=420",
    "rbxthumb://type=Asset&id=1301532317&w=420&h=420",
    "rbxthumb://type=Asset&id=11773019072&w=420&h=420",
    "rbxthumb://type=Asset&id=11660423573&w=420&h=420",
    "rbxthumb://type=Asset&id=109412457381204&w=420&h=420",
    "rbxthumb://type=Asset&id=32951426&w=420&h=420",
    "rbxthumb://type=Asset&id=10285293170&w=420&h=420",
    "rbxthumb://type=Asset&id=11254436924&w=420&h=420",
    "rbxthumb://type=Asset&id=113682666817136&w=420&h=420",
    "rbxthumb://type=Asset&id=18306429198&w=420&h=420",
    "rbxthumb://type=Asset&id=83677040163363&w=420&h=420",
    "rbxthumb://type=Asset&id=92982112921274&w=420&h=420",
    "rbxthumb://type=Asset&id=75985897964190&w=420&h=420",
    "rbxthumb://type=Asset&id=91965007204396&w=420&h=420",
    "rbxthumb://type=Asset&id=108550928457595&w=420&h=420",
    "rbxthumb://type=Asset&id=112758420253845&w=420&h=420",
    "rbxthumb://type=Asset&id=135700976643537&w=420&h=420",
    "rbxthumb://type=Asset&id=124145396344946&w=420&h=420",
    "rbxthumb://type=Asset&id=83671542566237&w=420&h=420",
    "rbxthumb://type=Asset&id=6569741166&w=420&h=420",
    "rbxthumb://type=Asset&id=11607296674&w=420&h=420",
    "rbxthumb://type=Asset&id=5726383129&w=420&h=420",
    "rbxthumb://type=Asset&id=5422504072&w=420&h=420",
    "rbxthumb://type=Asset&id=1312653856&w=420&h=420",
    "rbxthumb://type=Asset&id=9181273959&w=420&h=420",
    "rbxthumb://type=Asset&id=3512459976&w=420&h=420",
    "rbxthumb://type=Asset&id=9897870796&w=420&h=420",
    "rbxthumb://type=Asset&id=19478788&w=420&h=420",
    "rbxthumb://type=Asset&id=11345695781&w=420&h=420",
    "rbxthumb://type=Asset&id=6991349714&w=420&h=420",
    "rbxthumb://type=Asset&id=6615496255&w=420&h=420",
    "rbxthumb://type=Asset&id=6569739866&w=420&h=420",
    "rbxthumb://type=Asset&id=7025317915&w=420&h=420",
    "rbxthumb://type=Asset&id=8377385874&w=420&h=420",
    "rbxthumb://type=Asset&id=9075557667&w=420&h=420",
    "rbxthumb://type=Asset&id=10777735185&w=420&h=420",
    "rbxthumb://type=Asset&id=12521295024&w=420&h=420",
    "rbxthumb://type=Asset&id=9075556842&w=420&h=420",
    "rbxthumb://type=Asset&id=11236157528&w=420&h=420",
    "rbxthumb://type=Asset&id=72455921520132&w=420&h=420",
    "rbxthumb://type=Asset&id=2311956970&w=420&h=420",
    "rbxthumb://type=Asset&id=10367063084&w=420&h=420",
    "rbxthumb://type=Asset&id=75074160606432&w=420&h=420",
    "rbxthumb://type=Asset&id=132266996382315&w=420&h=420"
}
local uniqueStickers = {}
local hash = {}
for _, v in ipairs(baseStickers) do
    if not hash[v] then
        table.insert(uniqueStickers, v)
        hash[v] = true
    end
end
baseStickers = uniqueStickers




local RANK_STYLES = {


    ["His🌸"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["Her🎀"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["B4DNAM"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["SMoK3R🚭"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["BR💔KEN"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["Majdoor⛑️"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["Narcissist"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["ELITE"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },
    ["VIP"] = { Tier = 1, Start = Color3.fromRGB(255, 0, 0), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 255, 255), Speed = 0.75, Mode = "RAINBOW_WHITE" },


    
    ["G0DF4THER"] = { Tier = 2, Start = Color3.fromRGB(0, 190, 255), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(120, 225, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["AUQAt.Ke.BAHR"] = { Tier = 2, Start = Color3.fromRGB(255, 20, 190), End = Color3.fromRGB(255, 0, 35), Name = Color3.fromRGB(255, 105, 175), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["MOMMy♥️"] = { Tier = 2, Start = Color3.fromRGB(255, 40, 150), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 160, 220), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["DaDDy🎅"] = { Tier = 2, Start = Color3.fromRGB(255, 105, 0), End = Color3.fromRGB(35, 35, 35), Name = Color3.fromRGB(255, 175, 90), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["Abusive🪓"] = { Tier = 2, Start = Color3.fromRGB(255, 145, 30), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 195, 105), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["MALKIN🌸"] = { Tier = 2, Start = Color3.fromRGB(255, 20, 150), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 150, 220), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["ToUR NaL SH4Da"] = { Tier = 2, Start = Color3.fromRGB(170, 75, 255), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(215, 170, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["DISB4ND3R⛓️‍💥"] = { Tier = 2, Start = Color3.fromRGB(0, 210, 255), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(110, 235, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["H4CK3R"] = { Tier = 2, Start = Color3.fromRGB(55, 235, 105), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(145, 255, 175), Speed = 0.8, Mode = "PRIMARY_SECONDARY" },
    ["Mod Staff"] = { Tier = 2, Start = Color3.fromRGB(70, 145, 255), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(155, 200, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 11 },
    ["__दानव__"] = { Tier = 2, Start = Color3.fromRGB(105, 70, 235), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(175, 145, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 18 },
    ["__देवता__"] = { Tier = 2, Start = Color3.fromRGB(255, 220, 65), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 240, 130), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 18 },
    ["__खलनायक__"] = { Tier = 2, Start = Color3.fromRGB(225, 70, 235), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(245, 155, 250), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 18 },
    ["__जल्लाद__"] = { Tier = 2, Start = Color3.fromRGB(235, 65, 65), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 150, 150), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 18 },
    ["BADMASH🚬"] = { Tier = 2, Start = Color3.fromRGB(0, 205, 170), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(105, 240, 215), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["D1SGR4CeD OnE"] = { Tier = 2, Start = Color3.fromRGB(75, 125, 255), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(150, 190, 255), Speed = 0.85, Mode = "PRIMARY_SECONDARY" },
    ["CoM GoD"] = { Tier = 2, Start = Color3.fromRGB(255, 190, 45), End = Color3.fromRGB(255, 255, 255), Name = Color3.fromRGB(255, 245, 190), Speed = 0.85, Mode = "PRIMARY_SECONDARY", TextSize = 11 },


    
    ["PsyChopath"] = { Tier = 3, Start = Color3.fromRGB(155, 45, 220), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(205, 110, 255), Speed = 1.0, Mode = "FONT_LOOP", TextSize = 11 },
    ["Architect"] = { Tier = 3, Start = Color3.fromRGB(255, 205, 35), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(255, 235, 100), Speed = 1.0, Mode = "FONT_LOOP", TextSize = 11 },
    ["Overseer"] = { Tier = 3, Start = Color3.fromRGB(45, 145, 255), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(120, 200, 255), Speed = 1.0, Mode = "FONT_LOOP", TextSize = 11 },
    ["~OG~"] = { Tier = 3, Start = Color3.fromRGB(255, 175, 35), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(255, 215, 95), Speed = 1.0, Mode = "FONT_LOOP", TextSize = 11 },
    ["HoNOR3D OnE"] = { Tier = 3, Start = Color3.fromRGB(0, 220, 205), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(90, 255, 240), Speed = 1.0, Mode = "FONT_LOOP", TextSize = 11 },


    
    ["BOT"] = { Tier = 99, Start = Color3.fromRGB(255, 185, 190), End = Color3.fromRGB(0, 0, 0), Name = Color3.fromRGB(255, 215, 220), Speed = 1.0, Mode = "FONT_LOOP" },
}









local function AnimateRoleFont(textObject, roleName, style)
    if not textObject or not textObject.Parent then return end
    style = style or RANK_STYLES[roleName] or RANK_STYLES["VIP"]


    for _, child in ipairs(textObject:GetChildren()) do
        if child.Name == "KronosRoleGradient" or child.Name == "KronosTier3Sheen" then
            child:Destroy()
        end
    end

    local mode = style.Mode or "FONT_LOOP"
    local duration = tonumber(style.Speed) or 1.0
    local startColor = style.Start or style.Name or Color3.new(1, 1, 1)
    local endColor = style.End or Color3.new(0, 0, 0)

    if mode == "RAINBOW_WHITE" then
        
        local gradient = Instance.new("UIGradient")
        gradient.Name = "KronosRoleGradient"
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 0, 0)),
            ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255, 0, 180)),
            ColorSequenceKeypoint.new(0.33, Color3.fromRGB(70, 0, 255)),
            ColorSequenceKeypoint.new(0.50, Color3.fromRGB(0, 220, 255)),
            ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0, 255, 90)),
            ColorSequenceKeypoint.new(0.82, Color3.fromRGB(255, 240, 0)),
            ColorSequenceKeypoint.new(0.93, Color3.fromRGB(255, 70, 0)),
            ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 255, 255)),
        })
        gradient.Rotation = 0
        gradient.Parent = textObject

        task.spawn(function()
            local offset = -1
            while textObject and textObject.Parent and gradient and gradient.Parent do
                local tween = TweenService:Create(
                    gradient,
                    TweenInfo.new(duration * 2.0, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut),
                    {Offset = Vector2.new(2, 0)}
                )
                gradient.Offset = Vector2.new(offset, 0)
                tween:Play()
                tween.Completed:Wait()
                offset = -1
            end
        end)
        return
    end

    if mode == "PRIMARY_SECONDARY" then
        
        local gradient = Instance.new("UIGradient")
        gradient.Name = "KronosRoleGradient"
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0.00, startColor),
            ColorSequenceKeypoint.new(0.50, endColor),
            ColorSequenceKeypoint.new(1.00, startColor),
        })
        gradient.Rotation = 0
        gradient.Parent = textObject

        task.spawn(function()
            while textObject and textObject.Parent and gradient and gradient.Parent do
                gradient.Offset = Vector2.new(-1, 0)
                local tween = TweenService:Create(
                    gradient,
                    TweenInfo.new(duration * 2.0, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut),
                    {Offset = Vector2.new(1, 0)}
                )
                tween:Play()
                tween.Completed:Wait()
            end
        end)
        return
    end


    
    if tonumber(style.Tier) == 3 then
        textObject.TextColor3 = Color3.fromRGB(255, 255, 255)

        local rainbow = Instance.new("UIGradient")
        rainbow.Name = "KronosRoleGradient"
        rainbow.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 0, 0)),
            ColorSequenceKeypoint.new(0.10, Color3.fromRGB(255, 55, 0)),
            ColorSequenceKeypoint.new(0.20, Color3.fromRGB(255, 235, 0)),
            ColorSequenceKeypoint.new(0.30, Color3.fromRGB(70, 255, 0)),
            ColorSequenceKeypoint.new(0.40, Color3.fromRGB(0, 255, 170)),
            ColorSequenceKeypoint.new(0.50, Color3.fromRGB(0, 190, 255)),
            ColorSequenceKeypoint.new(0.60, Color3.fromRGB(35, 70, 255)),
            ColorSequenceKeypoint.new(0.70, Color3.fromRGB(145, 0, 255)),
            ColorSequenceKeypoint.new(0.80, Color3.fromRGB(255, 0, 220)),
            ColorSequenceKeypoint.new(0.90, Color3.fromRGB(255, 0, 90)),
            ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 0, 0)),
        })
        rainbow.Rotation = 0
        rainbow.Parent = textObject


        
        local sheen = Instance.new("TextLabel")
        sheen.Name = "KronosTier3Sheen"
        sheen.BackgroundTransparency = 1
        sheen.BorderSizePixel = 0
        sheen.Size = UDim2.new(1, 0, 1, 0)
        sheen.Position = UDim2.new(0, 0, 0, 0)
        sheen.AnchorPoint = Vector2.new(0, 0)
        sheen.Text = textObject.Text
        sheen.Font = textObject.Font
        sheen.TextSize = textObject.TextSize
        sheen.TextScaled = textObject.TextScaled
        sheen.TextWrapped = textObject.TextWrapped
        sheen.TextXAlignment = textObject.TextXAlignment
        sheen.TextYAlignment = textObject.TextYAlignment
        sheen.RichText = textObject.RichText
        sheen.TextStrokeTransparency = 1
        sheen.TextTransparency = 0
        sheen.ZIndex = textObject.ZIndex + 1
        sheen.Active = false
        sheen.Selectable = false
        sheen.Parent = textObject

        local sheenGradient = Instance.new("UIGradient")
        sheenGradient.Name = "KronosRoleGradient"
        sheenGradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0.00, Color3.fromRGB(0, 0, 0)),
            ColorSequenceKeypoint.new(0.35, Color3.fromRGB(0, 0, 0)),
            ColorSequenceKeypoint.new(0.50, Color3.fromRGB(255, 255, 255)),
            ColorSequenceKeypoint.new(0.65, Color3.fromRGB(0, 0, 0)),
            ColorSequenceKeypoint.new(1.00, Color3.fromRGB(0, 0, 0)),
        })
        sheenGradient.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0.00, 0.90),
            NumberSequenceKeypoint.new(0.35, 0.82),
            NumberSequenceKeypoint.new(0.50, 0.15),
            NumberSequenceKeypoint.new(0.65, 0.82),
            NumberSequenceKeypoint.new(1.00, 0.90),
        })
        sheenGradient.Rotation = 0
        sheenGradient.Parent = sheen

        task.spawn(function()
            while textObject and textObject.Parent and rainbow and rainbow.Parent do
                rainbow.Offset = Vector2.new(-1, 0)
                local rainbowTween = TweenService:Create(
                    rainbow,
                    TweenInfo.new(duration * 3.0, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut),
                    {Offset = Vector2.new(1, 0)}
                )
                rainbowTween:Play()
                rainbowTween.Completed:Wait()
            end
        end)

        task.spawn(function()
            while textObject and textObject.Parent and sheen and sheen.Parent and sheenGradient and sheenGradient.Parent do
                sheenGradient.Offset = Vector2.new(1.25, 0)
                local sheenTween = TweenService:Create(
                    sheenGradient,
                    TweenInfo.new(duration * 1.35, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut),
                    {Offset = Vector2.new(-1.25, 0)}
                )
                sheenTween:Play()
                sheenTween.Completed:Wait()
            end
        end)
        return
    end


    textObject.TextColor3 = startColor
    task.spawn(function()
        while textObject and textObject.Parent do
            local toBlack = TweenService:Create(
                textObject,
                TweenInfo.new(duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {TextColor3 = endColor}
            )
            toBlack:Play()
            toBlack.Completed:Wait()
            if not textObject or not textObject.Parent then break end

            local toStart = TweenService:Create(
                textObject,
                TweenInfo.new(duration, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {TextColor3 = startColor}
            )
            toStart:Play()
            toStart.Completed:Wait()
        end
    end)
end

local ALL_AUTOCOMPLETE_OPTIONS = { "!tag", "!role", "!spoof", "!announce ", "!mute ", "/w " }
local CurrentContextMessage = nil
local EditingContextMessage = nil
local EditingContextIsGlobal = false
local ReplyContextMessage = nil
local ReplyHighlightToken = 0
local PrivateModeTargetUsername = nil
local PrivateModeTargetDisplayName = nil
local PrivateModePill = nil
local RefreshReplyUI = function() end
local ClearPersistentPrivatePrefix = function() end

local AssignedPlayerRoles = {}
local ActiveSelectedTag = nil
local AvailableUserRoles = {}


local RoleSwitchAuthorization = {}
local RoleRosterLoaded = false
local DisabledTags = {}        
local PersistentPvtPrefix = ""

local GlobalCachedMessages, GlobalUIElements = {}, {}
local ServerCachedMessages, ServerUIElements = {}, {}



local BlockedMessageUsers = {}

local function IsMessageUserBlocked(username)
    username = tostring(username or "")
    return username ~= "" and BlockedMessageUsers[string.lower(username)] == true
end

local function RemoveBlockedUserFromLocalChat(username)
    username = tostring(username or "")
    if username == "" then return end
    local wanted = string.lower(username)

    for _, cache in ipairs({GlobalCachedMessages, ServerCachedMessages}) do
        for key, msg in pairs(cache) do
            if type(msg) == "table" and string.lower(tostring(msg.Username or "")) == wanted then
                cache[key] = nil
            end
        end
    end

    for _, list in ipairs({GlobalUIElements, ServerUIElements}) do
        for i = #list, 1, -1 do
            local card = list[i]
            if card and string.lower(tostring(card:GetAttribute("MessageUsername") or "")) == wanted then
                if card.Parent then
                    card:Destroy()
                end
                table.remove(list, i)
            elseif card and not card.Parent then
                table.remove(list, i)
            end
        end
    end
end

local function BlockMessageUser(username)
    username = tostring(username or "")
    if username == "" or string.lower(username) == string.lower(tostring(LocalPlayer.Name or "")) then
        return false
    end
    BlockedMessageUsers[string.lower(username)] = true
    RemoveBlockedUserFromLocalChat(username)
    return true
end


local Moderation = { ActiveMutes = {} }



local ANNOUNCER_NAME = "ANNOUNCER"
local ANNOUNCER_PFP_ID = "132432242608689"
local ANNOUNCEMENT_TEXT_COLOR = Color3.fromRGB(255, 255, 0) 
local ANNOUNCEMENT_TEXT_SIZE = 12

Moderation.NormalizeName = function(s)
    return string.lower(tostring(s or ""))
end

Moderation.ResolveModerationTarget = function(query)
    query = tostring(query or ""):gsub("^%s+", ""):gsub("%s+$", "")
    if query == "" then return nil end
    local needle = Moderation.NormalizeName(query)

    local function checkPlayer(player)
        local name = Moderation.NormalizeName(player.Name)
        local display = Moderation.NormalizeName(player.DisplayName)
        if name == needle or display == needle then return player.Name, player.DisplayName end
    end

    for _, player in ipairs(Players:GetPlayers()) do
        local u, d = checkPlayer(player)
        if u then return u, d end
    end

    local bestUsername, bestDisplay
    for _, player in ipairs(Players:GetPlayers()) do
        local name = Moderation.NormalizeName(player.Name)
        local display = Moderation.NormalizeName(player.DisplayName)
        if string.sub(name, 1, #needle) == needle or string.sub(display, 1, #needle) == needle then
            if not bestUsername then
                bestUsername, bestDisplay = player.Name, player.DisplayName
            end
        end
    end
    if bestUsername then return bestUsername, bestDisplay end

    for _, cache in ipairs({GlobalCachedMessages, ServerCachedMessages}) do
        for _, msg in pairs(cache) do
            if type(msg) == "table" then
                local username = tostring(msg.Username or "")
                local display = tostring(msg.DisplayName or username)
                local lu, ld = Moderation.NormalizeName(username), Moderation.NormalizeName(display)
                if lu == needle or ld == needle or string.sub(lu, 1, #needle) == needle or string.sub(ld, 1, #needle) == needle then
                    return username, display
                end
            end
        end
    end
    return nil
end

Moderation.GetMuteKey = function(scope, username)
    return tostring(scope) .. "|" .. Moderation.NormalizeName(username)
end

Moderation.GetActiveMute = function(scope, username)
    local key = Moderation.GetMuteKey(scope, username)
    local mute = Moderation.ActiveMutes[key]
    if mute and tonumber(mute.ExpiresAt or 0) > os.time() then
        return mute
    end
    Moderation.ActiveMutes[key] = nil
    return nil
end

Moderation.FormatMuteRemaining = function(seconds)
    seconds = math.max(0, math.floor(tonumber(seconds) or 0))
    local minutes = math.floor(seconds / 60)
    local secs = seconds % 60
    return string.format("%02d:%02d", minutes, secs)
end

Moderation.RefreshAllRoleAutocomplete = function()
    local optionsMap = { ["!tag"] = true, ["!role"] = true, ["!spoof"] = true, ["!announce "] = true, ["!mute "] = true, ["/w "] = true }
   
    for rankName, _ in pairs(RANK_STYLES) do
        if rankName ~= "BOT" then
            optionsMap[rankName] = true
        end
    end
   
    for playerName, roles in pairs(AvailableUserRoles) do
        for _, roleName in ipairs(roles) do
            optionsMap[roleName] = true
        end
    end

    ALL_AUTOCOMPLETE_OPTIONS = {}
    for opt, _ in pairs(optionsMap) do
        table.insert(ALL_AUTOCOMPLETE_OPTIONS, opt)
    end
end
Moderation.RefreshAllRoleAutocomplete()




local function EncodePlayerId(userId)
    local idStr = tostring(userId)
    if string.len(idStr) >= 5 then
        local first5 = string.sub(idStr, 1, 5)
        local rest = string.sub(idStr, 6)
        return rest .. first5 .. "0"
    end
    return idStr .. "0"
end




local http_req = (syn and syn.request) or (http and http.request) or http_request or request or (Fluxus and fluxus.request)

local function HttpRequest(url, method, data)
    if not http_req then return nil end
    local success, response = pcall(function()
        return http_req({
            Url = url,
            Method = method or "GET",
            Headers = { ["Content-Type"] = "application/json" },
            Body = data and HttpService:JSONEncode(data) or nil
        })
    end)

    if success and response and response.Body then
        local decodedSuccess, decodedData = pcall(function()
            return HttpService:JSONDecode(response.Body)
        end)
        return decodedSuccess and decodedData or nil
    end
    return nil
end

local function HttpRequestRaw(url)
    if not http_req then return nil end

    local success, response = pcall(function()
        return http_req({
            Url = url,
            Method = "GET",
            Headers = { ["Cache-Control"] = "no-cache" }
        })
    end)

    if success and response and response.Body then
        return tostring(response.Body)
    end

    return nil
end





MUSIC_SESSION_FIREBASE_URL = "https://kmusicsessionxd-default-rtdb.asia-southeast1.firebasedatabase.app/"
MUSIC_SESSION_ROOT = MUSIC_SESSION_FIREBASE_URL .. "music_sessions"
YOUTUBE_ENGINE_URL = "https://pastefy.app/W816biPS/raw"
KRONO_JOCKIE_NAME = "Krono Jockie"
KRONO_JOCKIE_USERNAME = "krono_jockie"
KRONO_JOCKIE_PFP = "7261787230"

ActiveMusicSessionId = nil
ActiveMusicSessionData = nil
MusicSessionMuted = false
MusicSessionLastRevision = nil
MusicSessionStopPendingId = nil
MusicSessionCurrentTrack = nil
MusicSessionEngineReady = false
MusicSessionEngineLoading = false
MusicSessionEnginePlayingUrl = nil
MusicSessionMemberCache = {}
MusicSessionLastAppliedUrl = nil

PROFILE_SAVE_PREFIX = "Kronos_Profile_"
ProfileSocials = { Instagram = "", Discord = "", Snapchat = "" }
local function LoadLocalText(key)
    if readfile and isfile and isfile(PROFILE_SAVE_PREFIX .. key .. ".txt") then
        local ok, value = pcall(function() return readfile(PROFILE_SAVE_PREFIX .. key .. ".txt") end)
        if ok and value then return value end
    end
    return ""
end
local function SaveLocalText(key, value)
    if writefile then
        pcall(function() writefile(PROFILE_SAVE_PREFIX .. key .. ".txt", tostring(value or "")) end)
    end
end
for key in pairs(ProfileSocials) do ProfileSocials[key] = LoadLocalText(key) end

SERVER_PROFILE_ROOT = SERVER_FIREBASE_URL .. "server_profiles/" .. SERVER_JOB_ID

local function MusicSessionRandomId()
    local chars = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"
    local out = ""
    for _ = 1, 8 do
        local i = math.random(1, #chars)
        out = out .. string.sub(chars, i, i)
    end
    return out
end

local function MusicSessionPath(sessionId)
    return MUSIC_SESSION_ROOT .. "/" .. tostring(sessionId) .. ".json"
end

local function MusicSessionUrl(sessionId, suffix)
    return MUSIC_SESSION_ROOT .. "/" .. tostring(sessionId) .. (suffix or "") .. ".json"
end

local function MusicSessionIsHost()
    return ActiveMusicSessionData and ActiveMusicSessionData.HostUsername == LocalPlayer.Name
end

local function MusicSessionRevision()
    return tostring(os.time()) .. "_" .. tostring(math.random(100000, 999999))
end

local function NormalizeYouTubeUrl(url)
    url = tostring(url or ""):match("^%s*(.-)%s*$") or ""
    if url == "" then return nil end
    if url:match("^https?://") then return url end
    return nil
end

local function GetYouTubeVideoId(url)
    url = tostring(url or "")
    return url:match("[?&]v=([%w_-]+)") or url:match("youtu%.be/([%w_-]+)") or url:match("youtube%.com/shorts/([%w_-]+)")
end

local function IsYouTubePlaylistUrl(url)
    return tostring(url or ""):find("[?&]list=[%w_-]+") ~= nil
end

local function GetYouTubePlaylistId(url)
    return tostring(url or ""):match("[?&]list=([%w_-]+)")
end

local function GetYouTubeTitle(url)
    local encoded = tostring(url or ""):gsub(" ", "%%20")
    local data = HttpRequest("https://www.youtube.com/oembed?url=" .. encoded .. "&format=json", "GET")
    if type(data) == "table" and data.title then return tostring(data.title) end
    return nil
end

local function ExtractPlaylistVideoUrls(url)
    local playlistId = GetYouTubePlaylistId(url)
    if not playlistId then return nil, "Invalid YouTube playlist URL." end
    local html = HttpRequestRaw("https://www.youtube.com/playlist?list=" .. playlistId)
    if not html or html == "" then return nil, "Unable to read the YouTube playlist." end
    local urls, seen = {}, {}
    for videoId in tostring(html):gmatch('"videoId":"([%w_-]+)"') do
        if not seen[videoId] then
            seen[videoId] = true
            table.insert(urls, "https://www.youtube.com/watch?v=" .. videoId)
        end
        if #urls >= 100 then break end
    end
    if #urls == 0 then return nil, "No playable videos were found in that playlist." end
    return urls
end

local YouTubeEngineGui
local YouTubeEngineInput
local YouTubeEngineButton

local function HideYouTubeEngineGui(gui)
    if not gui then return end
    for _, child in ipairs(gui:GetChildren()) do
        if child:IsA("Frame") then child.Visible = false end
    end
end

local function FindYouTubeEngineControls(gui)
    if not gui then return end
    local input, button
    for _, desc in ipairs(gui:GetDescendants()) do
        if desc:IsA("TextBox") and tostring(desc.PlaceholderText):lower():find("paste youtube", 1, true) then
            input = desc
            break
        end
    end
    if input and input.Parent then
        for _, child in ipairs(input.Parent:GetChildren()) do
            if child:IsA("ImageButton") then
                button = child
                break
            end
        end
    end
    YouTubeEngineInput = input
    YouTubeEngineButton = button
end

local function EnsureYouTubeEngine()
    if YouTubeEngineInput and YouTubeEngineButton and YouTubeEngineInput.Parent then
        return true
    end
    if MusicSessionEngineLoading then
        return false
    end

    MusicSessionEngineLoading = true
    local ok, result = pcall(function()
        local existing = LocalPlayer.PlayerGui:FindFirstChild("TATrMG")
        if existing then
            return existing
        end
        local raw = game:HttpGet(YOUTUBE_ENGINE_URL)
        local fn, err = loadstring(raw)
        if not fn then error(err) end
        task.spawn(fn)
        return LocalPlayer.PlayerGui:WaitForChild("TATrMG", 12)
    end)

    if ok and result then
        YouTubeEngineGui = result
        HideYouTubeEngineGui(result)
        FindYouTubeEngineControls(result)
        if not YouTubeEngineInput or not YouTubeEngineButton then
            YouTubeEngineGui = nil
        end
    end
    MusicSessionEngineLoading = false
    return YouTubeEngineInput ~= nil and YouTubeEngineButton ~= nil
end

local function TriggerYouTubeEngine(url)
    url = NormalizeYouTubeUrl(url)
    if not url then return false end
    if not EnsureYouTubeEngine() then return false end

    YouTubeEngineInput.Text = url
    local fired = false
    pcall(function()
        for _, conn in ipairs(getconnections(YouTubeEngineButton.MouseButton1Click)) do
            if conn.Function then
                conn:Fire()
                fired = true
            end
        end
    end)
    if not fired then
        pcall(function() YouTubeEngineButton:Activate() fired = true end)
    end
    return fired
end

local function StopLocalMusicSessionAudio()
    if MusicSessionCurrentTrack then
        pcall(function() MusicSessionCurrentTrack:Stop() end)
    end
    MusicSessionCurrentTrack = nil
    MusicSessionEnginePlayingUrl = nil
    MusicSessionLastAppliedUrl = nil
end

local function FindEngineTrack(previousSounds)
    local function scan(container)
        if not container then return nil end
        for _, desc in ipairs(container:GetDescendants()) do
            if desc:IsA("Sound") and desc.IsPlaying then
                local previous = previousSounds[desc]
                if not previous or previous.SoundId ~= desc.SoundId or not previous.IsPlaying then
                    return desc
                end
            end
        end
        return nil
    end
    return scan(workspace) or scan(SoundService)
end

local function WaitForEngineTrack(url, timeout)
    local previous = {}
    for _, container in ipairs({workspace, SoundService}) do
        for _, desc in ipairs(container:GetDescendants()) do
            if desc:IsA("Sound") then
                previous[desc] = { SoundId = desc.SoundId, IsPlaying = desc.IsPlaying }
            end
        end
    end

    local deadline = os.clock() + (timeout or 20)
    while os.clock() < deadline do
        local track = FindEngineTrack(previous)
        if track then return track end
        task.wait(0.25)
    end
    return nil
end

local function StartYouTubeTrack(url, position)
    url = NormalizeYouTubeUrl(url)
    if not url then return false end
    if MusicSessionEnginePlayingUrl == url and MusicSessionCurrentTrack then
        if position and position > 0 then
            pcall(function() MusicSessionCurrentTrack.TimePosition = position end)
        end
        pcall(function() MusicSessionCurrentTrack:Play() end)
        return true
    end

    StopLocalMusicSessionAudio()
    MusicSessionEnginePlayingUrl = url
    MusicSessionLastAppliedUrl = url
    if not TriggerYouTubeEngine(url) then
        MusicSessionEnginePlayingUrl = nil
        return false
    end

    task.spawn(function()
        local track = WaitForEngineTrack(url, 20)
        if not track or MusicSessionEnginePlayingUrl ~= url then return end
        MusicSessionCurrentTrack = track
        pcall(function()
            track.Volume = MusicSessionMuted and 0 or 0.5
            if position and position > 0 then track.TimePosition = position end
        end)
        if MusicSessionIsHost() then
            track.Ended:Connect(function()
                if ActiveMusicSessionData and MusicSessionIsHost() and MusicSessionEnginePlayingUrl == url then
                    task.defer(HostNextMusic)
                end
            end)
        end
    end)
    return true
end

local function GetMusicQueue(data)
    if type(data) ~= "table" or type(data.Queue) ~= "table" then return {} end
    local queue = {}
    for _, item in ipairs(data.Queue) do
        if type(item) == "table" and item.Url then table.insert(queue, item) end
    end
    return queue
end

local function GetMusicCurrentItem(data)
    local queue = GetMusicQueue(data)
    local index = tonumber(data and data.CurrentIndex) or 0
    return queue[index], index, #queue
end

local function GetMusicPosition(data)
    if type(data) ~= "table" then return 0 end
    local base = tonumber(data.Position or 0) or 0
    if data.Paused then return math.max(0, base) end
    local startedAt = tonumber(data.StartedAt or 0) or 0
    if startedAt <= 0 then return math.max(0, base) end
    return math.max(0, base + (os.time() - startedAt))
end

local function ApplyMusicSessionAudio(session)
    if type(session) ~= "table" then
        StopLocalMusicSessionAudio()
        return
    end
    local item = GetMusicCurrentItem(session)
    if not item or not session.Playing then
        if session.Paused and item and MusicSessionCurrentTrack then
            pcall(function() MusicSessionCurrentTrack:Pause() end)
        elseif not session.Playing then
            StopLocalMusicSessionAudio()
        end
        return
    end

    local url = NormalizeYouTubeUrl(item.Url)
    if not url then
        StopLocalMusicSessionAudio()
        return
    end

    local position = GetMusicPosition(session)
    if MusicSessionLastAppliedUrl ~= url or not MusicSessionCurrentTrack then
        StartYouTubeTrack(url, position)
    else
        pcall(function()
            MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5
            if not MusicSessionCurrentTrack.IsPlaying then MusicSessionCurrentTrack:Play() end
        end)
    end
end

local function ResetMusicSessionToChoiceMenu()
    StopLocalMusicSessionAudio()
    ActiveMusicSessionId = nil
    ActiveMusicSessionData = nil
    MusicSessionLastRevision = nil
    MusicSessionStopPendingId = nil
    MusicSessionMuted = false
    MusicSessionMemberCache = {}

    pcall(function()
        if MusicSessionCurrentTrack then MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5 end
    end)
    pcall(function()
        MusicSessionFrame.Visible = true
        MusicSessionFrame.CanvasPosition = Vector2.new(0, 0)
        MusicCreatePanel.Visible = false
        MusicJoinPanel.Visible = false
        MusicMuteButton.Visible = false
        MusicJoinMuteButton.Visible = false
        MusicChoiceTitle.Visible = true
        MusicCreateChoiceButton.Visible = true
        MusicJoinChoiceButton.Visible = true
        MusicChoiceHint.Visible = true
        MusicSessionIdInput.Text = ""
        MusicNowPlayingLabel.Text = "Not in a music session"
        MusicHostLabel.Text = "Host: —"
        MusicSessionIdLabel.Text = "Session ID: —"
        MusicJoinMuteButton.Text = "🔊 MUTE MY AUDIO"
        MusicMuteButton.Text = "🔊 MUTE"
    end)
end

local function RefreshMusicSessionMembers()
    if not ActiveMusicSessionId then return end
    local members = HttpRequest(MusicSessionUrl(ActiveMusicSessionId, "/members"), "GET")
    if type(members) ~= "table" then members = {} end
    MusicSessionMemberCache = members
    if UpdateMusicSessionMembersUI then UpdateMusicSessionMembersUI() end
end

local function RefreshMusicSessionState()
    if not ActiveMusicSessionId then
        StopLocalMusicSessionAudio()
        return
    end

    local sessionIdAtRequest = ActiveMusicSessionId
    local data = HttpRequest(MusicSessionPath(sessionIdAtRequest), "GET")
    if type(data) ~= "table" or sessionIdAtRequest ~= ActiveMusicSessionId then return end

    local kick = HttpRequest(MusicSessionUrl(sessionIdAtRequest, "/kicks/" .. tostring(LocalPlayer.UserId)), "GET")
    if kick ~= nil then
        StopLocalMusicSessionAudio()
        ShowLocalSystemMessage("[AI MoD] : You were kicked from Music Session " .. sessionIdAtRequest, true)
        ResetMusicSessionToChoiceMenu()
        return
    end

    if data.Ended == true then
        ResetMusicSessionToChoiceMenu()
        return
    end

    ActiveMusicSessionData = data
    local revision = tostring(data.Revision or data.UpdatedAt or data.CurrentIndex or data.Playing or data.Paused)

    if data.Playing == true and not data.Paused then
        if revision ~= MusicSessionLastRevision or not MusicSessionCurrentTrack or not MusicSessionCurrentTrack.IsPlaying then
            MusicSessionLastRevision = revision
            ApplyMusicSessionAudio(data)
        elseif MusicSessionCurrentTrack then
            pcall(function() MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5 end)
        end
    elseif data.Paused == true then
        MusicSessionLastRevision = revision
        if MusicSessionCurrentTrack then
            pcall(function()
                MusicSessionCurrentTrack:Pause()
                MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5
            end)
        end
    else
        MusicSessionLastRevision = revision
        StopLocalMusicSessionAudio()
    end

    UpdateMusicSessionDisplay()
    UpdateMusicSessionMode()
    RefreshMusicSessionMembers()
end

task.spawn(function()
    while true do
        pcall(RefreshMusicSessionState)
        task.wait(1.25)
    end
end)

local ROLE_PRIORITY = {
    
    "His🌸", "Her🎀", "B4DNAM", "SMoK3R🚭", "BR💔KEN", "Majdoor⛑️",
    "Narcissist", "ELITE", "VIP",


    "G0DF4THER", "AUQAt.Ke.BAHR", "MOMMy♥️", "DaDDy🎅", "Abusive🪓",
    "MALKIN🌸", "ToUR NaL SH4Da", "DISB4ND3R⛓️‍💥", "H4CK3R", "Mod Staff",
    "__दानव__", "__देवता__", "__खलनायक__", "__जल्लाद__", "BADMASH🚬", "D1SGR4CeD OnE", "CoM GoD",


    "PsyChopath", "Architect", "Overseer", "~OG~", "HoNOR3D OnE"
}

local ROLE_PRIORITY_INDEX = {}
for index, roleName in ipairs(ROLE_PRIORITY) do ROLE_PRIORITY_INDEX[roleName] = index end

local ROLE_PERMISSIONS = {}

local function RoleNameEquals(a, b)
    return string.lower(tostring(a or "")) == string.lower(tostring(b or ""))
end

local function NormalizeRoleLookupName(roleName)
    local normalized = string.lower(tostring(roleName or ""))
    normalized = normalized:gsub("·", " ")
    normalized = normalized:gsub("%.", " ")
    normalized = normalized:gsub("%s+", " ")
    return normalized:match("^%s*(.-)%s*$") or normalized
end

local LEGACY_ROLE_ALIASES = {
    [NormalizeRoleLookupName("CoM GoD🔗")] = "CoM GoD",
    [NormalizeRoleLookupName("PsYchopath")] = "PsyChopath",
    [NormalizeRoleLookupName("HoN0R3D OnE")] = "HoNOR3D OnE",
}

local function FindCanonicalRole(roleName)
    local wanted = NormalizeRoleLookupName(roleName)
    local alias = LEGACY_ROLE_ALIASES[wanted]
    if alias then return alias end
    for _, candidate in ipairs(ROLE_PRIORITY) do
        if NormalizeRoleLookupName(candidate) == wanted then return candidate end
    end
    return nil
end

local function GetPlayerRoles(username)
    return AvailableUserRoles[username] or {}
end

local function GetHighestRole(username)
    local roles = GetPlayerRoles(username)
    local best, bestIndex
    for _, roleName in ipairs(roles) do
        local canonical = FindCanonicalRole(roleName) or roleName
        local idx = ROLE_PRIORITY_INDEX[canonical] or 9999
        if not bestIndex or idx < bestIndex then
            best, bestIndex = canonical, idx
        end
    end
    return best or "VIP"
end

local function PlayerHasRole(username, roleName)
    local wanted = FindCanonicalRole(roleName) or roleName
    for _, assignedRole in ipairs(GetPlayerRoles(username)) do
        local canonicalAssigned = FindCanonicalRole(assignedRole) or assignedRole
        if RoleNameEquals(canonicalAssigned, wanted) then return true end
    end
    return false
end

local function GetAssignedOrCalculatedRole(username)
    local selected = AssignedPlayerRoles[username]
    if selected then
        local canonicalSelected = FindCanonicalRole(selected) or selected
        local selectedStyle = RANK_STYLES[canonicalSelected]
        if type(selectedStyle) == "table" then
            local tier = tonumber(selectedStyle.Tier)


            
            if tier == 1 then
                return canonicalSelected
            end


            
            if (tier == 2 or tier == 3) and PlayerHasRole(username, canonicalSelected) then
                return canonicalSelected
            end
        end
    end


    
    if username == LocalPlayer.Name and ActiveSelectedTag then
        local activeCanonical = FindCanonicalRole(ActiveSelectedTag) or ActiveSelectedTag
        local activeStyle = RANK_STYLES[activeCanonical]
        if type(activeStyle) == "table" and tonumber(activeStyle.Tier) == 1 then
            return activeCanonical
        end
    end

    return GetHighestRole(username)
end

local function GetLocalChatRole()
    local selected = AssignedPlayerRoles[LocalPlayer.Name]
    if selected then
        local canonical = FindCanonicalRole(selected) or selected
        local style = RANK_STYLES[canonical]
        if type(style) == "table" then
            local tier = tonumber(style.Tier)
            if tier == 1 then
                return canonical
            elseif (tier == 2 or tier == 3) and PlayerHasRole(LocalPlayer.Name, canonical) then
                return canonical
            end
        end
    end

    return GetAssignedOrCalculatedRole(LocalPlayer.Name)
end

local function CanSwitchToRole(roleName)
    local canonical = FindCanonicalRole(roleName)
    local style = canonical and RANK_STYLES[canonical] or nil
    if not canonical or type(style) ~= "table" then
        return false
    end


    if tonumber(style.Tier) == 1 then
        return true
    end


    if tonumber(style.Tier) > 3 then
        return false
    end




    if not RoleRosterLoaded then
        return false
    end

    local authorization = RoleSwitchAuthorization[canonical]
    if not authorization then
        return false
    end

    local usernameKey = string.lower(tostring(LocalPlayer.Name or ""))
    local userIdKey = tostring(LocalPlayer.UserId)
    return authorization.Usernames[usernameKey] == true
        and authorization.UserIds[userIdKey] == true
end





















local COMMAND_ROSTER_URL = "https://raw.githubusercontent.com/basementfox98/KroCha/refs/heads/main/AudioIDduplicate"

local CommandRoster = {}
local CommandRosterLoaded = false

local COMMAND_ROSTER_SLOTS = {
    Spoof = "Join Sound",
    Announce = "Notification sound",
    Mute = "Join sound 2",
    PlayMusic = "INTRO sound",
    Kick = "Ambient sound",
    Ban = "Server sound",
}

local function NormalizeRosterId(value)
    local s = tostring(value or ""):match("^%s*(%d+)%s*$")
    if not s then return nil end


    s = s:gsub("^0+", "")
    if s == "" then s = "0" end
    if s == "0" then return nil end

    return s
end

local function AddRosterId(target, value)
    local id = NormalizeRosterId(value)
    if id then
        target[id] = true
    end
end

local function AddRosterIds(target, value)
    if type(value) == "table" then
        for _, item in pairs(value) do
            AddRosterIds(target, item)
        end
    else
        AddRosterId(target, value)
    end
end

local function ExtractIdsFromRosterText(rawText, slotName, allSlotNames)
    local ids = {}

    local escaped = slotName:gsub("([^%w])", "%%%1")
    local startAt = rawText:find(escaped)
    if not startAt then
        return ids
    end

    local sectionStart = rawText:find("[{[]", startAt + #slotName)
    if not sectionStart then
        return ids
    end

    local sectionEnd = #rawText + 1

    for _, otherName in ipairs(allSlotNames) do
        if otherName ~= slotName then
            local otherEscaped = otherName:gsub("([^%w])", "%%%1")
            local p = rawText:find(otherEscaped, sectionStart + 1)
            if p and p < sectionEnd then
                sectionEnd = p
            end
        end
    end

    local section = rawText:sub(sectionStart, sectionEnd - 1)


    for numberText in section:gmatch("%d+") do
        AddRosterId(ids, numberText)
    end

    return ids
end

local function BuildCommandRoster(rawJson, rawText)
    local result = {}

    for permission in pairs(COMMAND_ROSTER_SLOTS) do
        result[permission] = {}
    end


    if type(rawJson) == "table" then
        for permission, slotName in pairs(COMMAND_ROSTER_SLOTS) do
            local slot = rawJson[slotName]

            if type(slot) == "table" then
                if type(slot.UserIds) == "table" then
                    AddRosterIds(result[permission], slot.UserIds)
                else
                    AddRosterIds(result[permission], slot)
                end
            else
                AddRosterId(result[permission], slot)
            end
        end

        return result
    end


    if type(rawText) ~= "string" or rawText == "" then
        return nil
    end

    local slotNames = {}
    for _, slotName in pairs(COMMAND_ROSTER_SLOTS) do
        table.insert(slotNames, slotName)
    end

    for permission, slotName in pairs(COMMAND_ROSTER_SLOTS) do
        result[permission] = ExtractIdsFromRosterText(rawText, slotName, slotNames)
    end

    return result
end

local function FetchCommandRoster()
    
    local rawText = HttpRequestRaw(COMMAND_ROSTER_URL)
    if not rawText or rawText == "" then
        CommandRoster = {}
        CommandRosterLoaded = false
        return false
    end

    local rawJson = nil
    pcall(function()
        rawJson = HttpService:JSONDecode(rawText)
    end)

    local parsed = BuildCommandRoster(rawJson, rawText)
    if type(parsed) ~= "table" then
        CommandRoster = {}
        CommandRosterLoaded = false
        return false
    end

    CommandRoster = parsed
    CommandRosterLoaded = true
    return true
end

local function IsCommandUserAuthorized(permission)
    if not permission then
        return false
    end


    if not FetchCommandRoster() then
        return false
    end

    local myUserId = NormalizeRosterId(LocalPlayer.UserId)
    if not myUserId then
        return false
    end

    local allowed = CommandRoster[permission]
    return type(allowed) == "table" and allowed[myUserId] == true
end

local function CanUseSpoof(_) return IsCommandUserAuthorized("Spoof") end
local function CanUseAnnouncement(_) return IsCommandUserAuthorized("Announce") end
local function CanUseModerationCommands(_) return IsCommandUserAuthorized("Mute") end
local function CanUseKick(_) return IsCommandUserAuthorized("Kick") end
local function CanUseBan(_) return IsCommandUserAuthorized("Ban") end
local function CanUsePlayMusic(_) return IsCommandUserAuthorized("PlayMusic") end



local function HasAdminPermission(_)
    return IsCommandUserAuthorized("Kick")
        or IsCommandUserAuthorized("Ban")
        or IsCommandUserAuthorized("Mute")
end

local function FetchRemoteRankStyles()
    
end

local function FetchRemoteRoles()
    local jsonRoster = HttpRequest(GITHUB_ROLE_LIST_URL, "GET")
    if not jsonRoster or type(jsonRoster) ~= "table" then
        RoleRosterLoaded = false
        RoleSwitchAuthorization = {}
        return
    end

    local fresh = {}
    local freshRoleSwitchAuthorization = {}
    for rankName, playerList in pairs(jsonRoster) do
        local canonicalRole = FindCanonicalRole(rankName)
        if canonicalRole and type(playerList) == "table" then
            local roleAuthorization = { Usernames = {}, UserIds = {} }
            if type(playerList.Usernames) == "table" then
                for _, username in ipairs(playerList.Usernames) do
                    local normalizedUsername = tostring(username or ""):match("^%s*(.-)%s*$")
                    if normalizedUsername ~= "" then
                        roleAuthorization.Usernames[string.lower(normalizedUsername)] = true
                    end
                end
            end
            if type(playerList.UserIds) == "table" then
                for _, userId in ipairs(playerList.UserIds) do
                    local normalizedUserId = tostring(userId or ""):match("^%s*(.-)%s*$")
                    if normalizedUserId ~= "" then
                        roleAuthorization.UserIds[normalizedUserId] = true
                    end
                end
            end
            freshRoleSwitchAuthorization[canonicalRole] = roleAuthorization

            local entries = playerList
            
            if playerList.Usernames or playerList.UserIds then
                entries = {}
                if type(playerList.Usernames) == "table" then
                    for _, username in ipairs(playerList.Usernames) do
                        table.insert(entries, {Username = username})
                    end
                end
                if type(playerList.UserIds) == "table" then
                    for _, userId in ipairs(playerList.UserIds) do
                        table.insert(entries, {UserId = userId})
                    end
                end
            end
            for _, entry in ipairs(entries) do
                local targetUsername, targetUserId, targetEncodedId
                if type(entry) == "table" then
                    targetUsername = entry.Username or entry.Name
                    targetUserId = entry.UserId or entry.UserID or entry.Id
                    targetEncodedId = entry.EncodedId
                elseif type(entry) == "string" then
                    targetUsername = entry
                elseif type(entry) == "number" then
                    targetUserId = entry
                end

                targetUserId = targetUserId and tonumber(targetUserId) or nil
                targetEncodedId = targetEncodedId and tostring(targetEncodedId) or nil

                for _, player in ipairs(Players:GetPlayers()) do
                    local usernameMatch = targetUsername and RoleNameEquals(player.Name, targetUsername)
                    local userIdMatch = targetUserId and player.UserId == targetUserId
                    local encodedMatch = targetEncodedId and EncodePlayerId(player.UserId) == targetEncodedId
                    if usernameMatch or userIdMatch or encodedMatch then
                        fresh[player.Name] = fresh[player.Name] or {}
                        if not table.find(fresh[player.Name], canonicalRole) then
                            table.insert(fresh[player.Name], canonicalRole)
                        end
                    end
                end
            end
        end
    end

    AvailableUserRoles = fresh
    RoleSwitchAuthorization = freshRoleSwitchAuthorization
    RoleRosterLoaded = true
    for _, player in ipairs(Players:GetPlayers()) do
        local selected = AssignedPlayerRoles[player.Name]
        local selectedCanonical = selected and (FindCanonicalRole(selected) or selected) or nil
        local selectedStyle = selectedCanonical and RANK_STYLES[selectedCanonical] or nil
        local selectedTier = type(selectedStyle) == "table" and tonumber(selectedStyle.Tier) or nil




        
        if selected and selectedTier == 1 then
            AssignedPlayerRoles[player.Name] = selectedCanonical
        elseif selected and (selectedTier == 2 or selectedTier == 3) and PlayerHasRole(player.Name, selectedCanonical) then
            AssignedPlayerRoles[player.Name] = selectedCanonical
        else
            AssignedPlayerRoles[player.Name] = GetHighestRole(player.Name)
        end
    end
    Moderation.RefreshAllRoleAutocomplete()
end




local BlurEffect = Lighting:FindFirstChild("Kronos_GlassBlur") or Instance.new("DepthOfFieldEffect")
BlurEffect.Name = "Kronos_GlassBlur"
BlurEffect.FarIntensity = 0.1
BlurEffect.FocusDistance = 0
BlurEffect.InFocusRadius = 30
BlurEffect.NearIntensity = 0.75
BlurEffect.Parent = Lighting


task.spawn(function()
    local hue = 0
    while true do
        task.wait(0.03)
        if not script.Parent then break end
        hue = (hue + 0.008) % 1
        local found = false
        for _, obj in ipairs(script.Parent:GetDescendants()) do
            if obj:IsA("TextLabel") and obj.Text == "KABIR" then
                obj.TextColor3 = Color3.fromHSV(hue, 0.9, 1)
                found = true
                break
            end
        end
        if not found then break end
    end
end)

ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Kronos_Chat_Gui"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Enabled = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 450, 0, 320)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
MainFrame.BackgroundTransparency = 0.35
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = false
MainFrame.Parent = ScreenGui

MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 16)
MainCorner.Parent = MainFrame

GlassGloss = Instance.new("Frame")
GlassGloss.Name = "GlassGloss"
GlassGloss.Size = UDim2.new(1, 0, 0.45, 0)
GlassGloss.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GlassGloss.BackgroundTransparency = 0.95
GlassGloss.BorderSizePixel = 0
GlassGloss.Parent = MainFrame

GlossCorner = Instance.new("UICorner")
GlossCorner.CornerRadius = UDim.new(0, 16)
GlossCorner.Parent = GlassGloss

MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 2.5
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
MainStroke.Transparency = 0.2
MainStroke.Color = Color3.fromRGB(255, 0, 0)
MainStroke.Parent = MainFrame

BorderGradient = Instance.new("UIGradient")
BorderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 50, 50)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(40, 40, 60)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 50, 50)),
})
BorderGradient.Parent = MainStroke

RunService.RenderStepped:Connect(function(dt)
    BorderGradient.Rotation = (BorderGradient.Rotation + (40 * dt)) % 360
end)

HeaderBar = Instance.new("Frame")
HeaderBar.Name = "HeaderBar"
HeaderBar.Size = UDim2.new(1, 0, 0, 36)
HeaderBar.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
HeaderBar.BackgroundTransparency = 0.3
HeaderBar.BorderSizePixel = 0
HeaderBar.Parent = MainFrame

HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 16)
HeaderCorner.Parent = HeaderBar

GlobalTabBtn = Instance.new("TextButton")
GlobalTabBtn.Name = "GlobalTabBtn"
GlobalTabBtn.Size = UDim2.new(0, 65, 0, 26)
GlobalTabBtn.Position = UDim2.new(0, 75, 0, 5)
GlobalTabBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
GlobalTabBtn.BackgroundTransparency = 0.2
GlobalTabBtn.Font = Enum.Font.GothamBold
GlobalTabBtn.Text = "GLOBAL"
GlobalTabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GlobalTabBtn.TextSize = 10
GlobalTabBtn.Parent = HeaderBar

GlobalTabCorner = Instance.new("UICorner")
GlobalTabCorner.CornerRadius = UDim.new(0, 8)
GlobalTabCorner.Parent = GlobalTabBtn

ServerTabBtn = Instance.new("TextButton")
ServerTabBtn.Name = "ServerTabBtn"
ServerTabBtn.Size = UDim2.new(0, 65, 0, 26)
ServerTabBtn.Position = UDim2.new(0, 6, 0, 5)
ServerTabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
ServerTabBtn.BackgroundTransparency = 0.4
ServerTabBtn.Font = Enum.Font.GothamBold
ServerTabBtn.Text = "SERVER"
ServerTabBtn.TextColor3 = Color3.fromRGB(180, 180, 190)
ServerTabBtn.TextSize = 10
ServerTabBtn.Parent = HeaderBar

ServerTabCorner = Instance.new("UICorner")
ServerTabCorner.CornerRadius = UDim.new(0, 8)
ServerTabCorner.Parent = ServerTabBtn

AboutTabBtn = Instance.new("TextButton")
AboutTabBtn.Name = "AboutTabBtn"
AboutTabBtn.Size = UDim2.new(0, 60, 0, 26)
AboutTabBtn.Position = UDim2.new(0, 298, 0, 5)
AboutTabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
AboutTabBtn.BackgroundTransparency = 0.4
AboutTabBtn.Font = Enum.Font.GothamBold
AboutTabBtn.Text = "ABOUT"
AboutTabBtn.TextColor3 = Color3.fromRGB(180, 180, 190)
AboutTabBtn.TextSize = 10
AboutTabBtn.Parent = HeaderBar

AboutTabCorner = Instance.new("UICorner")
AboutTabCorner.CornerRadius = UDim.new(0, 8)
AboutTabCorner.Parent = AboutTabBtn

MusicTabBtn = Instance.new("TextButton")
MusicTabBtn.Name = "MusicSessionTabBtn"
MusicTabBtn.Size = UDim2.new(0, 84, 0, 26)
MusicTabBtn.Position = UDim2.new(0, 144, 0, 5)
MusicTabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
MusicTabBtn.BackgroundTransparency = 0.4
MusicTabBtn.Font = Enum.Font.GothamBold
MusicTabBtn.Text = "MUSIC"
MusicTabBtn.TextColor3 = Color3.fromRGB(180, 180, 190)
MusicTabBtn.TextSize = 10
MusicTabBtn.Parent = HeaderBar
MusicTabCorner = Instance.new("UICorner")
MusicTabCorner.CornerRadius = UDim.new(0, 8)
MusicTabCorner.Parent = MusicTabBtn

ProfileTabBtn = Instance.new("TextButton")
ProfileTabBtn.Name = "ProfileTabBtn"
ProfileTabBtn.Size = UDim2.new(0, 70, 0, 26)
ProfileTabBtn.Position = UDim2.new(0, 228, 0, 5)
ProfileTabBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
ProfileTabBtn.BackgroundTransparency = 0.4
ProfileTabBtn.Font = Enum.Font.GothamBold
ProfileTabBtn.Text = "PROFILE"
ProfileTabBtn.TextColor3 = Color3.fromRGB(180, 180, 190)
ProfileTabBtn.TextSize = 10
ProfileTabBtn.Parent = HeaderBar
ProfileTabCorner = Instance.new("UICorner")
ProfileTabCorner.CornerRadius = UDim.new(0, 8)
ProfileTabCorner.Parent = ProfileTabBtn

KronosHeaderLabel = Instance.new("TextLabel")
KronosHeaderLabel.Name = "KronosHeaderLabel"
KronosHeaderLabel.Size = UDim2.new(0, 64, 0, 26)
KronosHeaderLabel.Position = UDim2.new(1, -70, 0, 5)
KronosHeaderLabel.BackgroundTransparency = 1
KronosHeaderLabel.Font = Enum.Font.GothamBold
KronosHeaderLabel.Text = "KRONOS"
KronosHeaderLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
KronosHeaderLabel.TextSize = 13
KronosHeaderLabel.Parent = HeaderBar

task.spawn(function()
    local blackColor = Color3.fromRGB(0, 0, 0)
    local brightRedColor = Color3.fromRGB(255, 0, 0)
    local frequency = 1.0
    while true do
        local elapsed = tick()
        local factor = (math.sin(elapsed * frequency) + 1) / 2
        KronosHeaderLabel.TextColor3 = blackColor:Lerp(brightRedColor, factor)
        task.wait(0.03)
    end
end)

ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ChatToggleButton"
ToggleButton.Size = UDim2.new(0, 116, 0, 40)
ToggleButton.Position = UDim2.new(0.02, 0, 0.5, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
ToggleButton.BackgroundTransparency = 0.25
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Text = "  👁️  KRONOS"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 11
ToggleButton.AutoButtonColor = false
ToggleButton.Visible = true
ToggleButton.ZIndex = 200
ToggleButton.Parent = ScreenGui

ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 13)
ToggleCorner.Parent = ToggleButton

ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Thickness = 2.5
ToggleStroke.Color = Color3.fromRGB(255, 0, 0)
ToggleStroke.Transparency = 0.15
ToggleStroke.Parent = ToggleButton

ToggleScale = Instance.new("UIScale")
ToggleScale.Parent = ToggleButton

MainScale = Instance.new("UIScale")
MainScale.Parent = MainFrame

local ChatOriginalPosition = MainFrame.Position
local ChatHiddenPosition = ChatOriginalPosition + UDim2.new(0, 0, 0, 55)
local ChatIsShown = true
local ChatTransitionBusy = false

ToggleButton.MouseEnter:Connect(function()
    TweenService:Create(
        ToggleScale,
        TweenInfo.new(0.12, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {Scale = 1.06}
    ):Play()
end)

ToggleButton.MouseLeave:Connect(function()
    TweenService:Create(
        ToggleScale,
        TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {Scale = 1}
    ):Play()
end)

task.spawn(function()
    while ToggleButton.Parent do
        TweenService:Create(
            ToggleStroke,
            TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Transparency = 0.55}
        ):Play()
        task.wait(0.8)
        TweenService:Create(
            ToggleStroke,
            TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {Transparency = 0.12}
        ):Play()
        task.wait(0.8)
    end
end)

local function SetChatVisibility(show)
    if ChatTransitionBusy then return end
    ChatTransitionBusy = true

    if show then
        ChatIsShown = true
        MainFrame.Visible = true
        MainFrame.Position = ChatHiddenPosition
        MainScale.Scale = 0.72

        local move = TweenService:Create(
            MainFrame,
            TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out),
            {Position = ChatOriginalPosition}
        )
        local scale = TweenService:Create(
            MainScale,
            TweenInfo.new(0.28, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
            {Scale = 1}
        )
        move:Play()
        scale:Play()
        move.Completed:Wait()
        ToggleButton.Text = "  👁️  KRONOS"
    else
        ChatIsShown = false

        local move = TweenService:Create(
            MainFrame,
            TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.In),
            {Position = ChatHiddenPosition}
        )
        local scale = TweenService:Create(
            MainScale,
            TweenInfo.new(0.24, Enum.EasingStyle.Quint, Enum.EasingDirection.In),
            {Scale = 0.72}
        )
        move:Play()
        scale:Play()
        move.Completed:Wait()

        if not ChatIsShown then
            MainFrame.Visible = false
        end
        ToggleButton.Text = "  ⭕  KRONOS"
    end

    ChatTransitionBusy = false
end

ToggleButton.MouseButton1Click:Connect(function()
    SetChatVisibility(not ChatIsShown)
end)

MessageContainer = Instance.new("ScrollingFrame")
MessageContainer.Name = "MessageContainer"
MessageContainer.Size = UDim2.new(1, -16, 1, -90)
MessageContainer.Position = UDim2.new(0, 8, 0, 40)
MessageContainer.BackgroundTransparency = 1
MessageContainer.BorderSizePixel = 0
MessageContainer.ScrollBarThickness = 4
MessageContainer.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
MessageContainer.CanvasSize = UDim2.new(0, 0, 0, 0)
MessageContainer.Parent = MainFrame

UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 6)
UIListLayout.Parent = MessageContainer

UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    MessageContainer.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y + 12)
    MessageContainer.CanvasPosition = Vector2.new(0, MessageContainer.CanvasSize.Y.Offset)
end)


NotifContainerFrame = Instance.new("Frame")
NotifContainerFrame.Name = "NotifContainerFrame"
NotifContainerFrame.Size = UDim2.new(0, 350, 0, 40)
NotifContainerFrame.Position = UDim2.new(0.5, -175, 0, 20)
NotifContainerFrame.BackgroundTransparency = 1
NotifContainerFrame.ZIndex = 100
NotifContainerFrame.Parent = ScreenGui

local ActiveNotifBtn = nil
local ShownNotificationIds = {}

local function PlayNotificationSound(soundId)
    local sound = Instance.new("Sound")
    sound.Name = "KronosNotificationSound"
    sound.SoundId = soundId or NOTIFICATION_SOUND_ID
    sound.Volume = 0.65
    sound.Parent = ScreenGui
    pcall(function() sound:Play() end)
    sound.Ended:Connect(function()
        if sound then sound:Destroy() end
    end)
    task.delay(8, function()
        if sound and sound.Parent then sound:Destroy() end
    end)
end

local function DisplayNewMessageNotif(msgData, messageId)
    if not msgData then return end
    if msgData.IsPrivate and msgData.Username ~= LocalPlayer.Name and msgData.TargetUsername ~= LocalPlayer.Name then return end
    if msgData.Username == LocalPlayer.Name and not msgData.IsJoinMessage then return end

    messageId = tostring(messageId or ((msgData.JoinEventId or "") .. "_" .. (msgData.Timestamp or "") .. "_" .. (msgData.Username or "")))
    if ShownNotificationIds[messageId] then return end
    ShownNotificationIds[messageId] = true

    local rawName = msgData.DisplayName or msgData.Username or "Unknown"
    local isClient = (msgData.Username == LocalPlayer.Name)
    local formattedName = GetFormattedDisplayName(rawName, isClient)
    local rawText = msgData.Text or ""
    if msgData.IsJoinMessage then
        rawText = tostring(msgData.JoinedDisplayName or msgData.Text or "Unknown") .. " has Joined the Server Chat"
    elseif string.sub(rawText, 1, 11) == "rbxthumb://" or string.sub(rawText, 1, 13) == "rbxassetid://" then
        rawText = "[Sticker]"
    end
    local formattedMsg = TruncateText(rawText, 34)

    if ActiveNotifBtn and ActiveNotifBtn.Parent then
        ActiveNotifBtn:Destroy()
        ActiveNotifBtn = nil
    end

    local notifBtn = Instance.new("TextButton")
    notifBtn.Name = "MessageNotification"
    notifBtn.Size = UDim2.new(0, 360, 0, 54)
    notifBtn.Position = UDim2.new(0, 0, -1, 0)
    notifBtn.AnchorPoint = Vector2.new(0, 0)
    notifBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
    notifBtn.BackgroundTransparency = 0.22
    notifBtn.BorderSizePixel = 0
    notifBtn.Text = ""
    notifBtn.AutoButtonColor = false
    notifBtn.ZIndex = 101
    notifBtn.Parent = NotifContainerFrame

    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 12)
    notifCorner.Parent = notifBtn

    local notifStroke = Instance.new("UIStroke")
    notifStroke.Thickness = 1.8
    notifStroke.Transparency = 0.1
    notifStroke.Parent = notifBtn

    local notifGradient = Instance.new("UIGradient")
    if msgData.IsJoinMessage then
        notifGradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 0, 0)),
            ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255, 128, 0)),
            ColorSequenceKeypoint.new(0.33, Color3.fromRGB(255, 255, 0)),
            ColorSequenceKeypoint.new(0.50, Color3.fromRGB(0, 255, 0)),
            ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0, 255, 255)),
            ColorSequenceKeypoint.new(0.83, Color3.fromRGB(80, 100, 255)),
            ColorSequenceKeypoint.new(1.00, Color3.fromRGB(255, 0, 255)),
        })
    else
        notifGradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 40, 40)),
            ColorSequenceKeypoint.new(0.5, Color3.fromRGB(20, 20, 30)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 40, 40)),
        })
    end
    notifGradient.Parent = notifStroke

    local notifPfp = Instance.new("ImageLabel")
    notifPfp.Name = "NotificationPFP"
    notifPfp.Size = UDim2.new(0, 38, 0, 38)
    notifPfp.Position = UDim2.new(0, 8, 0.5, -19)
    notifPfp.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    notifPfp.BackgroundTransparency = 0.08
    notifPfp.Image = "rbxthumb://type=Asset&id=" .. tostring(msgData.PfpId or DefaultPfpId) .. "&w=150&h=150"
    notifPfp.ZIndex = 102
    notifPfp.Parent = notifBtn

    local pfpCorner = Instance.new("UICorner")
    pfpCorner.CornerRadius = UDim.new(1, 0)
    pfpCorner.Parent = notifPfp

    local pfpStroke = Instance.new("UIStroke")
    pfpStroke.Thickness = 1.2
    pfpStroke.Color = msgData.IsJoinMessage and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 55, 55)
    pfpStroke.Transparency = 0.15
    pfpStroke.Parent = notifPfp

    local notifText = Instance.new("TextLabel")
    notifText.Size = UDim2.new(1, -58, 1, -8)
    notifText.Position = UDim2.new(0, 54, 0, 4)
    notifText.BackgroundTransparency = 1
    notifText.Font = Enum.Font.GothamBold
    notifText.Text = msgData.IsJoinMessage and (tostring(msgData.JoinedDisplayName or "Unknown") .. " has Joined the Server Chat") or (formattedName .. " : " .. formattedMsg)
    notifText.TextColor3 = Color3.fromRGB(245, 245, 250)
    notifText.TextSize = 11
    notifText.TextWrapped = true
    notifText.TextXAlignment = Enum.TextXAlignment.Left
    notifText.TextYAlignment = Enum.TextYAlignment.Center
    notifText.ZIndex = 102
    notifText.Parent = notifBtn

    task.spawn(function()
        while notifBtn and notifBtn.Parent do
            local dt = RunService.RenderStepped:Wait()
            notifGradient.Rotation = (notifGradient.Rotation + (65 * dt)) % 360
        end
    end)

    ActiveNotifBtn = notifBtn
    PlayNotificationSound(msgData.IsJoinMessage and JOIN_SOUND_ID or NOTIFICATION_SOUND_ID)

    TweenService:Create(notifBtn, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
        Position = UDim2.new(0, 0, 0, 0)
    }):Play()

    task.delay(3.5, function()
        if ActiveNotifBtn == notifBtn and notifBtn and notifBtn.Parent then
            ActiveNotifBtn = nil
            local slideOut = TweenService:Create(notifBtn, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
                Position = UDim2.new(0, 0, -1, 0)
            })
            slideOut:Play()
            slideOut.Completed:Wait()
            if notifBtn and notifBtn.Parent then notifBtn:Destroy() end
        end
    end)

    notifBtn.Activated:Connect(function()
        if ActiveNotifBtn == notifBtn then ActiveNotifBtn = nil end
        local slideOut = TweenService:Create(notifBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(0, 0, -1, 0)
        })
        slideOut:Play()
        slideOut.Completed:Wait()
        if notifBtn and notifBtn.Parent then notifBtn:Destroy() end
    end)
end


StickerPanel = Instance.new("Frame")
StickerPanel.Name = "StickerPanel"
StickerPanel.Size = UDim2.new(0, 260, 0, 200)
StickerPanel.Position = UDim2.new(1, -268, 1, -250)
StickerPanel.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
StickerPanel.BackgroundTransparency = 0.2
StickerPanel.BorderSizePixel = 0
StickerPanel.Visible = false
StickerPanel.ZIndex = 25
StickerPanel.Parent = MainFrame

StickerPanelCorner = Instance.new("UICorner")
StickerPanelCorner.CornerRadius = UDim.new(0, 10)
StickerPanelCorner.Parent = StickerPanel

StickerPanelStroke = Instance.new("UIStroke")
StickerPanelStroke.Thickness = 1.5
StickerPanelStroke.Color = Color3.fromRGB(220, 30, 30)
StickerPanelStroke.Transparency = 0.4
StickerPanelStroke.Parent = StickerPanel






CUSTOM_STICKERS_SAVE_KEY = "Kronos_CustomStickers"
PersistentCustomStickers = {}

function LoadPersistentCustomStickers()
    if not (readfile and isfile and isfile(CUSTOM_STICKERS_SAVE_KEY .. ".txt")) then return end
    pcall(function()
        local raw = readfile(CUSTOM_STICKERS_SAVE_KEY .. ".txt")
        if raw and raw ~= "" then
            local decoded = HttpService:JSONDecode(raw)
            if type(decoded) == "table" then
                for _, id in ipairs(decoded) do
                    id = tostring(id or "")
                    if id:match("^%d+$") and not table.find(PersistentCustomStickers, id) then
                        table.insert(PersistentCustomStickers, id)
                    end
                end
            end
        end
    end)
end

function SavePersistentCustomStickers()
    if writefile then
        pcall(function()
            writefile(CUSTOM_STICKERS_SAVE_KEY .. ".txt", HttpService:JSONEncode(PersistentCustomStickers))
        end)
    end
end

function NormalizeStickerId(value)
    value = tostring(value or ""):gsub("^%s+", ""):gsub("%s+$", "")
    local id = value:match("^%d+$") or value:match("(%d+)")
    if not id then return nil end
    return id
end

LoadPersistentCustomStickers()

StickerScroller = Instance.new("ScrollingFrame")
StickerScroller.Name = "StickerScroller"
StickerScroller.Size = UDim2.new(1, -12, 1, -12)
StickerScroller.Position = UDim2.new(0, 6, 0, 6)
StickerScroller.BackgroundTransparency = 1
StickerScroller.BorderSizePixel = 0
StickerScroller.ScrollBarThickness = 4
StickerScroller.ScrollBarImageColor3 = Color3.fromRGB(220, 30, 30)
StickerScroller.ZIndex = 26
StickerScroller.Parent = StickerPanel

StickerGrid = Instance.new("UIGridLayout")
StickerGrid.CellSize = UDim2.new(0, 70, 0, 70)
StickerGrid.CellPadding = UDim2.new(0, 6, 0, 6)
StickerGrid.SortOrder = Enum.SortOrder.LayoutOrder
StickerGrid.Parent = StickerScroller

StickerGrid:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    StickerScroller.CanvasSize = UDim2.new(0, 0, 0, StickerGrid.AbsoluteContentSize.Y + 12)
end)



StickerAddPopup = Instance.new("Frame")
StickerAddPopup.Name = "StickerAddPopup"
StickerAddPopup.Size = UDim2.new(0, 238, 0, 116)
StickerAddPopup.Position = UDim2.new(0, 11, 0, 42)
StickerAddPopup.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
StickerAddPopup.BackgroundTransparency = 0.03
StickerAddPopup.BorderSizePixel = 0
StickerAddPopup.Visible = false
StickerAddPopup.ZIndex = 40
StickerAddPopup.Parent = StickerPanel

StickerAddCorner = Instance.new("UICorner")
StickerAddCorner.CornerRadius = UDim.new(0, 10)
StickerAddCorner.Parent = StickerAddPopup
StickerAddStroke = Instance.new("UIStroke")
StickerAddStroke.Thickness = 1.5
StickerAddStroke.Color = Color3.fromRGB(220, 30, 30)
StickerAddStroke.Parent = StickerAddPopup

StickerAddTitle = Instance.new("TextLabel")
StickerAddTitle.Size = UDim2.new(1, -16, 0, 22)
StickerAddTitle.Position = UDim2.new(0, 8, 0, 6)
StickerAddTitle.BackgroundTransparency = 1
StickerAddTitle.Text = "+  SAVE NEW STICKER"
StickerAddTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerAddTitle.Font = Enum.Font.GothamBold
StickerAddTitle.TextSize = 12
StickerAddTitle.TextXAlignment = Enum.TextXAlignment.Left
StickerAddTitle.ZIndex = 41
StickerAddTitle.Parent = StickerAddPopup

StickerIdInput = Instance.new("TextBox")
StickerIdInput.Name = "StickerIdInput"
StickerIdInput.Size = UDim2.new(1, -16, 0, 32)
StickerIdInput.Position = UDim2.new(0, 8, 0, 32)
StickerIdInput.BackgroundColor3 = Color3.fromRGB(8, 8, 15)
StickerIdInput.BackgroundTransparency = 0.05
StickerIdInput.BorderSizePixel = 0
StickerIdInput.ClearTextOnFocus = false
StickerIdInput.PlaceholderText = "Enter Roblox Sticker ID"
StickerIdInput.PlaceholderColor3 = Color3.fromRGB(150, 150, 160)
StickerIdInput.Text = ""
StickerIdInput.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerIdInput.Font = Enum.Font.GothamMedium
StickerIdInput.TextSize = 12
StickerIdInput.ZIndex = 41
StickerIdInput.Parent = StickerAddPopup

StickerInputCorner = Instance.new("UICorner")
StickerInputCorner.CornerRadius = UDim.new(0, 7)
StickerInputCorner.Parent = StickerIdInput

StickerSaveButton = Instance.new("TextButton")
StickerSaveButton.Size = UDim2.new(0.5, -10, 0, 28)
StickerSaveButton.Position = UDim2.new(0, 8, 1, -34)
StickerSaveButton.BackgroundColor3 = Color3.fromRGB(220, 30, 30)
StickerSaveButton.BorderSizePixel = 0
StickerSaveButton.Text = "SAVE"
StickerSaveButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerSaveButton.Font = Enum.Font.GothamBold
StickerSaveButton.TextSize = 11
StickerSaveButton.ZIndex = 41
StickerSaveButton.Parent = StickerAddPopup
StickerSaveCorner = Instance.new("UICorner")
StickerSaveCorner.CornerRadius = UDim.new(0, 7)
StickerSaveCorner.Parent = StickerSaveButton

StickerCancelButton = Instance.new("TextButton")
StickerCancelButton.Size = UDim2.new(0.5, -10, 0, 28)
StickerCancelButton.Position = UDim2.new(0.5, 2, 1, -34)
StickerCancelButton.BackgroundColor3 = Color3.fromRGB(55, 55, 65)
StickerCancelButton.BorderSizePixel = 0
StickerCancelButton.Text = "CANCEL"
StickerCancelButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerCancelButton.Font = Enum.Font.GothamBold
StickerCancelButton.TextSize = 11
StickerCancelButton.ZIndex = 41
StickerCancelButton.Parent = StickerAddPopup
StickerCancelCorner = Instance.new("UICorner")
StickerCancelCorner.CornerRadius = UDim.new(0, 7)
StickerCancelCorner.Parent = StickerCancelButton

function GetStickerUrlFromId(id)
    return "rbxthumb://type=Asset&id=" .. tostring(id) .. "&w=420&h=420"
end

function SendStickerFromUrl(stickerUrl)
    StickerPanel.Visible = false
    StickerAddPopup.Visible = false
    if MessageInput then
        MessageInput.Text = stickerUrl
        if SendChatMessage then SendChatMessage() end
    end
end







StickerViewPopup = Instance.new("Frame")
StickerViewPopup.Name = "StickerViewPopup"
StickerViewPopup.Size = UDim2.new(0, 286, 0, 310)
StickerViewPopup.Position = UDim2.new(0.5, -143, 0.5, -155)
StickerViewPopup.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
StickerViewPopup.BackgroundTransparency = 0.02
StickerViewPopup.BorderSizePixel = 0
StickerViewPopup.Visible = false
StickerViewPopup.ZIndex = 150
StickerViewPopup.Parent = ScreenGui

local stickerViewCorner = Instance.new("UICorner")
stickerViewCorner.CornerRadius = UDim.new(0, 12)
stickerViewCorner.Parent = StickerViewPopup

local stickerViewStroke = Instance.new("UIStroke")
stickerViewStroke.Thickness = 1.6
stickerViewStroke.Color = Color3.fromRGB(220, 35, 45)
stickerViewStroke.Transparency = 0.1
stickerViewStroke.Parent = StickerViewPopup

StickerViewTitle = Instance.new("TextLabel")
StickerViewTitle.Size = UDim2.new(1, -54, 0, 30)
StickerViewTitle.Position = UDim2.new(0, 13, 0, 7)
StickerViewTitle.BackgroundTransparency = 1
StickerViewTitle.Text = "STICKER PREVIEW"
StickerViewTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerViewTitle.Font = Enum.Font.GothamBold
StickerViewTitle.TextSize = 12
StickerViewTitle.TextXAlignment = Enum.TextXAlignment.Left
StickerViewTitle.ZIndex = 151
StickerViewTitle.Parent = StickerViewPopup

StickerViewClose = Instance.new("TextButton")
StickerViewClose.Size = UDim2.new(0, 30, 0, 30)
StickerViewClose.Position = UDim2.new(1, -39, 0, 6)
StickerViewClose.BackgroundColor3 = Color3.fromRGB(150, 25, 35)
StickerViewClose.BorderSizePixel = 0
StickerViewClose.Text = "×"
StickerViewClose.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerViewClose.Font = Enum.Font.GothamBold
StickerViewClose.TextSize = 20
StickerViewClose.AutoButtonColor = true
StickerViewClose.ZIndex = 152
StickerViewClose.Parent = StickerViewPopup

local stickerViewCloseCorner = Instance.new("UICorner")
stickerViewCloseCorner.CornerRadius = UDim.new(0, 8)
stickerViewCloseCorner.Parent = StickerViewClose

StickerViewImage = Instance.new("ImageLabel")
StickerViewImage.Size = UDim2.new(0, 230, 0, 205)
StickerViewImage.Position = UDim2.new(0.5, -115, 0, 43)
StickerViewImage.BackgroundColor3 = Color3.fromRGB(8, 8, 15)
StickerViewImage.BackgroundTransparency = 0.04
StickerViewImage.BorderSizePixel = 0
StickerViewImage.ScaleType = Enum.ScaleType.Fit
StickerViewImage.ZIndex = 151
StickerViewImage.Parent = StickerViewPopup

local stickerViewImageCorner = Instance.new("UICorner")
stickerViewImageCorner.CornerRadius = UDim.new(0, 10)
stickerViewImageCorner.Parent = StickerViewImage




StickerViewSave = Instance.new("TextButton")
StickerViewSave.Name = "StickerViewSave"
StickerViewSave.Size = UDim2.new(0, 138, 0, 36)
StickerViewSave.Position = UDim2.new(0.5, -69, 1, -46)
StickerViewSave.BackgroundColor3 = Color3.fromRGB(205, 25, 35)
StickerViewSave.BackgroundTransparency = 0
StickerViewSave.BorderSizePixel = 0
StickerViewSave.Text = "SAVE STICKER"
StickerViewSave.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerViewSave.Font = Enum.Font.GothamBold
StickerViewSave.TextSize = 11
StickerViewSave.TextXAlignment = Enum.TextXAlignment.Center
StickerViewSave.TextYAlignment = Enum.TextYAlignment.Center
StickerViewSave.AutoButtonColor = true
StickerViewSave.Active = true
StickerViewSave.Selectable = true
StickerViewSave.Visible = false
StickerViewSave.ZIndex = 154
StickerViewSave.Parent = StickerViewPopup

local svsc_final = Instance.new("UICorner")
svsc_final.CornerRadius = UDim.new(0, 8)
svsc_final.Parent = StickerViewSave

local svss_final = Instance.new("UIStroke")
svss_final.Thickness = 1.2
svss_final.Color = Color3.fromRGB(255, 95, 105)
svss_final.Transparency = 0.15
svss_final.Parent = StickerViewSave

local CurrentStickerViewUrl = ""
local CurrentStickerViewId = ""
local RebuildStickerGrid = nil

local function ExtractStickerIdFromUrl(url)
    local s = tostring(url or "")
    return s:match("id=(%d+)")
end

local function StickerViewerAlreadySaved(id)
    id = tostring(id or "")
    if id == "" then
        return false
    end

    for _, saved in ipairs(PersistentCustomStickers) do
        if tostring(saved) == id then
            return true
        end
    end

    local normalized = GetStickerUrlFromId(id)
    for _, hardcoded in ipairs(baseStickers) do
        if tostring(hardcoded) == normalized then
            return true
        end
    end

    return false
end

function OpenStickerViewer(stickerUrl)
    CurrentStickerViewUrl = tostring(stickerUrl or "")
    CurrentStickerViewId = NormalizeStickerId(ExtractStickerIdFromUrl(CurrentStickerViewUrl)) or ""

    StickerViewImage.Image = CurrentStickerViewUrl

    local alreadySaved = StickerViewerAlreadySaved(CurrentStickerViewId)
    StickerViewSave.Visible = CurrentStickerViewId ~= ""
    StickerViewSave.Text = alreadySaved and "SAVED ✓" or "SAVE STICKER"
    StickerViewSave.BackgroundColor3 = alreadySaved and Color3.fromRGB(55, 145, 75) or Color3.fromRGB(205, 25, 35)
    StickerViewSave.Active = CurrentStickerViewId ~= ""
    StickerViewPopup.Visible = CurrentStickerViewUrl ~= ""
end

StickerViewClose.Activated:Connect(function()
    StickerViewPopup.Visible = false
    StickerViewSave.Visible = false
    StickerViewSave.Text = "SAVE STICKER"
    StickerViewSave.BackgroundColor3 = Color3.fromRGB(205, 25, 35)
    StickerViewSave.Active = false
end)

StickerViewSave.Activated:Connect(function()
    local stickerId = NormalizeStickerId(CurrentStickerViewId)
    if not stickerId then
        return
    end

    if StickerViewerAlreadySaved(stickerId) then
        StickerViewSave.Text = "SAVED ✓"
        StickerViewSave.BackgroundColor3 = Color3.fromRGB(55, 145, 75)
        ShowLocalSystemMessage("[AI MoD] : This sticker is already saved ✓")
        return
    end

    table.insert(PersistentCustomStickers, 1, stickerId)
    SavePersistentCustomStickers()


    
    if RebuildStickerGrid then
        RebuildStickerGrid()
    end

    StickerViewSave.Text = "SAVED ✓"
    StickerViewSave.BackgroundColor3 = Color3.fromRGB(55, 145, 75)
    StickerViewSave.Active = false
    ShowLocalSystemMessage("[AI MoD] : Sticker saved locally ✓")
end)

function BuildStickerButton(imageUrl, order, isAddButton)
    local button
    if isAddButton then
        button = Instance.new("TextButton")
        button.Text = "+"
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        button.Font = Enum.Font.GothamBold
        button.TextSize = 30
    else
        button = Instance.new("ImageButton")
        button.Image = imageUrl
        button.ScaleType = Enum.ScaleType.Fit
    end
    button.Name = isAddButton and "AddStickerButton" or "StickerCard"
    button.LayoutOrder = order
    button.BackgroundColor3 = isAddButton and Color3.fromRGB(190, 20, 30) or Color3.fromRGB(25, 25, 38)
    button.BackgroundTransparency = isAddButton and 0.08 or 0.3
    button.BorderSizePixel = 0
    button.ZIndex = 27
    button.Parent = StickerScroller

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = button
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = isAddButton and 1.5 or 1
    stroke.Color = isAddButton and Color3.fromRGB(255, 90, 90) or Color3.fromRGB(255, 255, 255)
    stroke.Transparency = isAddButton and 0.15 or 0.8
    stroke.Parent = button

    if isAddButton then
        button.Activated:Connect(function()
            StickerAddPopup.Visible = true
            StickerIdInput.Text = ""
            StickerIdInput:CaptureFocus()
        end)
    else
        button.Activated:Connect(function()
            SendStickerFromUrl(imageUrl)
        end)
    end
    return button
end



RebuildStickerGrid = function()
    for _, child in ipairs(StickerScroller:GetChildren()) do
        if child:IsA("GuiButton") then
            child:Destroy()
        end
    end


    BuildStickerButton(nil, 1, true)


    
    for index, stickerId in ipairs(PersistentCustomStickers) do
        BuildStickerButton(GetStickerUrlFromId(stickerId), index + 1, false)
    end

    for index, stickerUrl in ipairs(baseStickers) do
        BuildStickerButton(stickerUrl, #PersistentCustomStickers + index + 1, false)
    end
end

RebuildStickerGrid()

StickerSaveButton.Activated:Connect(function()
    local stickerId = NormalizeStickerId(StickerIdInput.Text)
    if not stickerId then
        StickerIdInput.Text = ""
        StickerIdInput.PlaceholderText = "Enter a valid numeric ID"
        return
    end

    for i = #PersistentCustomStickers, 1, -1 do
        if PersistentCustomStickers[i] == stickerId then
            table.remove(PersistentCustomStickers, i)
        end
    end
    table.insert(PersistentCustomStickers, 1, stickerId)
    SavePersistentCustomStickers()


    if RebuildStickerGrid then
        RebuildStickerGrid()
    end

    StickerAddPopup.Visible = false
    StickerIdInput.Text = ""
end)

StickerCancelButton.Activated:Connect(function()
    StickerAddPopup.Visible = false
    StickerIdInput.Text = ""
end)


AboutFrame = Instance.new("Frame")
AboutFrame.Name = "AboutFrame"
AboutFrame.Size = UDim2.new(1, -16, 1, -48)
AboutFrame.Position = UDim2.new(0, 8, 0, 40)
AboutFrame.BackgroundTransparency = 1
AboutFrame.Visible = false
AboutFrame.Parent = MainFrame






MusicSessionFrame = Instance.new("ScrollingFrame")
MusicSessionFrame.Name = "MusicSessionFrame"
MusicSessionFrame.Size = UDim2.new(1, -16, 1, -48)
MusicSessionFrame.Position = UDim2.new(0, 8, 0, 40)
MusicSessionFrame.BackgroundTransparency = 1
MusicSessionFrame.BorderSizePixel = 0
MusicSessionFrame.ScrollBarThickness = 3
MusicSessionFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
MusicSessionFrame.CanvasSize = UDim2.new(0, 0, 0, 680)
MusicSessionFrame.Visible = false
MusicSessionFrame.Parent = MainFrame

MusicTopCard = Instance.new("Frame")
MusicTopCard.Size = UDim2.new(1, 0, 0, 84)
MusicTopCard.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MusicTopCard.BackgroundTransparency = 0.28
MusicTopCard.BorderSizePixel = 0
MusicTopCard.Parent = MusicSessionFrame
MusicTopCorner = Instance.new("UICorner")
MusicTopCorner.CornerRadius = UDim.new(0, 10)
MusicTopCorner.Parent = MusicTopCard
MusicTopStroke = Instance.new("UIStroke")
MusicTopStroke.Thickness = 1.2
MusicTopStroke.Color = Color3.fromRGB(255, 0, 0)
MusicTopStroke.Transparency = 0.55
MusicTopStroke.Parent = MusicTopCard

MusicSessionTitle = Instance.new("TextLabel")
MusicSessionTitle.Size = UDim2.new(1, -18, 0, 22)
MusicSessionTitle.Position = UDim2.new(0, 9, 0, 6)
MusicSessionTitle.BackgroundTransparency = 1
MusicSessionTitle.Font = Enum.Font.GothamBold
MusicSessionTitle.Text = "🎵 MUSIC SESSION"
MusicSessionTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicSessionTitle.TextSize = 14
MusicSessionTitle.TextXAlignment = Enum.TextXAlignment.Left
MusicSessionTitle.Parent = MusicTopCard

MusicNowPlayingLabel = Instance.new("TextLabel")
MusicNowPlayingLabel.Size = UDim2.new(1, -18, 0, 20)
MusicNowPlayingLabel.Position = UDim2.new(0, 9, 0, 28)
MusicNowPlayingLabel.BackgroundTransparency = 1
MusicNowPlayingLabel.Font = Enum.Font.Gotham
MusicNowPlayingLabel.Text = "Not in a music session"
MusicNowPlayingLabel.TextColor3 = Color3.fromRGB(210, 210, 220)
MusicNowPlayingLabel.TextSize = 10
MusicNowPlayingLabel.TextXAlignment = Enum.TextXAlignment.Left
MusicNowPlayingLabel.TextTruncate = Enum.TextTruncate.AtEnd
MusicNowPlayingLabel.Parent = MusicTopCard

MusicHostLabel = Instance.new("TextLabel")
MusicHostLabel.Size = UDim2.new(0.72, 0, 0, 18)
MusicHostLabel.Position = UDim2.new(0, 9, 0, 49)
MusicHostLabel.BackgroundTransparency = 1
MusicHostLabel.Font = Enum.Font.Gotham
MusicHostLabel.Text = "Host: —"
MusicHostLabel.TextColor3 = Color3.fromRGB(150, 150, 165)
MusicHostLabel.TextSize = 9
MusicHostLabel.TextXAlignment = Enum.TextXAlignment.Left
MusicHostLabel.Parent = MusicTopCard

MusicSessionIdLabel = Instance.new("TextLabel")
MusicSessionIdLabel.Size = UDim2.new(0.55, 0, 0, 18)
MusicSessionIdLabel.Position = UDim2.new(0, 9, 0, 65)
MusicSessionIdLabel.BackgroundTransparency = 1
MusicSessionIdLabel.Font = Enum.Font.Code
MusicSessionIdLabel.Text = "Session ID: —"
MusicSessionIdLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
MusicSessionIdLabel.TextSize = 9
MusicSessionIdLabel.TextXAlignment = Enum.TextXAlignment.Left
MusicSessionIdLabel.Parent = MusicTopCard

MusicMuteButton = Instance.new("TextButton")
MusicMuteButton.Size = UDim2.new(0, 90, 0, 25)
MusicMuteButton.Position = UDim2.new(1, -99, 0, 52)
MusicMuteButton.BackgroundColor3 = Color3.fromRGB(55, 55, 70)
MusicMuteButton.BackgroundTransparency = 0.08
MusicMuteButton.Font = Enum.Font.GothamBold
MusicMuteButton.Text = "🔊 MUTE"
MusicMuteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicMuteButton.TextSize = 9
MusicMuteButton.Parent = MusicTopCard
MusicMuteCorner = Instance.new("UICorner")
MusicMuteCorner.CornerRadius = UDim.new(0, 7)
MusicMuteCorner.Parent = MusicMuteButton
MusicMuteButton.Visible = false

function MusicMakeButton(parent, name, text, pos, size)
    local b = Instance.new("TextButton")
    b.Name = name
    b.Size = size
    b.Position = pos
    b.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
    b.BackgroundTransparency = 0.12
    b.BorderSizePixel = 0
    b.Font = Enum.Font.GothamBold
    b.Text = text
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.TextSize = 9
    b.Parent = parent
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 7)
    c.Parent = b
    return b
end

function MusicMakeSecondaryButton(parent, name, text, pos, size)
    local b = MusicMakeButton(parent, name, text, pos, size)
    b.BackgroundColor3 = Color3.fromRGB(45, 45, 60)
    b.BackgroundTransparency = 0.05
    return b
end

MusicChoiceTitle = Instance.new("TextLabel")
MusicChoiceTitle.Size = UDim2.new(1, -12, 0, 24)
MusicChoiceTitle.Position = UDim2.new(0, 6, 0, 104)
MusicChoiceTitle.BackgroundTransparency = 1
MusicChoiceTitle.Font = Enum.Font.GothamBold
MusicChoiceTitle.Text = "What would you like to do?"
MusicChoiceTitle.TextColor3 = Color3.fromRGB(235, 235, 245)
MusicChoiceTitle.TextSize = 12
MusicChoiceTitle.TextXAlignment = Enum.TextXAlignment.Center
MusicChoiceTitle.Parent = MusicSessionFrame

MusicCreateChoiceButton = MusicMakeButton(MusicSessionFrame, "MusicCreateChoiceButton", "🎵  CREATE A SESSION", UDim2.new(0, 6, 0, 136), UDim2.new(0.5, -9, 0, 54))
MusicJoinChoiceButton = MusicMakeButton(MusicSessionFrame, "MusicJoinChoiceButton", "🔗  JOIN A SESSION", UDim2.new(0.5, 3, 0, 136), UDim2.new(0.5, -9, 0, 54))
MusicCreateChoiceButton.TextSize = 10
MusicJoinChoiceButton.TextSize = 10

MusicChoiceHint = Instance.new("TextLabel")
MusicChoiceHint.Size = UDim2.new(1, -20, 0, 42)
MusicChoiceHint.Position = UDim2.new(0, 10, 0, 200)
MusicChoiceHint.BackgroundTransparency = 1
MusicChoiceHint.Font = Enum.Font.Gotham
MusicChoiceHint.Text = "Create a session to play YouTube music for everyone who joins, or join a friend's session using their Session ID."
MusicChoiceHint.TextColor3 = Color3.fromRGB(155, 155, 170)
MusicChoiceHint.TextSize = 9
MusicChoiceHint.TextWrapped = true
MusicChoiceHint.TextXAlignment = Enum.TextXAlignment.Center
MusicChoiceHint.Parent = MusicSessionFrame

MusicCreatePanel = Instance.new("Frame")
MusicCreatePanel.Size = UDim2.new(1, 0, 0, 560)
MusicCreatePanel.Position = UDim2.new(0, 0, 0, 96)
MusicCreatePanel.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MusicCreatePanel.BackgroundTransparency = 0.25
MusicCreatePanel.BorderSizePixel = 0
MusicCreatePanel.Visible = false
MusicCreatePanel.Parent = MusicSessionFrame
MusicCreateCorner = Instance.new("UICorner")
MusicCreateCorner.CornerRadius = UDim.new(0, 10)
MusicCreateCorner.Parent = MusicCreatePanel

MusicCreateTitle = Instance.new("TextLabel")
MusicCreateTitle.Size = UDim2.new(1, -20, 0, 24)
MusicCreateTitle.Position = UDim2.new(0, 10, 0, 8)
MusicCreateTitle.BackgroundTransparency = 1
MusicCreateTitle.Font = Enum.Font.GothamBold
MusicCreateTitle.Text = "CREATE MUSIC SESSION"
MusicCreateTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicCreateTitle.TextSize = 12
MusicCreateTitle.TextXAlignment = Enum.TextXAlignment.Left
MusicCreateTitle.Parent = MusicCreatePanel

MusicCreateInfo = Instance.new("TextLabel")
MusicCreateInfo.Size = UDim2.new(1, -20, 0, 38)
MusicCreateInfo.Position = UDim2.new(0, 10, 0, 34)
MusicCreateInfo.BackgroundTransparency = 1
MusicCreateInfo.Font = Enum.Font.Gotham
MusicCreateInfo.Text = "Paste a YouTube video URL or playlist URL. The host controls playback, queue order, and session members."
MusicCreateInfo.TextColor3 = Color3.fromRGB(175, 175, 190)
MusicCreateInfo.TextSize = 9
MusicCreateInfo.TextWrapped = true
MusicCreateInfo.TextXAlignment = Enum.TextXAlignment.Left
MusicCreateInfo.Parent = MusicCreatePanel

MusicEndCreateButton = MusicMakeButton(MusicCreatePanel, "MusicEndCreateButton", "END SESSION / BACK TO CHOICE", UDim2.new(0, 10, 0, 72), UDim2.new(1, -20, 0, 26))
MusicEndCreateButton.Active = true
MusicEndCreateButton.AutoButtonColor = true
MusicEndCreateButton.ZIndex = 100

MusicInviteGlobalButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicInviteGlobalButton", "INVITE GLOBAL", UDim2.new(0, 10, 0, 106), UDim2.new(0.5, -15, 0, 27))
MusicInviteServerButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicInviteServerButton", "INVITE SERVER", UDim2.new(0.5, 5, 0, 106), UDim2.new(0.5, -15, 0, 27))

MusicYouTubeInput = Instance.new("TextBox")
MusicYouTubeInput.Size = UDim2.new(1, -20, 0, 31)
MusicYouTubeInput.Position = UDim2.new(0, 10, 0, 141)
MusicYouTubeInput.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
MusicYouTubeInput.BackgroundTransparency = 0.08
MusicYouTubeInput.BorderSizePixel = 0
MusicYouTubeInput.Font = Enum.Font.Code
MusicYouTubeInput.PlaceholderText = "Paste YouTube video or playlist URL..."
MusicYouTubeInput.PlaceholderColor3 = Color3.fromRGB(145, 145, 160)
MusicYouTubeInput.Text = ""
MusicYouTubeInput.TextColor3 = Color3.fromRGB(245, 245, 250)
MusicYouTubeInput.TextSize = 10
MusicYouTubeInput.ClearTextOnFocus = false
MusicYouTubeInput.Parent = MusicCreatePanel
MusicYouTubeCorner = Instance.new("UICorner")
MusicYouTubeCorner.CornerRadius = UDim.new(0, 8)
MusicYouTubeCorner.Parent = MusicYouTubeInput

MusicAddQueueButton = MusicMakeButton(MusicCreatePanel, "MusicAddQueueButton", "ADD TO QUEUE", UDim2.new(0, 10, 0, 178), UDim2.new(0.5, -15, 0, 29))
MusicPlayUrlButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicPlayUrlButton", "PLAY NOW", UDim2.new(0.5, 5, 0, 178), UDim2.new(0.5, -15, 0, 29))

MusicPreviousButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicPreviousButton", "⏮ PREVIOUS", UDim2.new(0, 10, 0, 214), UDim2.new(0.25, -12, 0, 28))
MusicPlayPauseButton = MusicMakeButton(MusicCreatePanel, "MusicPlayPauseButton", "▶ PLAY", UDim2.new(0.25, 2, 0, 214), UDim2.new(0.25, -12, 0, 28))
MusicNextButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicNextButton", "NEXT ⏭", UDim2.new(0.5, -4, 0, 214), UDim2.new(0.25, -12, 0, 28))
MusicStopButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicStopButton", "■ STOP", UDim2.new(0.75, 6, 0, 214), UDim2.new(0.25, -16, 0, 28))

MusicClearQueueButton = MusicMakeSecondaryButton(MusicCreatePanel, "MusicClearQueueButton", "CLEAR QUEUE", UDim2.new(0, 10, 0, 249), UDim2.new(0.5, -15, 0, 25))
MusicQueueStatusLabel = Instance.new("TextLabel")
MusicQueueStatusLabel.Size = UDim2.new(0.5, -5, 0, 25)
MusicQueueStatusLabel.Position = UDim2.new(0.5, 5, 0, 249)
MusicQueueStatusLabel.BackgroundTransparency = 1
MusicQueueStatusLabel.Font = Enum.Font.Gotham
MusicQueueStatusLabel.Text = "Queue: 0"
MusicQueueStatusLabel.TextColor3 = Color3.fromRGB(155, 155, 170)
MusicQueueStatusLabel.TextSize = 9
MusicQueueStatusLabel.TextXAlignment = Enum.TextXAlignment.Right
MusicQueueStatusLabel.Parent = MusicCreatePanel

MusicQueueHeader = Instance.new("TextLabel")
MusicQueueHeader.Size = UDim2.new(1, -20, 0, 20)
MusicQueueHeader.Position = UDim2.new(0, 10, 0, 279)
MusicQueueHeader.BackgroundTransparency = 1
MusicQueueHeader.Font = Enum.Font.GothamBold
MusicQueueHeader.Text = "QUEUE"
MusicQueueHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicQueueHeader.TextSize = 10
MusicQueueHeader.TextXAlignment = Enum.TextXAlignment.Left
MusicQueueHeader.Parent = MusicCreatePanel

MusicQueueScroller = Instance.new("ScrollingFrame")
MusicQueueScroller.Size = UDim2.new(1, -20, 0, 155)
MusicQueueScroller.Position = UDim2.new(0, 10, 0, 301)
MusicQueueScroller.BackgroundColor3 = Color3.fromRGB(12, 12, 20)
MusicQueueScroller.BackgroundTransparency = 0.12
MusicQueueScroller.BorderSizePixel = 0
MusicQueueScroller.ScrollBarThickness = 3
MusicQueueScroller.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
MusicQueueScroller.Parent = MusicCreatePanel
MusicQueueCorner = Instance.new("UICorner")
MusicQueueCorner.CornerRadius = UDim.new(0, 7)
MusicQueueCorner.Parent = MusicQueueScroller
MusicQueueLayout = Instance.new("UIListLayout")
MusicQueueLayout.SortOrder = Enum.SortOrder.LayoutOrder
MusicQueueLayout.Padding = UDim.new(0, 3)
MusicQueueLayout.Parent = MusicQueueScroller
MusicQueueLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    MusicQueueScroller.CanvasSize = UDim2.new(0, 0, 0, MusicQueueLayout.AbsoluteContentSize.Y + 8)
end)

MusicMembersHeader = Instance.new("TextLabel")
MusicMembersHeader.Size = UDim2.new(1, -20, 0, 20)
MusicMembersHeader.Position = UDim2.new(0, 10, 0, 464)
MusicMembersHeader.BackgroundTransparency = 1
MusicMembersHeader.Font = Enum.Font.GothamBold
MusicMembersHeader.Text = "SESSION MEMBERS"
MusicMembersHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicMembersHeader.TextSize = 10
MusicMembersHeader.TextXAlignment = Enum.TextXAlignment.Left
MusicMembersHeader.Parent = MusicCreatePanel

MusicMembersScroller = Instance.new("ScrollingFrame")
MusicMembersScroller.Size = UDim2.new(1, -20, 0, 80)
MusicMembersScroller.Position = UDim2.new(0, 10, 0, 485)
MusicMembersScroller.BackgroundColor3 = Color3.fromRGB(12, 12, 20)
MusicMembersScroller.BackgroundTransparency = 0.12
MusicMembersScroller.BorderSizePixel = 0
MusicMembersScroller.ScrollBarThickness = 3
MusicMembersScroller.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
MusicMembersScroller.Parent = MusicCreatePanel
MusicMembersCorner = Instance.new("UICorner")
MusicMembersCorner.CornerRadius = UDim.new(0, 7)
MusicMembersCorner.Parent = MusicMembersScroller
MusicMembersLayout = Instance.new("UIListLayout")
MusicMembersLayout.SortOrder = Enum.SortOrder.Name
MusicMembersLayout.Padding = UDim.new(0, 3)
MusicMembersLayout.Parent = MusicMembersScroller
MusicMembersLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    MusicMembersScroller.CanvasSize = UDim2.new(0, 0, 0, MusicMembersLayout.AbsoluteContentSize.Y + 8)
end)

MusicJoinPanel = Instance.new("Frame")
MusicJoinPanel.Size = UDim2.new(1, 0, 0, 405)
MusicJoinPanel.Position = UDim2.new(0, 0, 0, 96)
MusicJoinPanel.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MusicJoinPanel.BackgroundTransparency = 0.25
MusicJoinPanel.BorderSizePixel = 0
MusicJoinPanel.Visible = false
MusicJoinPanel.Parent = MusicSessionFrame
MusicJoinCorner = Instance.new("UICorner")
MusicJoinCorner.CornerRadius = UDim.new(0, 10)
MusicJoinCorner.Parent = MusicJoinPanel

MusicJoinTitle = Instance.new("TextLabel")
MusicJoinTitle.Size = UDim2.new(1, -20, 0, 24)
MusicJoinTitle.Position = UDim2.new(0, 10, 0, 8)
MusicJoinTitle.BackgroundTransparency = 1
MusicJoinTitle.Font = Enum.Font.GothamBold
MusicJoinTitle.Text = "JOIN MUSIC SESSION"
MusicJoinTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicJoinTitle.TextSize = 12
MusicJoinTitle.TextXAlignment = Enum.TextXAlignment.Left
MusicJoinTitle.Parent = MusicJoinPanel

MusicJoinInfo = Instance.new("TextLabel")
MusicJoinInfo.Size = UDim2.new(1, -20, 0, 38)
MusicJoinInfo.Position = UDim2.new(0, 10, 0, 34)
MusicJoinInfo.BackgroundTransparency = 1
MusicJoinInfo.Font = Enum.Font.Gotham
MusicJoinInfo.Text = "Joiners automatically receive the host's current track and queue. You only control your own audio volume."
MusicJoinInfo.TextColor3 = Color3.fromRGB(175, 175, 190)
MusicJoinInfo.TextSize = 9
MusicJoinInfo.TextWrapped = true
MusicJoinInfo.TextXAlignment = Enum.TextXAlignment.Left
MusicJoinInfo.Parent = MusicJoinPanel

MusicSessionIdInput = Instance.new("TextBox")
MusicSessionIdInput.Size = UDim2.new(1, -20, 0, 34)
MusicSessionIdInput.Position = UDim2.new(0, 10, 0, 82)
MusicSessionIdInput.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
MusicSessionIdInput.BackgroundTransparency = 0.08
MusicSessionIdInput.BorderSizePixel = 0
MusicSessionIdInput.Font = Enum.Font.Code
MusicSessionIdInput.PlaceholderText = "ENTER SESSION ID"
MusicSessionIdInput.PlaceholderColor3 = Color3.fromRGB(145, 145, 160)
MusicSessionIdInput.Text = ""
MusicSessionIdInput.TextColor3 = Color3.fromRGB(245, 245, 250)
MusicSessionIdInput.TextSize = 12
MusicSessionIdInput.ClearTextOnFocus = false
MusicSessionIdInput.Parent = MusicJoinPanel
MusicSessionIdCorner = Instance.new("UICorner")
MusicSessionIdCorner.CornerRadius = UDim.new(0, 8)
MusicSessionIdCorner.Parent = MusicSessionIdInput

MusicJoinNowButton = MusicMakeButton(MusicJoinPanel, "MusicJoinNowButton", "JOIN SESSION", UDim2.new(0, 10, 0, 126), UDim2.new(0.5, -15, 0, 30))
MusicLeaveJoinButton = MusicMakeSecondaryButton(MusicJoinPanel, "MusicLeaveJoinButton", "LEAVE SESSION", UDim2.new(0.5, 5, 0, 126), UDim2.new(0.5, -15, 0, 30))
MusicLeaveJoinButton.Active = true
MusicLeaveJoinButton.AutoButtonColor = true
MusicLeaveJoinButton.ZIndex = 100
MusicJoinInviteGlobalButton = MusicMakeSecondaryButton(MusicJoinPanel, "MusicJoinInviteGlobalButton", "INVITE GLOBAL", UDim2.new(0, 10, 0, 166), UDim2.new(0.5, -15, 0, 27))
MusicJoinInviteServerButton = MusicMakeSecondaryButton(MusicJoinPanel, "MusicJoinInviteServerButton", "INVITE SERVER", UDim2.new(0.5, 5, 0, 166), UDim2.new(0.5, -15, 0, 27))
MusicJoinMuteButton = MusicMakeSecondaryButton(MusicJoinPanel, "MusicJoinMuteButton", "🔊 MUTE MY AUDIO", UDim2.new(0, 10, 0, 201), UDim2.new(1, -20, 0, 27))
MusicJoinMuteButton.Visible = false

MusicJoinQueueHeader = Instance.new("TextLabel")
MusicJoinQueueHeader.Size = UDim2.new(1, -20, 0, 20)
MusicJoinQueueHeader.Position = UDim2.new(0, 10, 0, 239)
MusicJoinQueueHeader.BackgroundTransparency = 1
MusicJoinQueueHeader.Font = Enum.Font.GothamBold
MusicJoinQueueHeader.Text = "UP NEXT"
MusicJoinQueueHeader.TextColor3 = Color3.fromRGB(255, 255, 255)
MusicJoinQueueHeader.TextSize = 10
MusicJoinQueueHeader.TextXAlignment = Enum.TextXAlignment.Left
MusicJoinQueueHeader.Parent = MusicJoinPanel

MusicJoinQueueScroller = Instance.new("ScrollingFrame")
MusicJoinQueueScroller.Size = UDim2.new(1, -20, 0, 145)
MusicJoinQueueScroller.Position = UDim2.new(0, 10, 0, 261)
MusicJoinQueueScroller.BackgroundColor3 = Color3.fromRGB(12, 12, 20)
MusicJoinQueueScroller.BackgroundTransparency = 0.12
MusicJoinQueueScroller.BorderSizePixel = 0
MusicJoinQueueScroller.ScrollBarThickness = 3
MusicJoinQueueScroller.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
MusicJoinQueueScroller.Parent = MusicJoinPanel
MusicJoinQueueCorner = Instance.new("UICorner")
MusicJoinQueueCorner.CornerRadius = UDim.new(0, 7)
MusicJoinQueueCorner.Parent = MusicJoinQueueScroller
MusicJoinQueueLayout = Instance.new("UIListLayout")
MusicJoinQueueLayout.SortOrder = Enum.SortOrder.LayoutOrder
MusicJoinQueueLayout.Padding = UDim.new(0, 3)
MusicJoinQueueLayout.Parent = MusicJoinQueueScroller
MusicJoinQueueLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    MusicJoinQueueScroller.CanvasSize = UDim2.new(0, 0, 0, MusicJoinQueueLayout.AbsoluteContentSize.Y + 8)
end)

MusicSelectedCategory = nil

function UpdateMusicSessionDisplay()
    local data = ActiveMusicSessionData
    if not data then
        MusicNowPlayingLabel.Text = "Not in a music session"
        MusicHostLabel.Text = "Host: —"
        MusicSessionIdLabel.Text = "Session ID: —"
        return
    end

    local current, index, count = GetMusicCurrentItem(data)
    local song = current and tostring(current.Name or current.Url or "YouTube Track") or "Nothing queued"
    local state = data.Paused and "PAUSED" or (data.Playing and "PLAYING" or "STOPPED")
    MusicNowPlayingLabel.Text = "[" .. state .. "] " .. TruncateText(song, 62) .. "  •  " .. tostring(index or 0) .. "/" .. tostring(count or 0)
    MusicHostLabel.Text = "Host: " .. tostring(data.HostDisplayName or "Unknown") .. "  (@" .. tostring(data.HostUsername or "unknown") .. ")"
    MusicSessionIdLabel.Text = "Session ID: " .. tostring(ActiveMusicSessionId or "—")
    MusicPlayPauseButton.Text = data.Paused and "▶ PLAY" or (data.Playing and "⏸ PAUSE" or "▶ PLAY")
    MusicQueueStatusLabel.Text = "Queue: " .. tostring(math.max(0, (count or 0) - (index or 0))) .. " up next"
end

function UpdateMusicSessionMembersUI()
    if not MusicMembersScroller then return end
    for _, child in ipairs(MusicMembersScroller:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end

    local members = MusicSessionMemberCache or {}
    local rows = 0
    for userId, member in pairs(members) do
        if type(member) == "table" then
            rows += 1
            local row = Instance.new("Frame")
            row.Name = "Member_" .. tostring(userId)
            row.Size = UDim2.new(1, -8, 0, 28)
            row.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
            row.BackgroundTransparency = 0.12
            row.BorderSizePixel = 0
            row.Parent = MusicMembersScroller
            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 6)
            corner.Parent = row

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, -72, 1, 0)
            label.Position = UDim2.new(0, 8, 0, 0)
            label.BackgroundTransparency = 1
            label.Font = Enum.Font.GothamMedium
            label.Text = tostring(member.DisplayName or member.Username or "Unknown") .. (tostring(userId) == tostring(LocalPlayer.UserId) and "  • YOU" or "")
            label.TextColor3 = Color3.fromRGB(235, 235, 240)
            label.TextSize = 9
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.TextTruncate = Enum.TextTruncate.AtEnd
            label.Parent = row

            if MusicSessionIsHost() and tostring(userId) ~= tostring(LocalPlayer.UserId) then
                local kick = Instance.new("TextButton")
                kick.Size = UDim2.new(0, 52, 0, 20)
                kick.Position = UDim2.new(1, -58, 0.5, -10)
                kick.BackgroundColor3 = Color3.fromRGB(130, 35, 35)
                kick.BorderSizePixel = 0
                kick.Font = Enum.Font.GothamBold
                kick.Text = "KICK"
                kick.TextColor3 = Color3.fromRGB(255, 255, 255)
                kick.TextSize = 8
                kick.Parent = row
                local kickCorner = Instance.new("UICorner")
                kickCorner.CornerRadius = UDim.new(0, 5)
                kickCorner.Parent = kick
                kick.Activated:Connect(function()
                    local id = ActiveMusicSessionId
                    if not id or not MusicSessionIsHost() then return end
                    HttpRequest(MusicSessionUrl(id, "/kicks/" .. tostring(userId)), "PUT", { At = os.time(), By = LocalPlayer.Name })
                    HttpRequest(MusicSessionUrl(id, "/members/" .. tostring(userId)), "DELETE")
                    task.delay(0.1, RefreshMusicSessionMembers)
                end)
            end
        end
    end
    MusicMembersScroller.CanvasSize = UDim2.new(0, 0, 0, rows * 31 + 8)
end

local function RenderMusicQueue(scroller, hostView)
    if not scroller then return end
    for _, child in ipairs(scroller:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    local data = ActiveMusicSessionData
    local queue = GetMusicQueue(data)
    local currentIndex = tonumber(data and data.CurrentIndex) or 0
    local rows = 0

    for index, item in ipairs(queue) do
        if type(item) == "table" then
            rows += 1
            local row = Instance.new("Frame")
            row.Size = UDim2.new(1, -8, 0, 27)
            row.BackgroundColor3 = index == currentIndex and Color3.fromRGB(55, 28, 28) or Color3.fromRGB(25, 25, 38)
            row.BackgroundTransparency = 0.12
            row.BorderSizePixel = 0
            row.LayoutOrder = index
            row.Parent = scroller
            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 6)
            corner.Parent = row

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, hostView and -60 or -10, 1, 0)
            label.Position = UDim2.new(0, 8, 0, 0)
            label.BackgroundTransparency = 1
            label.Font = Enum.Font.GothamMedium
            label.Text = (index == currentIndex and "▶ " or "   ") .. tostring(index) .. ". " .. tostring(item.Name or item.Url or "YouTube Track")
            label.TextColor3 = Color3.fromRGB(235, 235, 240)
            label.TextSize = 8
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.TextTruncate = Enum.TextTruncate.AtEnd
            label.Parent = row

            if hostView then
                local remove = Instance.new("TextButton")
                remove.Size = UDim2.new(0, 44, 0, 19)
                remove.Position = UDim2.new(1, -50, 0.5, -9)
                remove.BackgroundColor3 = Color3.fromRGB(70, 45, 50)
                remove.BorderSizePixel = 0
                remove.Font = Enum.Font.GothamBold
                remove.Text = "×"
                remove.TextColor3 = Color3.fromRGB(255, 255, 255)
                remove.TextSize = 12
                remove.Parent = row
                local removeCorner = Instance.new("UICorner")
                removeCorner.CornerRadius = UDim.new(0, 5)
                removeCorner.Parent = remove
                remove.Activated:Connect(function()
                    if not MusicSessionIsHost() then return end
                    local newQueue = GetMusicQueue(ActiveMusicSessionData)
                    if not newQueue[index] then return end
                    table.remove(newQueue, index)
                    local newIndex = tonumber(ActiveMusicSessionData.CurrentIndex) or 1
                    if index < newIndex then newIndex -= 1 end
                    if #newQueue == 0 then
                        HostStopMusic()
                        return
                    end
                    if newIndex > #newQueue then newIndex = #newQueue end
                    ActiveMusicSessionData.Queue = newQueue
                    ActiveMusicSessionData.CurrentIndex = newIndex
                    ActiveMusicSessionData.Revision = MusicSessionRevision()
                    HttpRequest(MusicSessionPath(ActiveMusicSessionId), "PATCH", {
                        Queue = newQueue,
                        CurrentIndex = newIndex,
                        Revision = ActiveMusicSessionData.Revision,
                        UpdatedAt = os.time(),
                    })
                    UpdateMusicSessionDisplay()
                    RenderMusicQueue(MusicQueueScroller, true)
                    RenderMusicQueue(MusicJoinQueueScroller, false)
                end)
            end
        end
    end
    scroller.CanvasSize = UDim2.new(0, 0, 0, rows * 30 + 8)
end

function UpdateMusicSessionMode()
    local inSession = ActiveMusicSessionId ~= nil
    MusicChoiceTitle.Visible = not inSession and not MusicCreatePanel.Visible and not MusicJoinPanel.Visible
    MusicCreateChoiceButton.Visible = not inSession and not MusicCreatePanel.Visible and not MusicJoinPanel.Visible
    MusicJoinChoiceButton.Visible = not inSession and not MusicCreatePanel.Visible and not MusicJoinPanel.Visible
    MusicChoiceHint.Visible = not inSession and not MusicCreatePanel.Visible and not MusicJoinPanel.Visible

    if not inSession then
        MusicCreatePanel.Visible = false
        MusicJoinPanel.Visible = false
        MusicMuteButton.Visible = false
        MusicJoinMuteButton.Visible = false
        return
    end

    local host = MusicSessionIsHost()
    MusicCreatePanel.Visible = host
    MusicJoinPanel.Visible = not host
    MusicMuteButton.Visible = true
    MusicJoinMuteButton.Visible = not host
    MusicJoinMuteButton.Text = MusicSessionMuted and "🔇 UNMUTE MY AUDIO" or "🔊 MUTE MY AUDIO"
    MusicChoiceTitle.Visible = false
    MusicCreateChoiceButton.Visible = false
    MusicJoinChoiceButton.Visible = false
    MusicChoiceHint.Visible = false

    RenderMusicQueue(host and MusicQueueScroller or MusicJoinQueueScroller, host)
    if host then UpdateMusicSessionMembersUI() end
end

local function HostWriteSession(patch)
    if not ActiveMusicSessionId or not MusicSessionIsHost() then return false end
    patch = patch or {}
    patch.Revision = patch.Revision or MusicSessionRevision()
    patch.UpdatedAt = patch.UpdatedAt or os.time()
    local result = HttpRequest(MusicSessionPath(ActiveMusicSessionId), "PATCH", patch)
    if type(ActiveMusicSessionData) == "table" then
        for key, value in pairs(patch) do ActiveMusicSessionData[key] = value end
    end
    MusicSessionLastRevision = patch.Revision
    return result ~= nil or true
end

function HostQueueYouTube(url, playNow)
    if not ActiveMusicSessionId or not MusicSessionIsHost() then
        ShowLocalSystemMessage("[AI MoD] : Only the Music Session Host can add music.", true)
        return
    end

    url = NormalizeYouTubeUrl(url)
    if not url then
        ShowLocalSystemMessage("[AI MoD] : Enter a valid YouTube URL.", true)
        return
    end

    local urls = { url }
    if IsYouTubePlaylistUrl(url) then
        local extracted, errorText = ExtractPlaylistVideoUrls(url)
        if not extracted then
            ShowLocalSystemMessage("[AI MoD] : " .. tostring(errorText), true)
            return
        end
        urls = extracted
    end

    local queue = GetMusicQueue(ActiveMusicSessionData)
    local added = 0
    for _, videoUrl in ipairs(urls) do
        local duplicate = false
        for _, existing in ipairs(queue) do
            if type(existing) == "table" and tostring(existing.Url) == tostring(videoUrl) then
                duplicate = true
                break
            end
        end
        if not duplicate then
            table.insert(queue, {
                Url = videoUrl,
                Name = GetYouTubeTitle(videoUrl) or ("YouTube • " .. tostring(GetYouTubeVideoId(videoUrl) or "Track")),
                AddedBy = LocalPlayer.Name,
                AddedAt = os.time(),
            })
            added += 1
        end
    end

    if added == 0 then
        ShowLocalSystemMessage("[AI MoD] : Those tracks are already in the queue.", true)
        return
    end

    local currentIndex = tonumber(ActiveMusicSessionData.CurrentIndex) or 0
    local wasPlaying = ActiveMusicSessionData.Playing == true
    if currentIndex <= 0 then currentIndex = 1 end

    local patch = {
        Queue = queue,
        CurrentIndex = currentIndex,
    }

    if playNow then
        currentIndex = #queue - added + 1
        patch.CurrentIndex = currentIndex
        patch.Playing = true
        patch.Paused = false
        patch.Position = 0
        patch.StartedAt = os.time()
    elseif not wasPlaying and not ActiveMusicSessionData.Paused then
        patch.CurrentIndex = currentIndex
        patch.Playing = true
        patch.Paused = false
        patch.Position = 0
        patch.StartedAt = os.time()
    end

    HostWriteSession(patch)
    UpdateMusicSessionDisplay()
    UpdateMusicSessionMode()
    ShowLocalSystemMessage("[AI MoD] : Added " .. tostring(added) .. " YouTube track" .. (added == 1 and "" or "s") .. " to the queue.")
    if ActiveMusicSessionData.Playing then ApplyMusicSessionAudio(ActiveMusicSessionData) end
end

function HostToggleMusicPlayback()
    if not ActiveMusicSessionId or not MusicSessionIsHost() then return end
    local data = ActiveMusicSessionData
    local current = GetMusicCurrentItem(data)
    if not current then
        ShowLocalSystemMessage("[AI MoD] : Queue is empty. Add a YouTube URL first.", true)
        return
    end

    local position = GetMusicPosition(data)
    if data.Playing and not data.Paused then
        HostWriteSession({ Playing = false, Paused = true, Position = position, StartedAt = 0 })
        if MusicSessionCurrentTrack then pcall(function() MusicSessionCurrentTrack:Pause() end) end
    else
        HostWriteSession({ Playing = true, Paused = false, Position = position, StartedAt = os.time() - position })
        ApplyMusicSessionAudio(ActiveMusicSessionData)
    end
    UpdateMusicSessionDisplay()
end

function HostNextMusic()
    if not ActiveMusicSessionId or not MusicSessionIsHost() then return end
    local queue = GetMusicQueue(ActiveMusicSessionData)
    local index = tonumber(ActiveMusicSessionData.CurrentIndex) or 0
    if index < #queue then
        index += 1
        HostWriteSession({ CurrentIndex = index, Playing = true, Paused = false, Position = 0, StartedAt = os.time() })
        StopLocalMusicSessionAudio()
        ApplyMusicSessionAudio(ActiveMusicSessionData)
    else
        HostWriteSession({ Playing = false, Paused = true, Position = 0, StartedAt = 0 })
        StopLocalMusicSessionAudio()
    end
    UpdateMusicSessionDisplay()
end

function HostPreviousMusic()
    if not ActiveMusicSessionId or not MusicSessionIsHost() then return end
    local queue = GetMusicQueue(ActiveMusicSessionData)
    local index = tonumber(ActiveMusicSessionData.CurrentIndex) or 1
    local position = GetMusicPosition(ActiveMusicSessionData)
    if position > 5 then
        HostWriteSession({ Playing = true, Paused = false, Position = 0, StartedAt = os.time() })
    elseif index > 1 then
        index -= 1
        HostWriteSession({ CurrentIndex = index, Playing = true, Paused = false, Position = 0, StartedAt = os.time() })
    else
        HostWriteSession({ Playing = true, Paused = false, Position = 0, StartedAt = os.time() })
    end
    StopLocalMusicSessionAudio()
    ApplyMusicSessionAudio(ActiveMusicSessionData)
    UpdateMusicSessionDisplay()
end

function HostStopMusic()
    if not ActiveMusicSessionId or not MusicSessionIsHost() then
        ShowLocalSystemMessage("[AI MoD] : Only the Music Session Host can stop music.", true)
        return
    end
    local position = GetMusicPosition(ActiveMusicSessionData)
    HostWriteSession({ Playing = false, Paused = true, Position = position, StartedAt = 0 })
    StopLocalMusicSessionAudio()
    UpdateMusicSessionDisplay()
end

function StartOwnMusicSession()
    if ActiveMusicSessionId then
        ShowLocalSystemMessage("[AI MoD] : You already have a Music Session: " .. ActiveMusicSessionId)
        return
    end
    local id = MusicSessionRandomId()
    local payload = {
        SessionId = id,
        HostUsername = LocalPlayer.Name,
        HostDisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        HostUserId = LocalPlayer.UserId,
        HostPfpId = CurrentPfpId,
        Playing = false,
        Paused = true,
        Ended = false,
        CurrentIndex = 0,
        Position = 0,
        StartedAt = 0,
        Queue = {},
        Revision = MusicSessionRevision(),
        CreatedAt = os.time(),
        UpdatedAt = os.time(),
    }
    local result = HttpRequest(MusicSessionPath(id), "PUT", payload)
    if result ~= nil or HttpRequest(MusicSessionPath(id), "GET") then
        ActiveMusicSessionId = id
        ActiveMusicSessionData = payload
        MusicSessionLastRevision = payload.Revision
        HttpRequest(MusicSessionUrl(id, "/members/" .. tostring(LocalPlayer.UserId)), "PUT", {
            Username = LocalPlayer.Name,
            DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
            JoinedAt = os.time(),
            PfpId = CurrentPfpId,
        })
        UpdateMusicSessionDisplay()
        UpdateMusicSessionMode()
        RefreshMusicSessionMembers()
        ShowLocalSystemMessage("[AI MoD] : Music Session started. Session ID: " .. id)
    else
        ShowLocalSystemMessage("[AI MoD] : Failed to create Music Session. Check Firebase access.", true)
    end
end

function JoinMusicSessionById(id)
    id = tostring(id or ""):upper():gsub("%s+", "")
    if id == "" then
        ShowLocalSystemMessage("[AI MoD] : Enter a Session ID first.", true)
        return
    end
    local data = HttpRequest(MusicSessionPath(id), "GET")
    if type(data) ~= "table" or not data.HostUsername or data.Ended == true then
        ShowLocalSystemMessage("[AI MoD] : Music Session not found or already ended: " .. id, true)
        return
    end

    local kick = HttpRequest(MusicSessionUrl(id, "/kicks/" .. tostring(LocalPlayer.UserId)), "GET")
    if kick ~= nil then
        ShowLocalSystemMessage("[AI MoD] : You have been kicked from this Music Session.", true)
        return
    end

    ActiveMusicSessionId = id
    ActiveMusicSessionData = data
    MusicSessionLastRevision = nil
    HttpRequest(MusicSessionUrl(id, "/members/" .. tostring(LocalPlayer.UserId)), "PUT", {
        Username = LocalPlayer.Name,
        DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        JoinedAt = os.time(),
        PfpId = CurrentPfpId,
    })
    ApplyMusicSessionAudio(data)
    UpdateMusicSessionDisplay()
    UpdateMusicSessionMode()
    RefreshMusicSessionMembers()
    ShowLocalSystemMessage("[AI MoD] : Joined Music Session " .. id .. " hosted by " .. tostring(data.HostDisplayName or data.HostUsername) .. " ✓")
end

function LeaveOrEndMusicSession()
    local id = ActiveMusicSessionId
    local sessionData = ActiveMusicSessionData
    if not id then
        ResetMusicSessionToChoiceMenu()
        return
    end

    local isHost = sessionData and sessionData.HostUsername == LocalPlayer.Name
    ResetMusicSessionToChoiceMenu()

    task.spawn(function()
        pcall(function()
            if isHost then
                HttpRequest(MusicSessionPath(id), "PATCH", {
                    Ended = true,
                    Playing = false,
                    Paused = true,
                    UpdatedAt = os.time(),
                    EndedAt = os.time(),
                    Revision = MusicSessionRevision(),
                })
            else
                HttpRequest(MusicSessionUrl(id, "/members/" .. tostring(LocalPlayer.UserId)), "DELETE")
            end
        end)
    end)
end

function SendMusicSessionInvite(isGlobal)
    if not ActiveMusicSessionId or not ActiveMusicSessionData then
        ShowLocalSystemMessage("[AI MoD] : Start or join a Music Session first.", true)
        return
    end
    local payload = {
        ClientMessageId = LocalPlayer.UserId .. "_musicinvite_" .. tostring(math.floor(os.clock() * 1000000)) .. "_" .. tostring(math.random(100000,999999)),
        Username = KRONO_JOCKIE_USERNAME,
        DisplayName = KRONO_JOCKIE_NAME,
        Text = GetFormattedDisplayName(LocalPlayer.DisplayName, true) .. " invited you to join their Music Session",
        Timestamp = os.time(),
        Role = "BOT",
        PfpId = KRONO_JOCKIE_PFP,
        IsPrivate = false,
        IsMusicSessionInvite = true,
        MusicSessionId = ActiveMusicSessionId,
        MusicSessionHostUsername = ActiveMusicSessionData.HostUsername,
        MusicSessionHostDisplayName = ActiveMusicSessionData.HostDisplayName,
        MusicSessionHostPfpId = ActiveMusicSessionData.HostPfpId or CurrentPfpId,
    }
    local endpoint = isGlobal and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
    local cache = isGlobal and GlobalCachedMessages or ServerCachedMessages
    local ui = isGlobal and GlobalUIElements or ServerUIElements
    local localKey = "local_" .. payload.ClientMessageId
    cache[localKey] = payload
    local card = CreateMessageCard(payload, isGlobal)
    card.Visible = (ActiveTab == (isGlobal and "GLOBAL" or "SERVER"))
    table.insert(ui, card)
    task.spawn(function() HttpRequest(endpoint, "POST", payload) end)
end

MusicMuteButton.MouseButton1Click:Connect(function()
    MusicSessionMuted = not MusicSessionMuted
    if MusicSessionCurrentTrack then
        pcall(function() MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5 end)
    end
    MusicMuteButton.Text = MusicSessionMuted and "🔇 UNMUTE" or "🔊 MUTE"
    MusicJoinMuteButton.Text = MusicSessionMuted and "🔇 UNMUTE MY AUDIO" or "🔊 MUTE MY AUDIO"
end)

MusicCreateChoiceButton.MouseButton1Click:Connect(function()
    MusicChoiceTitle.Visible = false
    MusicCreateChoiceButton.Visible = false
    MusicJoinChoiceButton.Visible = false
    MusicChoiceHint.Visible = false
    MusicCreatePanel.Visible = true
    MusicJoinPanel.Visible = false
    StartOwnMusicSession()
    UpdateMusicSessionMode()
end)

MusicJoinChoiceButton.MouseButton1Click:Connect(function()
    MusicChoiceTitle.Visible = false
    MusicCreateChoiceButton.Visible = false
    MusicJoinChoiceButton.Visible = false
    MusicChoiceHint.Visible = false
    MusicCreatePanel.Visible = false
    MusicJoinPanel.Visible = true
    MusicSessionFrame.CanvasPosition = Vector2.new(0, 0)
end)

MusicJoinNowButton.MouseButton1Click:Connect(function()
    JoinMusicSessionById(MusicSessionIdInput.Text)
end)

MusicLeaveJoinButton.Activated:Connect(function() LeaveOrEndMusicSession() end)
MusicEndCreateButton.Activated:Connect(function() LeaveOrEndMusicSession() end)
MusicInviteGlobalButton.MouseButton1Click:Connect(function() SendMusicSessionInvite(true) end)
MusicInviteServerButton.MouseButton1Click:Connect(function() SendMusicSessionInvite(false) end)
MusicJoinInviteGlobalButton.MouseButton1Click:Connect(function() SendMusicSessionInvite(true) end)
MusicJoinInviteServerButton.MouseButton1Click:Connect(function() SendMusicSessionInvite(false) end)

MusicAddQueueButton.Activated:Connect(function()
    HostQueueYouTube(MusicYouTubeInput.Text, false)
    MusicYouTubeInput.Text = ""
end)

MusicPlayUrlButton.Activated:Connect(function()
    HostQueueYouTube(MusicYouTubeInput.Text, true)
    MusicYouTubeInput.Text = ""
end)

MusicYouTubeInput.FocusLost:Connect(function(enterPressed)
    if enterPressed then
        HostQueueYouTube(MusicYouTubeInput.Text, false)
        MusicYouTubeInput.Text = ""
    end
end)

MusicPreviousButton.Activated:Connect(function() HostPreviousMusic() end)
MusicPlayPauseButton.Activated:Connect(function() HostToggleMusicPlayback() end)
MusicNextButton.Activated:Connect(function() HostNextMusic() end)
MusicStopButton.Activated:Connect(function() HostStopMusic() end)

MusicClearQueueButton.Activated:Connect(function()
    if not MusicSessionIsHost() then return end
    local current = GetMusicCurrentItem(ActiveMusicSessionData)
    local queue = current and { current } or {}
    HostWriteSession({ Queue = queue, CurrentIndex = current and 1 or 0 })
    UpdateMusicSessionDisplay()
    UpdateMusicSessionMode()
end)

MusicJoinMuteButton.MouseButton1Click:Connect(function()
    MusicSessionMuted = not MusicSessionMuted
    if MusicSessionCurrentTrack then
        pcall(function() MusicSessionCurrentTrack.Volume = MusicSessionMuted and 0 or 0.5 end)
    end
    MusicJoinMuteButton.Text = MusicSessionMuted and "🔇 UNMUTE MY AUDIO" or "🔊 MUTE MY AUDIO"
    MusicMuteButton.Text = MusicSessionMuted and "🔇 UNMUTE" or "🔊 MUTE"
end)

MusicSessionFrame:GetPropertyChangedSignal("AbsoluteCanvasSize"):Connect(function()
    if MusicSessionFrame.AbsoluteCanvasSize.Y < 680 then
        MusicSessionFrame.CanvasSize = UDim2.new(0, 0, 0, 680)
    end
end)

ProfileFrame = Instance.new("ScrollingFrame")
ProfileFrame.Name = "ProfileFrame"
ProfileFrame.Size = UDim2.new(1, -16, 1, -48)
ProfileFrame.Position = UDim2.new(0, 8, 0, 40)
ProfileFrame.BackgroundTransparency = 1
ProfileFrame.BorderSizePixel = 0
ProfileFrame.ScrollBarThickness = 3
ProfileFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
ProfileFrame.ScrollingDirection = Enum.ScrollingDirection.Y
ProfileFrame.CanvasSize = UDim2.new(0, 0, 0, 680)
ProfileFrame.Visible = false
ProfileFrame.Parent = MainFrame




local function KronosPfpImage(id, size)
    id = tostring(id or "")
    size = size or 420
    if id == "" then return "" end
    return "rbxthumb://type=Asset&id=" .. id .. "&w=" .. tostring(size) .. "&h=" .. tostring(size)
end

ProfileHeaderCard = Instance.new("Frame")
ProfileHeaderCard.Size = UDim2.new(1, 0, 0, 96)
ProfileHeaderCard.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
ProfileHeaderCard.BackgroundTransparency = 0.22
ProfileHeaderCard.BorderSizePixel = 0
ProfileHeaderCard.Parent = ProfileFrame
ProfileHeaderCorner = Instance.new("UICorner")
ProfileHeaderCorner.CornerRadius = UDim.new(0, 11)
ProfileHeaderCorner.Parent = ProfileHeaderCard
ProfileHeaderStroke = Instance.new("UIStroke")
ProfileHeaderStroke.Thickness = 1.2
ProfileHeaderStroke.Color = Color3.fromRGB(70, 70, 82)
ProfileHeaderStroke.Transparency = 0.35
ProfileHeaderStroke.Parent = ProfileHeaderCard



ProfilePfp = Instance.new("ImageLabel")
ProfilePfp.Name = "ProfilePfp"
ProfilePfp.Size = UDim2.new(0, 68, 0, 68)
ProfilePfp.Position = UDim2.new(0, 9, 0, 14)
ProfilePfp.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
ProfilePfp.Image = KronosPfpImage(CurrentPfpId, 420)
ProfilePfp.ScaleType = Enum.ScaleType.Crop
ProfilePfp.Parent = ProfileHeaderCard
ProfilePfpCorner = Instance.new("UICorner")
ProfilePfpCorner.CornerRadius = UDim.new(1, 0)
ProfilePfpCorner.Parent = ProfilePfp


ProfilePfpRing = Instance.new("UIStroke")
ProfilePfpRing.Name = "SelectedPfpRedRing"
ProfilePfpRing.Thickness = 3
ProfilePfpRing.Color = Color3.fromRGB(255, 35, 35)
ProfilePfpRing.Transparency = 0
ProfilePfpRing.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
ProfilePfpRing.Parent = ProfilePfp

ProfileDisplayName = Instance.new("TextLabel")
ProfileDisplayName.Size = UDim2.new(0, 190, 0, 32)
ProfileDisplayName.Position = UDim2.new(0, 88, 0, 17)
ProfileDisplayName.BackgroundTransparency = 1
ProfileDisplayName.Font = Enum.Font.GothamBold
ProfileDisplayName.Text = GetFormattedDisplayName(LocalPlayer.DisplayName, true)
ProfileDisplayName.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfileDisplayName.TextSize = 20
ProfileDisplayName.TextXAlignment = Enum.TextXAlignment.Left
ProfileDisplayName.TextTruncate = Enum.TextTruncate.AtEnd
ProfileDisplayName.Parent = ProfileHeaderCard


ProfileUsername = Instance.new("TextLabel")
ProfileUsername.Size = UDim2.new(0, 190, 0, 20)
ProfileUsername.Position = UDim2.new(0, 88, 0, 51)
ProfileUsername.BackgroundTransparency = 1
ProfileUsername.Font = Enum.Font.Gotham
ProfileUsername.Text = "@" .. LocalPlayer.Name
ProfileUsername.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfileUsername.TextSize = 11
ProfileUsername.TextXAlignment = Enum.TextXAlignment.Left
ProfileUsername.TextTruncate = Enum.TextTruncate.AtEnd
ProfileUsername.Parent = ProfileHeaderCard

function ProfileSocialImage(name, imageId, pos)
    local b = Instance.new("ImageButton")
    b.Name = name
    b.Size = UDim2.new(0, 28, 0, 28)
    b.Position = pos
    b.BackgroundColor3 = Color3.fromRGB(72, 72, 90)
    b.BackgroundTransparency = 0
    b.BorderSizePixel = 0
    b.Image = "rbxthumb://type=Asset&id=" .. tostring(imageId) .. "&w=150&h=150"
    b.ImageTransparency = 0
    b.ScaleType = Enum.ScaleType.Fit
    b.AutoButtonColor = true
    b.ZIndex = 4
    b.Parent = ProfileHeaderCard

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 7)
    c.Parent = b
    local stroke = Instance.new("UIStroke")
    stroke.Thickness = 1.2
    stroke.Transparency = 0.05
    stroke.Color = Color3.fromRGB(185, 185, 200)
    stroke.Parent = b
    return b
end


InstagramButton = ProfileSocialImage("InstagramButton", "18355586412", UDim2.new(0, 285, 0, 20))
DiscordButton = ProfileSocialImage("DiscordButton", "18505728250", UDim2.new(0, 319, 0, 20))
SnapchatButton = ProfileSocialImage("SnapchatButton", "385616312", UDim2.new(0, 353, 0, 20))

ProfileShareButton = Instance.new("TextButton")
ProfileShareButton.Name = "ProfileShareButton"
ProfileShareButton.Size = UDim2.new(0, 28, 0, 28)
ProfileShareButton.Position = UDim2.new(0, 387, 0, 20)
ProfileShareButton.BackgroundColor3 = Color3.fromRGB(72, 72, 90)
ProfileShareButton.BackgroundTransparency = 0
ProfileShareButton.BorderSizePixel = 0
ProfileShareButton.Text = "↗"
ProfileShareButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfileShareButton.Font = Enum.Font.GothamBold
ProfileShareButton.TextSize = 17
ProfileShareButton.AutoButtonColor = true
ProfileShareButton.ZIndex = 4
ProfileShareButton.Parent = ProfileHeaderCard
ProfileShareCorner = Instance.new("UICorner")
ProfileShareCorner.CornerRadius = UDim.new(0, 7)
ProfileShareCorner.Parent = ProfileShareButton
ProfileShareStroke = Instance.new("UIStroke")
ProfileShareStroke.Thickness = 1.2
ProfileShareStroke.Transparency = 0.05
ProfileShareStroke.Color = Color3.fromRGB(185, 185, 200)
ProfileShareStroke.Parent = ProfileShareButton








ProfilePfpCard = Instance.new("Frame")
ProfilePfpCard.Size = UDim2.new(1, 0, 0, 590)
ProfilePfpCard.Position = UDim2.new(0, 0, 0, 104)
ProfilePfpCard.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
ProfilePfpCard.BackgroundTransparency = 0.3
ProfilePfpCard.BorderSizePixel = 0
ProfilePfpCard.Parent = ProfileFrame
ProfilePfpCardCorner = Instance.new("UICorner")
ProfilePfpCardCorner.CornerRadius = UDim.new(0, 10)
ProfilePfpCardCorner.Parent = ProfilePfpCard

ProfilePfpTitle = Instance.new("TextLabel")
ProfilePfpTitle.Size = UDim2.new(1, -16, 0, 20)
ProfilePfpTitle.Position = UDim2.new(0, 8, 0, 7)
ProfilePfpTitle.BackgroundTransparency = 1
ProfilePfpTitle.Font = Enum.Font.GothamBold
ProfilePfpTitle.Text = "CHANGE PROFILE PICTURE"
ProfilePfpTitle.TextColor3 = Color3.fromRGB(240, 240, 245)
ProfilePfpTitle.TextSize = 10
ProfilePfpTitle.TextXAlignment = Enum.TextXAlignment.Left
ProfilePfpTitle.Parent = ProfilePfpCard

PfpCustomInput = Instance.new("TextBox")
PfpCustomInput.Size = UDim2.new(0, 205, 0, 29)
PfpCustomInput.Position = UDim2.new(0, 8, 0, 32)
PfpCustomInput.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
PfpCustomInput.BackgroundTransparency = 0.15
PfpCustomInput.BorderSizePixel = 0
PfpCustomInput.Font = Enum.Font.Code
PfpCustomInput.PlaceholderText = "Custom image ID"
PfpCustomInput.Text = ""
PfpCustomInput.TextColor3 = Color3.fromRGB(235, 235, 240)
PfpCustomInput.TextSize = 9
PfpCustomInput.Parent = ProfilePfpCard
PfpCustomCorner = Instance.new("UICorner")
PfpCustomCorner.CornerRadius = UDim.new(0, 7)
PfpCustomCorner.Parent = PfpCustomInput

PfpCustomSetButton = MusicMakeButton(ProfilePfpCard, "PfpCustomSetButton", "SET CUSTOM", UDim2.new(0, 218, 0, 32), UDim2.new(0, 105, 0, 29))
PfpRandomButton = MusicMakeButton(ProfilePfpCard, "PfpRandomButton", "RANDOM", UDim2.new(0, 331, 0, 32), UDim2.new(0, 105, 0, 29))

function BuildPfpSection(parent, title, emoji, ids, topY, sectionName)
    local section = Instance.new("Frame")
    section.Name = sectionName
    section.Size = UDim2.new(1, -16, 0, 250)
    section.Position = UDim2.new(0, 8, 0, topY)
    section.BackgroundColor3 = Color3.fromRGB(22, 22, 34)
    section.BackgroundTransparency = 0.12
    section.BorderSizePixel = 0
    section.Parent = parent

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 9)
    corner.Parent = section

    local heading = Instance.new("TextLabel")
    heading.Size = UDim2.new(1, -16, 0, 25)
    heading.Position = UDim2.new(0, 8, 0, 6)
    heading.BackgroundTransparency = 1
    heading.Font = Enum.Font.GothamBold
    heading.Text = title .. " " .. emoji
    heading.TextColor3 = Color3.fromRGB(245, 245, 250)
    heading.TextSize = 11
    heading.TextXAlignment = Enum.TextXAlignment.Left
    heading.Parent = section

    local scroller = Instance.new("ScrollingFrame")
    scroller.Name = "PfpScroller"
    scroller.Size = UDim2.new(1, -16, 0, 207)
    scroller.Position = UDim2.new(0, 8, 0, 34)
    scroller.BackgroundTransparency = 1
    scroller.BorderSizePixel = 0
    scroller.ScrollBarThickness = 3
    scroller.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
    scroller.ScrollingDirection = Enum.ScrollingDirection.Y
    scroller.AutomaticCanvasSize = Enum.AutomaticSize.Y
    scroller.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroller.Parent = section

    local grid = Instance.new("UIGridLayout")
    grid.CellSize = UDim2.new(0, 58, 0, 58)
    grid.CellPadding = UDim2.new(0, 6, 0, 6)
    grid.FillDirection = Enum.FillDirection.Horizontal
    grid.SortOrder = Enum.SortOrder.LayoutOrder
    grid.Parent = scroller

    for index, id in ipairs(ids) do
        local b = Instance.new("ImageButton")
        b.Name = "Pfp_" .. tostring(index)
        b.Size = UDim2.new(0, 58, 0, 58)
        b.BackgroundColor3 = Color3.fromRGB(30, 30, 43)
        b.BackgroundTransparency = 0.02
        b.BorderSizePixel = 0
        b.Image = KronosPfpImage(id, 150)
        b.ScaleType = Enum.ScaleType.Crop
        b.AutoButtonColor = true
        b.LayoutOrder = index
        b.Parent = scroller

        local bc = Instance.new("UICorner")
        bc.CornerRadius = UDim.new(1, 0)
        bc.Parent = b

        local bs = Instance.new("UIStroke")
        bs.Thickness = 1
        bs.Transparency = 0.45
        bs.Color = Color3.fromRGB(80, 80, 100)
        bs.Parent = b

        b.MouseButton1Click:Connect(function()
            SetProfilePfp(id)
        end)
    end

    return section
end

function SetProfilePfp(id)
    id = tostring(id or ""):match("%d+") or ""
    if id == "" then return end
    SavePfpIdLocally(id)
    ProfilePfp.Image = KronosPfpImage(id, 420)
    if ProfilePfpRing and ProfilePfpRing.Parent then
        ProfilePfpRing.Color = Color3.fromRGB(255, 35, 35)
        ProfilePfpRing.Thickness = 3
        ProfilePfpRing.Transparency = 0
    end
    ShowLocalSystemMessage("[AI MoD] : Profile picture saved locally ✓")
end


BuildPfpSection(ProfilePfpCard, "HER PFP CHOICE", "🌸", GirlPfpList, 100, "HerPfpSection")
BuildPfpSection(ProfilePfpCard, "HIS PFP CHOICE", "🔥", BoyPfpList, 354, "HisPfpSection")

PfpCustomSetButton.MouseButton1Click:Connect(function()
    SetProfilePfp(PfpCustomInput.Text)
end)

PfpRandomButton.MouseButton1Click:Connect(function()


    local combined = {}
    for _, id in ipairs(GirlPfpList) do table.insert(combined, id) end
    for _, id in ipairs(BoyPfpList) do table.insert(combined, id) end
    if #combined > 0 then
        SetProfilePfp(combined[math.random(1, #combined)])
    end
end)

ProfileFrame.CanvasSize = UDim2.new(0, 0, 0, 744)



ProfilePopup = Instance.new("Frame")
ProfilePopup.Size = UDim2.new(0, 320, 0, 132)
ProfilePopup.Position = UDim2.new(0.5, -160, 0.5, -66)
ProfilePopup.BackgroundColor3 = Color3.fromRGB(14, 14, 24)
ProfilePopup.BackgroundTransparency = 0
ProfilePopup.BorderSizePixel = 0
ProfilePopup.Visible = false
ProfilePopup.Active = true
ProfilePopup.ZIndex = 200
ProfilePopup.Parent = ScreenGui
ProfilePopupCorner = Instance.new("UICorner")
ProfilePopupCorner.CornerRadius = UDim.new(0, 12)
ProfilePopupCorner.Parent = ProfilePopup
ProfilePopupStroke = Instance.new("UIStroke")
ProfilePopupStroke.Thickness = 1.5
ProfilePopupStroke.Color = Color3.fromRGB(220, 35, 45)
ProfilePopupStroke.Transparency = 0.08
ProfilePopupStroke.Parent = ProfilePopup

ProfilePopupTitle = Instance.new("TextLabel")
ProfilePopupTitle.Size = UDim2.new(1, -52, 0, 24)
ProfilePopupTitle.Position = UDim2.new(0, 12, 0, 8)
ProfilePopupTitle.BackgroundTransparency = 1
ProfilePopupTitle.Font = Enum.Font.GothamBold
ProfilePopupTitle.Text = "SOCIAL USERNAME"
ProfilePopupTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfilePopupTitle.TextSize = 13
ProfilePopupTitle.TextXAlignment = Enum.TextXAlignment.Left
ProfilePopupTitle.ZIndex = 202
ProfilePopupTitle.Parent = ProfilePopup

ProfilePopupInput = Instance.new("TextBox")
ProfilePopupInput.Size = UDim2.new(1, -24, 0, 34)
ProfilePopupInput.Position = UDim2.new(0, 12, 0, 38)
ProfilePopupInput.BackgroundColor3 = Color3.fromRGB(32, 32, 46)
ProfilePopupInput.BackgroundTransparency = 0
ProfilePopupInput.PlaceholderText = "Enter your username / handle"
ProfilePopupInput.PlaceholderColor3 = Color3.fromRGB(175, 175, 195)
ProfilePopupInput.BorderSizePixel = 0
ProfilePopupInput.Font = Enum.Font.Gotham
ProfilePopupInput.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfilePopupInput.TextSize = 11
ProfilePopupInput.ClearTextOnFocus = false
ProfilePopupInput.ZIndex = 202
ProfilePopupInput.Parent = ProfilePopup
ProfilePopupInputCorner = Instance.new("UICorner")
ProfilePopupInputCorner.CornerRadius = UDim.new(0, 8)
ProfilePopupInputCorner.Parent = ProfilePopupInput
ProfilePopupInputStroke = Instance.new("UIStroke")
ProfilePopupInputStroke.Thickness = 1
ProfilePopupInputStroke.Color = Color3.fromRGB(95, 95, 120)
ProfilePopupInputStroke.Transparency = 0.15
ProfilePopupInputStroke.Parent = ProfilePopupInput

ProfilePopupSave = MusicMakeButton(ProfilePopup, "ProfilePopupSave", "SAVE", UDim2.new(0, 12, 0, 82), UDim2.new(0.5, -18, 0, 34))
ProfilePopupCancel = MusicMakeButton(ProfilePopup, "ProfilePopupCancel", "CANCEL", UDim2.new(0.5, 6, 0, 82), UDim2.new(0.5, -18, 0, 34))
ProfilePopupSave.BackgroundColor3 = Color3.fromRGB(210, 35, 45)
ProfilePopupSave.BackgroundTransparency = 0
ProfilePopupSave.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfilePopupSave.TextSize = 11
ProfilePopupSave.ZIndex = 203
ProfilePopupCancel.BackgroundColor3 = Color3.fromRGB(62, 62, 82)
ProfilePopupCancel.BackgroundTransparency = 0
ProfilePopupCancel.TextColor3 = Color3.fromRGB(255, 255, 255)
ProfilePopupCancel.TextSize = 11
ProfilePopupCancel.ZIndex = 203
for _, popupButton in ipairs({ProfilePopupSave, ProfilePopupCancel}) do
    local stroke = popupButton:FindFirstChildOfClass("UIStroke")
    if not stroke then
        stroke = Instance.new("UIStroke")
        stroke.Parent = popupButton
    end
    stroke.Thickness = 1.2
    stroke.Transparency = 0.03
    stroke.Color = Color3.fromRGB(190, 190, 210)
end

ActiveSocialKey = nil
function OpenSocialPopup(key)
    ActiveSocialKey = key
    ProfileSharePopup.Visible = false
    ProfilePopupTitle.Text = string.upper(key) .. " USERNAME"
    ProfilePopupInput.Text = ProfileSocials[key] or ""
    ProfilePopup.Visible = true
    ProfilePopupInput:CaptureFocus()
end
InstagramButton.MouseButton1Click:Connect(function() OpenSocialPopup("Instagram") end)
DiscordButton.MouseButton1Click:Connect(function() OpenSocialPopup("Discord") end)
SnapchatButton.MouseButton1Click:Connect(function() OpenSocialPopup("Snapchat") end)
ProfilePopupCancel.MouseButton1Click:Connect(function() ProfilePopup.Visible = false end)
ProfilePopupSave.MouseButton1Click:Connect(function()
    if ActiveSocialKey then
        local value = ProfilePopupInput.Text:gsub("^@", ""):gsub("%s+", "")
        ProfileSocials[ActiveSocialKey] = value
        SaveLocalText(ActiveSocialKey, value)
    end
    ProfilePopup.Visible = false
end)



ProfileSharePopup = Instance.new("Frame")
ProfileSharePopup.Size = UDim2.new(0, 300, 0, 132)
ProfileSharePopup.Position = UDim2.new(0.5, -150, 0.5, -66)
ProfileSharePopup.BackgroundColor3 = Color3.fromRGB(14, 14, 24)
ProfileSharePopup.BackgroundTransparency = 0
ProfileSharePopup.BorderSizePixel = 0
ProfileSharePopup.Visible = false
ProfileSharePopup.Active = true
ProfileSharePopup.ZIndex = 200
ProfileSharePopup.Parent = ScreenGui
ShareCorner = Instance.new("UICorner")
ShareCorner.CornerRadius = UDim.new(0, 12)
ShareCorner.Parent = ProfileSharePopup
ShareStroke = Instance.new("UIStroke")
ShareStroke.Thickness = 1.5
ShareStroke.Color = Color3.fromRGB(220, 35, 45)
ShareStroke.Transparency = 0.08
ShareStroke.Parent = ProfileSharePopup

ShareTitle = Instance.new("TextLabel")
ShareTitle.Size = UDim2.new(1, -24, 0, 24)
ShareTitle.Position = UDim2.new(0, 12, 0, 9)
ShareTitle.BackgroundTransparency = 1
ShareTitle.Font = Enum.Font.GothamBold
ShareTitle.Text = "SHARE PROFILE TO"
ShareTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ShareTitle.TextSize = 13
ShareTitle.TextXAlignment = Enum.TextXAlignment.Left
ShareTitle.ZIndex = 202
ShareTitle.Parent = ProfileSharePopup

ShareGlobal = MusicMakeButton(ProfileSharePopup, "ShareGlobal", "GLOBAL", UDim2.new(0, 12, 0, 43), UDim2.new(0.5, -18, 0, 34))
ShareServer = MusicMakeButton(ProfileSharePopup, "ShareServer", "SERVER", UDim2.new(0.5, 6, 0, 43), UDim2.new(0.5, -18, 0, 34))
ShareCancel = MusicMakeButton(ProfileSharePopup, "ShareCancel", "CANCEL", UDim2.new(0, 12, 0, 85), UDim2.new(1, -24, 0, 34))
ShareGlobal.BackgroundColor3 = Color3.fromRGB(210, 35, 45)
ShareGlobal.BackgroundTransparency = 0
ShareServer.BackgroundColor3 = Color3.fromRGB(55, 75, 105)
ShareServer.BackgroundTransparency = 0
ShareCancel.BackgroundColor3 = Color3.fromRGB(62, 62, 82)
ShareCancel.BackgroundTransparency = 0
for _, shareButton in ipairs({ShareGlobal, ShareServer, ShareCancel}) do
    shareButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    shareButton.TextSize = 11
    shareButton.ZIndex = 203
    local stroke = shareButton:FindFirstChildOfClass("UIStroke")
    if not stroke then
        stroke = Instance.new("UIStroke")
        stroke.Parent = shareButton
    end
    stroke.Thickness = 1.2
    stroke.Transparency = 0.03
    stroke.Color = Color3.fromRGB(190, 190, 210)
end
ProfileShareButton.MouseButton1Click:Connect(function()
    ProfilePopup.Visible = false
    ProfileSharePopup.Visible = true
end)
function SendProfileShare(isGlobal)
    local instagram = ProfileSocials.Instagram
    local discord = ProfileSocials.Discord
    local snapchat = ProfileSocials.Snapchat
    if instagram == "" and discord == "" and snapchat == "" then
        ShowLocalSystemMessage("[AI MoD] : Set at least one social username before sharing.", true)
        return
    end
    local payload = {
        ClientMessageId = LocalPlayer.UserId .. "_profile_share_" .. tostring(math.floor(os.clock()*1000000)) .. "_" .. tostring(math.random(100000,999999)),
        Username = LocalPlayer.Name,
        DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        Text = "",
        Timestamp = os.time(),
        Role = GetAssignedOrCalculatedRole(LocalPlayer.Name),
        PfpId = CurrentPfpId,
        IsPrivate = false,
        IsProfileShare = true,
        Instagram = instagram,
        Discord = discord,
        Snapchat = snapchat,
    }
    local endpoint = isGlobal and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
    local cache = isGlobal and GlobalCachedMessages or ServerCachedMessages
    local ui = isGlobal and GlobalUIElements or ServerUIElements
    local localKey = "local_" .. payload.ClientMessageId
    cache[localKey] = payload
    local card = CreateMessageCard(payload, isGlobal)
    card.Visible = (ActiveTab == (isGlobal and "GLOBAL" or "SERVER"))
    table.insert(ui, card)
    task.spawn(function() HttpRequest(endpoint, "POST", payload) end)
    ProfileSharePopup.Visible = false
end
ShareGlobal.MouseButton1Click:Connect(function() SendProfileShare(true) end)
ShareServer.MouseButton1Click:Connect(function() SendProfileShare(false) end)
ShareCancel.MouseButton1Click:Connect(function()
    ProfileSharePopup.Visible = false
end)


TagScrollFrame = Instance.new("ScrollingFrame")
TagScrollFrame.Name = "TagScrollFrame"
TagScrollFrame.Size = UDim2.new(1, 0, 0, 36)
TagScrollFrame.Position = UDim2.new(0, 0, 0, 0)
TagScrollFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
TagScrollFrame.BackgroundTransparency = 0.4
TagScrollFrame.BorderSizePixel = 0
TagScrollFrame.ScrollBarThickness = 3
TagScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
TagScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
TagScrollFrame.HorizontalScrollBarInset = Enum.ScrollBarInset.ScrollBar
TagScrollFrame.Parent = AboutFrame

TagScrollCorner = Instance.new("UICorner")
TagScrollCorner.CornerRadius = UDim.new(0, 8)
TagScrollCorner.Parent = TagScrollFrame

TagListLayout = Instance.new("UIListLayout")
TagListLayout.FillDirection = Enum.FillDirection.Horizontal
TagListLayout.SortOrder = Enum.SortOrder.Name
TagListLayout.Padding = UDim.new(0, 6)
TagListLayout.Parent = TagScrollFrame

TagPadding = Instance.new("UIPadding")
TagPadding.PaddingLeft = UDim.new(0, 6)
TagPadding.PaddingRight = UDim.new(0, 6)
TagPadding.PaddingTop = UDim.new(0, 5)
TagPadding.Parent = TagScrollFrame

TagListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    TagScrollFrame.CanvasSize = UDim2.new(0, TagListLayout.AbsoluteContentSize.X + 16, 0, 0)
end)

local function PopulateTagBar()
    for _, child in ipairs(TagScrollFrame:GetChildren()) do
        if child:IsA("Frame") or child:IsA("TextButton") then child:Destroy() end
    end

    for roleName, style in pairs(RANK_STYLES) do
        if roleName ~= "BOT" then
            local tagBtn = Instance.new("TextButton")
        tagBtn.Name = roleName
        tagBtn.AutomaticSize = Enum.AutomaticSize.X
        tagBtn.Size = UDim2.new(0, 0, 0, 22)
        tagBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
        tagBtn.BackgroundTransparency = 0.2
        tagBtn.Font = Enum.Font.GothamBold
        tagBtn.Text = "  " .. string.upper(roleName) .. "  "
        tagBtn.TextColor3 = style.Start
        tagBtn.TextSize = style.TextSize or 10
        tagBtn.Parent = TagScrollFrame
        AnimateRoleFont(tagBtn, roleName, style)

        local tagCorner = Instance.new("UICorner")
        tagCorner.CornerRadius = UDim.new(0, 6)
        tagCorner.Parent = tagBtn

        local tagStroke = Instance.new("UIStroke")
        tagStroke.Thickness = 1
        tagStroke.Color = style.Start
        tagStroke.Transparency = 0.5
        tagStroke.Parent = tagBtn

        tagBtn.MouseButton1Click:Connect(function()
            if setclipboard then setclipboard(roleName) end
            if MessageInput then
                MessageInput.Text = "!tag " .. roleName
            end
        end)
        end
    end
end

PopulateTagBar()

AboutInfoScroll = Instance.new("ScrollingFrame")
AboutInfoScroll.Name = "AboutInfoScroll"
AboutInfoScroll.Size = UDim2.new(1, 0, 0, 125)
AboutInfoScroll.Position = UDim2.new(0, 0, 0, 42)
AboutInfoScroll.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
AboutInfoScroll.BackgroundTransparency = 0.4
AboutInfoScroll.BorderSizePixel = 0
AboutInfoScroll.ScrollBarThickness = 3
AboutInfoScroll.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
AboutInfoScroll.Parent = AboutFrame

AboutInfoCorner = Instance.new("UICorner")
AboutInfoCorner.CornerRadius = UDim.new(0, 8)
AboutInfoCorner.Parent = AboutInfoScroll

AboutTextLabel = Instance.new("TextLabel")
AboutTextLabel.Size = UDim2.new(1, -12, 0, 0)
AboutTextLabel.Position = UDim2.new(0, 6, 0, 6)
AboutTextLabel.AutomaticSize = Enum.AutomaticSize.Y
AboutTextLabel.BackgroundTransparency = 1
AboutTextLabel.Font = Enum.Font.Gotham
AboutTextLabel.TextSize = 11
AboutTextLabel.TextColor3 = Color3.fromRGB(220, 220, 230)
AboutTextLabel.TextXAlignment = Enum.TextXAlignment.Left
AboutTextLabel.TextYAlignment = Enum.TextYAlignment.Top
AboutTextLabel.RichText = true
AboutTextLabel.TextWrapped = true
AboutTextLabel.Text = [[<b><font color='#FF3333' size='14'>KRONOS CHAT SYSTEM</font></b>
<font color='#888899'>Version 2.5 • Developed for Cross-Server Communication</font>

<b>[ CREDITS ]</b>
• <b>Architect</b>: Kabir_Priv
• <b>Stickers</b>: <font color='#FF0000'><b>ARES</b></font> / <font color='#FFD700'><b>Mickey</b></font>

<b>[ ABOUT SYSTEM ]</b>
KRONOS CHAT is a real-time cross-server chat infrastructure allowing seamless global and local server messaging with custom rank badges, stickers, privacy options, and instant requests.

<b>[ COMMANDS GUIDE ]</b>
• <b>!tag</b> : Toggle your custom tag display ON or OFF.
• <b>!tag [RoleName]</b> : Switch to any available tag. Tier 1 tags are freely selectable; Tier 2 and Tier 3 tags require roster authorization.
• <b>!spoof [NewName]</b> : Change your chat Display Name (Architect / Overseer / Moderator / Com-God / Hacker).
• <b>!role [User]</b> : Inspect a player's assigned roles and highest role.
• <b>!announce [Text]</b> : Send an ANNOUNCER announcement (Architect / Overseer / Moderator only, max 900 chars).
• <b>!mute [1-15] [User]</b> : Mute a user in the active chat scope (Architect / Overseer / Moderator only).
• <b>/w [User] [Message]</b> : Send direct private messages.]]
AboutTextLabel.Parent = AboutInfoScroll

AboutTextLabel:GetPropertyChangedSignal("AbsoluteSize"):Connect(function()
    AboutInfoScroll.CanvasSize = UDim2.new(0, 0, 0, AboutTextLabel.AbsoluteSize.Y + 16)
end)

RequestContainer = Instance.new("Frame")
RequestContainer.Name = "RequestContainer"
RequestContainer.Size = UDim2.new(1, 0, 0, 98)
RequestContainer.Position = UDim2.new(0, 0, 0, 172)
RequestContainer.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
RequestContainer.BackgroundTransparency = 0.4
RequestContainer.BorderSizePixel = 0
RequestContainer.Parent = AboutFrame

RequestCorner = Instance.new("UICorner")
RequestCorner.CornerRadius = UDim.new(0, 8)
RequestCorner.Parent = RequestContainer

RequestHeader = Instance.new("TextLabel")
RequestHeader.Size = UDim2.new(1, -10, 0, 18)
RequestHeader.Position = UDim2.new(0, 8, 0, 4)
RequestHeader.BackgroundTransparency = 1
RequestHeader.Font = Enum.Font.GothamBold
RequestHeader.Text = "SUBMIT TAG REQUEST / SUGGESTION"
RequestHeader.TextColor3 = Color3.fromRGB(255, 60, 60)
RequestHeader.TextSize = 10
RequestHeader.TextXAlignment = Enum.TextXAlignment.Left
RequestHeader.Parent = RequestContainer

RequestInput = Instance.new("TextBox")
RequestInput.Name = "RequestInput"
RequestInput.Size = UDim2.new(1, -16, 0, 44)
RequestInput.Position = UDim2.new(0, 8, 0, 22)
RequestInput.BackgroundColor3 = Color3.fromRGB(10, 10, 18)
RequestInput.BackgroundTransparency = 0.3
RequestInput.Font = Enum.Font.Gotham
RequestInput.PlaceholderText = "Enter your custom tag request or bug report..."
RequestInput.PlaceholderColor3 = Color3.fromRGB(130, 130, 140)
RequestInput.Text = ""
RequestInput.TextColor3 = Color3.fromRGB(240, 240, 250)
RequestInput.TextSize = 11
RequestInput.TextXAlignment = Enum.TextXAlignment.Left
RequestInput.TextYAlignment = Enum.TextYAlignment.Top
RequestInput.ClearTextOnFocus = false
RequestInput.TextWrapped = true
RequestInput.Parent = RequestContainer

RequestInputCorner = Instance.new("UICorner")
RequestInputCorner.CornerRadius = UDim.new(0, 6)
RequestInputCorner.Parent = RequestInput

SubmitRequestBtn = Instance.new("TextButton")
SubmitRequestBtn.Name = "SubmitRequestBtn"
SubmitRequestBtn.Size = UDim2.new(0, 110, 0, 22)
SubmitRequestBtn.Position = UDim2.new(1, -118, 0, 70)
SubmitRequestBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
SubmitRequestBtn.BackgroundTransparency = 0.2
SubmitRequestBtn.Font = Enum.Font.GothamBold
SubmitRequestBtn.Text = "SUBMIT"
SubmitRequestBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitRequestBtn.TextSize = 10
SubmitRequestBtn.Parent = RequestContainer

SubmitCorner = Instance.new("UICorner")
SubmitCorner.CornerRadius = UDim.new(0, 6)
SubmitCorner.Parent = SubmitRequestBtn

SubmitRequestBtn.MouseButton1Click:Connect(function()
    local text = RequestInput.Text
    if text == "" or text:match("^%s*$") then return end

    SubmitRequestBtn.Text = "SENDING..."
    SubmitRequestBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)

    local payload = {
        Username = LocalPlayer.Name,
        DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        UserId = LocalPlayer.UserId,
        RequestText = text,
        Timestamp = os.time()
    }

    task.spawn(function()
        local res = HttpRequest(REQUEST_ENDPOINT, "POST", payload)
        if res then
            SubmitRequestBtn.Text = "SENT ✓"
            SubmitRequestBtn.BackgroundColor3 = Color3.fromRGB(30, 180, 80)
            RequestInput.Text = ""
        else
            SubmitRequestBtn.Text = "FAILED ✗"
            SubmitRequestBtn.BackgroundColor3 = Color3.fromRGB(180, 30, 30)
        end

        task.wait(2)
        SubmitRequestBtn.Text = "SUBMIT"
        SubmitRequestBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
    end)
end)

-- Input Setup
InputBarFrame = Instance.new("Frame")
InputBarFrame.Name = "InputBarFrame"
InputBarFrame.Size = UDim2.new(1, -16, 0, 38)
InputBarFrame.Position = UDim2.new(0, 8, 1, -44)
InputBarFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
InputBarFrame.BackgroundTransparency = 0.4
InputBarFrame.BorderSizePixel = 0
InputBarFrame.Parent = MainFrame

InputCorner = Instance.new("UICorner")
InputCorner.CornerRadius = UDim.new(0, 12)
InputCorner.Parent = InputBarFrame

MessageInput = Instance.new("TextBox")
MessageInput.Name = "MessageInput"
MessageInput.Size = UDim2.new(1, -132, 1, -8)
MessageInput.Position = UDim2.new(0, 10, 0, 4)
MessageInput.BackgroundTransparency = 1
MessageInput.Font = Enum.Font.GothamMedium
MessageInput.PlaceholderText = "Type !tag or !tag [TagName] / Longpress a Message"
MessageInput.PlaceholderColor3 = Color3.fromRGB(150, 150, 160)
MessageInput.Text = ""
MessageInput.TextColor3 = Color3.fromRGB(240, 240, 245)
MessageInput.TextSize = 13
MessageInput.TextXAlignment = Enum.TextXAlignment.Left
MessageInput.ClearTextOnFocus = false
MessageInput.Parent = InputBarFrame

StickerToggleButton = Instance.new("TextButton")
StickerToggleButton.Name = "StickerToggleButton"
StickerToggleButton.Size = UDim2.new(0, 34, 1, -8)
StickerToggleButton.Position = UDim2.new(1, -80, 0, 4)
StickerToggleButton.BackgroundColor3 = Color3.fromRGB(220, 30, 30)
StickerToggleButton.BackgroundTransparency = 0.2
StickerToggleButton.Font = Enum.Font.GothamBold
StickerToggleButton.Text = "😁"
StickerToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StickerToggleButton.TextSize = 14
StickerToggleButton.Parent = InputBarFrame

StickerToggleCorner = Instance.new("UICorner")
StickerToggleCorner.CornerRadius = UDim.new(0, 8)
StickerToggleCorner.Parent = StickerToggleButton

local emojiList = {"😁","😀","😉","🥰","🤩","🤗","😎","😥","😣","🤔","😫","🤤","😭","😨","😮‍💨","🤑"}
task.spawn(function()
    local idx = 1
    while true do
        task.wait(1)
        idx = (idx % #emojiList) + 1
        StickerToggleButton.Text = emojiList[idx]
    end
end)

StickerToggleButton.MouseButton1Click:Connect(function()
    StickerPanel.Visible = not StickerPanel.Visible
end)

-- Global-chat-only server invite button. It is hidden whenever the
-- active tab is not GLOBAL and sends the current PlaceId + JobId as a
-- structured global message so recipients can join this exact server.
ServerInviteButton = Instance.new("TextButton")
ServerInviteButton.Name = "ServerInviteButton"
ServerInviteButton.Size = UDim2.new(0, 34, 1, -8)
ServerInviteButton.Position = UDim2.new(1, -118, 0, 4)
ServerInviteButton.BackgroundColor3 = Color3.fromRGB(220, 30, 30)
ServerInviteButton.BackgroundTransparency = 0.2
ServerInviteButton.Font = Enum.Font.GothamBold
ServerInviteButton.Text = "↗️"
ServerInviteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ServerInviteButton.TextSize = 15
ServerInviteButton.AutoButtonColor = true
ServerInviteButton.ZIndex = 12
ServerInviteButton.Parent = InputBarFrame

ServerInviteCorner = Instance.new("UICorner")
ServerInviteCorner.CornerRadius = UDim.new(0, 8)
ServerInviteCorner.Parent = ServerInviteButton

local function SendServerInvite()
    if ActiveTab ~= "GLOBAL" then return end

    local clientMessageId = LocalPlayer.UserId .. "_invite_" ..
        tostring(math.floor(os.clock() * 1000000)) .. "_" ..
        tostring(math.random(100000, 999999))

    local payload = {
        ClientMessageId = clientMessageId,
        Username = LocalPlayer.Name,
        DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        Text = "invited you to join their current server",
        Timestamp = os.time(),
        Role = GetAssignedOrCalculatedRole(LocalPlayer.Name),
        PfpId = CurrentPfpId,
        IsPrivate = false,
        IsServerInvite = true,
        PlaceId = game.PlaceId,
        JobId = SERVER_JOB_ID,
    }

    local localKey = "local_" .. clientMessageId
    GlobalCachedMessages[localKey] = payload

    local immediateCard = CreateMessageCard(payload, true)
    immediateCard.Visible = true
    table.insert(GlobalUIElements, immediateCard)

    task.spawn(function()
        pcall(function()
            HttpRequest(GLOBAL_MESSAGES_ENDPOINT, "POST", payload)
        end)
    end)
end

ServerInviteButton.MouseButton1Click:Connect(SendServerInvite)

PrivateModePill = Instance.new("TextButton")
PrivateModePill.Name = "PrivateModePill"
PrivateModePill.Size = UDim2.new(0, 150, 0, 22)
PrivateModePill.Position = UDim2.new(0, 8, 0, -25)
PrivateModePill.BackgroundColor3 = Color3.fromRGB(150, 25, 35)
PrivateModePill.BackgroundTransparency = 0.12
PrivateModePill.Font = Enum.Font.GothamBold
PrivateModePill.Text = "PVT"
PrivateModePill.TextColor3 = Color3.fromRGB(255, 255, 255)
PrivateModePill.TextSize = 9
PrivateModePill.Visible = false
PrivateModePill.ZIndex = 31
PrivateModePill.Parent = InputBarFrame
privatePillCorner = Instance.new("UICorner")
privatePillCorner.CornerRadius = UDim.new(0, 8)
privatePillCorner.Parent = PrivateModePill
local function DisablePrivateModeFromPill()
    -- Same explicit cancel model as ReplyStatusLabel.
    ClearPersistentPrivatePrefix()
    if MessageInput then
        MessageInput:CaptureFocus()
    end
end

PrivateModePill.Active = true
PrivateModePill.Selectable = false
PrivateModePill.AutoButtonColor = false

-- Primary activation, matching the Reply control.
PrivateModePill.Activated:Connect(DisablePrivateModeFromPill)

-- Input fallback for mouse/touch clients.
PrivateModePill.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
        DisablePrivateModeFromPill()
    end
end)

ReplyStatusLabel = Instance.new("TextButton")
ReplyStatusLabel.Name = "ReplyStatusLabel"
ReplyStatusLabel.Size = UDim2.new(0, 250, 0, 22)
ReplyStatusLabel.Position = UDim2.new(0, 165, 0, -25)
ReplyStatusLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
ReplyStatusLabel.BackgroundTransparency = 0.12
ReplyStatusLabel.Font = Enum.Font.Gotham
ReplyStatusLabel.TextColor3 = Color3.fromRGB(230, 230, 240)
ReplyStatusLabel.TextSize = 9
ReplyStatusLabel.TextXAlignment = Enum.TextXAlignment.Left
ReplyStatusLabel.Text = ""
ReplyStatusLabel.Visible = false
ReplyStatusLabel.ZIndex = 31
ReplyStatusLabel.Parent = InputBarFrame
replyStatusCorner = Instance.new("UICorner")
replyStatusCorner.CornerRadius = UDim.new(0, 8)
replyStatusCorner.Parent = ReplyStatusLabel
RefreshReplyUI = function()
    if ReplyContextMessage then
        ReplyStatusLabel.Text = "  ↩ Replying to " .. TruncateText(ReplyContextMessage.DisplayName or ReplyContextMessage.Username or "message", 22) .. "  ×"
        ReplyStatusLabel.Visible = true
    else
        ReplyStatusLabel.Visible = false
        ReplyStatusLabel.Text = ""
    end
end
ReplyStatusLabel.MouseButton1Click:Connect(function()
    ReplyContextMessage = nil
    RefreshReplyUI()
    MessageInput:CaptureFocus()
end)

SendButton = Instance.new("TextButton")
SendButton.Name = "SendButton"
SendButton.Size = UDim2.new(0, 38, 1, -8)
SendButton.Position = UDim2.new(1, -42, 0, 4)
SendButton.BackgroundColor3 = Color3.fromRGB(220, 30, 30)
SendButton.BackgroundTransparency = 0.2
SendButton.Font = Enum.Font.GothamBold
SendButton.Text = ">"
SendButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SendButton.TextSize = 14
SendButton.Parent = InputBarFrame

SendCorner = Instance.new("UICorner")
SendCorner.CornerRadius = UDim.new(0, 8)
SendCorner.Parent = SendButton

AutoCompleteFrame = Instance.new("ScrollingFrame")
AutoCompleteFrame.Name = "AutoCompleteFrame"
AutoCompleteFrame.Size = UDim2.new(0, 235, 0, 132)
AutoCompleteFrame.Position = UDim2.new(0, 8, 1, -182)
AutoCompleteFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
AutoCompleteFrame.BackgroundTransparency = 0.2
AutoCompleteFrame.BorderSizePixel = 0
AutoCompleteFrame.ScrollBarThickness = 4
AutoCompleteFrame.Active = true
AutoCompleteFrame.Selectable = true
AutoCompleteFrame.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
AutoCompleteFrame.Visible = false
AutoCompleteFrame.ZIndex = 30
AutoCompleteFrame.Parent = MainFrame

AutoCompleteCorner = Instance.new("UICorner")
AutoCompleteCorner.CornerRadius = UDim.new(0, 8)
AutoCompleteCorner.Parent = AutoCompleteFrame

AutoCompleteLayout = Instance.new("UIListLayout")
AutoCompleteLayout.SortOrder = Enum.SortOrder.LayoutOrder
AutoCompleteLayout.Padding = UDim.new(0, 2)
AutoCompleteLayout.Parent = AutoCompleteFrame

local function HideCommandAutocomplete()
    for _, child in ipairs(AutoCompleteFrame:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    AutoCompleteFrame.Visible = false
end

local function ShowCommandAutocomplete()
    if not MessageInput then return end

    HideCommandAutocomplete()

    local typed = MessageInput.Text or ""
    local lower = string.lower(typed)
    local command = nil
    local query = ""

    if lower == "!tag" or lower:sub(1, 5) == "!tag " then
        command = "!tag "
        query = typed:sub(6)
    elseif lower == "!spoof" or lower:sub(1, 7) == "!spoof " then
        command = "!spoof "
        query = typed:sub(8)
    elseif lower == "!mute" or lower:sub(1, 6) == "!mute " then
        command = "!mute "
        query = typed:sub(7)
    elseif lower == "!announce" then
        command = "!announce "
        query = ""
    else
        return
    end

    local suggestions = {}
    local seen = {}

    if command == "!announce " then
        -- Show !announce in autocomplete only to roles that can actually use it.
        if not CanUseAnnouncement(LocalPlayer.Name) then
            return
        end
        table.insert(suggestions, "!announce ")
    elseif command == "!tag " then
        for roleName, style in pairs(RANK_STYLES) do
            if roleName ~= "BOT" and type(style) == "table" and not seen[roleName] then
                local tier = tonumber(style.Tier)
                if tier == 1 or CanSwitchToRole(roleName) then
                    table.insert(suggestions, roleName)
                    seen[roleName] = true
                end
            end
        end
    else
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer then
                table.insert(suggestions, player.DisplayName)
            end
        end
    end

    table.sort(suggestions, function(a, b)
        return string.lower(a) < string.lower(b)
    end)

    local q = string.lower(query)
    local shown = 0

    for _, suggestion in ipairs(suggestions) do
        if q == "" or string.sub(string.lower(suggestion), 1, #q) == q then
            local button = Instance.new("TextButton")
            button.Size = UDim2.new(1, -8, 0, 24)
            button.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
            button.BackgroundTransparency = 0.15
            button.BorderSizePixel = 0
            button.Font = Enum.Font.GothamMedium
            button.Text = "  " .. suggestion
            button.TextColor3 = Color3.fromRGB(235, 235, 240)
            button.TextSize = 10
            button.TextXAlignment = Enum.TextXAlignment.Left
            button.ZIndex = 31
            button.Parent = AutoCompleteFrame

            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 6)
            corner.Parent = button

            local stroke = Instance.new("UIStroke")
            stroke.Thickness = 1
            stroke.Color = Color3.fromRGB(255, 0, 0)
            stroke.Transparency = 0.65
            stroke.Parent = button

            button.MouseEnter:Connect(function()
                button.BackgroundColor3 = Color3.fromRGB(55, 25, 35)
            end)
            button.MouseLeave:Connect(function()
                button.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
            end)
            button.MouseButton1Click:Connect(function()
                local selectedCommand = (command == "!announce ") and command or (command .. suggestion)
                HideCommandAutocomplete()

                -- Setting the textbox after removing the old suggestions avoids
                -- TextChanged rebuilding the list during the click event.
                MessageInput.Text = selectedCommand
                MessageInput.CursorPosition = #selectedCommand + 1
                MessageInput:CaptureFocus()
            end)

            button.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.Touch then
                    local selectedCommand = (command == "!announce ") and command or (command .. suggestion)
                    HideCommandAutocomplete()
                    MessageInput.Text = selectedCommand
                    MessageInput.CursorPosition = #selectedCommand + 1
                    MessageInput:CaptureFocus()
                end
            end)

            shown = shown + 1
            if shown >= 6 then break end
        end
    end

    AutoCompleteFrame.Visible = shown > 0
end

AutoCompleteLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    AutoCompleteFrame.CanvasSize = UDim2.new(0, 0, 0, AutoCompleteLayout.AbsoluteContentSize.Y + 6)
end)

ContextMenu = Instance.new("Frame")
ContextMenu.Name = "ContextMenu"
ContextMenu.Size = UDim2.new(0, 170, 0, 200)
ContextMenu.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
ContextMenu.BackgroundTransparency = 0.2
ContextMenu.BorderSizePixel = 0
ContextMenu.Visible = false
ContextMenu.ZIndex = 20
ContextMenu.Parent = ScreenGui

ContextCorner = Instance.new("UICorner")
ContextCorner.CornerRadius = UDim.new(0, 10)
ContextCorner.Parent = ContextMenu

ContextList = Instance.new("UIListLayout")
ContextList.SortOrder = Enum.SortOrder.LayoutOrder
ContextList.Parent = ContextMenu

function HideMessageContextMenu()
    ContextMenu.Visible = false
    CurrentContextMessage = nil
end

function ResolvePrivateRecipient(prefix)
    prefix = tostring(prefix or "")
    if prefix == "" then return nil end
    local needle = string.lower(string.sub(prefix, 1, 8))

    -- Prefer players currently present in this server.
    for _, player in ipairs(Players:GetPlayers()) do
        local displayName = player.DisplayName or player.Name or ""
        if string.lower(string.sub(displayName, 1, 8)) == needle then
            return player.Name, displayName
        end
    end

    -- Fall back to people whose messages are already known to this client.
    local caches = { GlobalCachedMessages, ServerCachedMessages }
    for _, cache in ipairs(caches) do
        for _, msg in pairs(cache) do
            if type(msg) == "table" then
                local displayName = msg.DisplayName or msg.Username or ""
                if string.lower(string.sub(displayName, 1, 8)) == needle then
                    return msg.Username, displayName
                end
            end
        end
    end

    return nil
end

function IsPrivateModeActive()
    return PrivateModeTargetUsername ~= nil and PrivateModeTargetDisplayName ~= nil
end

function RefreshPrivateModeUI()
    if not PrivateModePill then return end
    local active = IsPrivateModeActive()
    PrivateModePill.Visible = active
    PrivateModePill.Text = active
        and ("PVT • " .. TruncateText(PrivateModeTargetDisplayName or "Private", 16) .. "  ×")
        or ""
    PrivateModePill.ZIndex = 100
end

-- New PVT mechanism:
-- The /pvt command is NOT stored inside the TextBox anymore.
-- PVT state lives entirely in these two variables. The pill is the explicit
-- mode indicator/control and the textbox remains a normal editable textbox.
function SetPersistentPrivatePrefix(displayName, username)
    local safeName = tostring(displayName or username or "Unknown")
    PrivateModeTargetUsername = username
    PrivateModeTargetDisplayName = safeName
    PersistentPvtPrefix = "/pvt @" .. string.sub(safeName, 1, 8) .. " "
    if MessageInput then
        MessageInput.Text = ""
        MessageInput.CursorPosition = 1
        MessageInput:CaptureFocus()
    end
    RefreshPrivateModeUI()
end

ClearPersistentPrivatePrefix = function()
    -- Explicit PVT cancel, independent of textbox focus/content.
    PersistentPvtPrefix = ""
    PrivateModeTargetUsername = nil
    PrivateModeTargetDisplayName = nil
    if MessageInput then
        MessageInput.Text = ""
        MessageInput.CursorPosition = 1
    end
    RefreshPrivateModeUI()
end

function ShowMessageContextMenu(msgData, position)
    CurrentContextMessage = msgData
    -- Rebuild on every long-press so self-message-only actions are decided
    -- from the message currently being viewed.
    pcall(BuildMessageContextMenu)
    ContextMenu.Visible = true
    ContextMenu.Position = UDim2.fromOffset(position.X, position.Y)

    -- Keep the menu on-screen.
    task.defer(function()
        if not ContextMenu.Visible then return end
        local viewport = workspace.CurrentCamera and workspace.CurrentCamera.ViewportSize or Vector2.new(800, 600)
        local size = ContextMenu.AbsoluteSize
        local x = math.clamp(position.X, 4, math.max(4, viewport.X - size.X - 4))
        local y = math.clamp(position.Y, 4, math.max(4, viewport.Y - size.Y - 4))
        ContextMenu.Position = UDim2.fromOffset(x, y)
    end)
end

function AddContextMenuButton(text, order, callback)
    local button = Instance.new("TextButton")
    button.Name = text:gsub("%s+", "") .. "Button"
    button.Size = UDim2.new(1, 0, 0, 40)
    button.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    button.BackgroundTransparency = 0.15
    button.BorderSizePixel = 0
    button.Font = Enum.Font.GothamMedium
    button.Text = text
    button.TextColor3 = Color3.fromRGB(240, 240, 245)
    button.TextSize = 12
    button.LayoutOrder = order
    button.ZIndex = 21
    button.Parent = ContextMenu

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 7)
    corner.Parent = button

    button.MouseButton1Click:Connect(function()
        callback()
        HideMessageContextMenu()
    end)
    return button
end

function BuildMessageContextMenu()
    for _, child in ipairs(ContextMenu:GetChildren()) do
        if child:IsA("TextButton") then child:Destroy() end
    end

    -- The menu must only be as tall as the options that actually exist.
    -- Delete/Block are conditionally added below, so a fixed 200px height
    -- creates an empty area whenever one or both options are unavailable.
    ContextMenu.Size = UDim2.new(0, 170, 0, 0)

    AddContextMenuButton("Copy Message", 1, function()
        if CurrentContextMessage and setclipboard then
            setclipboard(tostring(CurrentContextMessage.Text or ""))
        end
    end)

    AddContextMenuButton("Reply", 2, function()
        if not CurrentContextMessage then return end
        ReplyContextMessage = CurrentContextMessage
        RefreshReplyUI()
        MessageInput:CaptureFocus()
    end)

    AddContextMenuButton("Pvt Message", 3, function()
        if not CurrentContextMessage then return end
        local msg = CurrentContextMessage
        local displayName = msg.DisplayName or msg.Username or "Unknown"
        SetPersistentPrivatePrefix(displayName, msg.Username)
    end)

    -- Block is intentionally temporary and client-side only. It is never
    -- written to Firebase or local storage, so rejoining clears the block.
    if CurrentContextMessage
        and tostring(CurrentContextMessage.Username or "") ~= ""
        and tostring(CurrentContextMessage.Username or "") ~= tostring(LocalPlayer.Name or "")
        and not CurrentContextMessage.SystemMessage then

        AddContextMenuButton("Block", 4, function()
            if not CurrentContextMessage then return end
            local targetUsername = tostring(CurrentContextMessage.Username or "")
            local targetDisplayName = tostring(CurrentContextMessage.DisplayName or targetUsername)
            if BlockMessageUser(targetUsername) then
                ShowLocalSystemMessage("[KRONOS] : Blocked " .. targetDisplayName .. ". Their messages are hidden for this session.")
            end
        end)
    end

    -- Edit/Delete are deliberately available only for the local player's
    
    if CurrentContextMessage
        and tostring(CurrentContextMessage.Username or "") == tostring(LocalPlayer.Name)
        and not CurrentContextMessage.SystemMessage
        and not CurrentContextMessage.IsMusicSessionInvite
        and not CurrentContextMessage.IsServerInvite
        and not CurrentContextMessage.IsProfileShare
        and not CurrentContextMessage.IsSticker
        and not CurrentContextMessage.Deleted then

        AddContextMenuButton("Delete Message", 5, function()
            if not CurrentContextMessage then return end
            local target = CurrentContextMessage
            local endpoint = (ActiveTab == "GLOBAL") and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
            local clientId = tostring(target.ClientMessageId or "")
            if clientId == "" then
                ShowLocalSystemMessage("[AI MoD] : This message cannot be deleted.", true)
                return
            end




            local deletedAt = os.time()
            PendingMessageMutations[clientId] = {
                Deleted = true,
                DeletedAt = deletedAt,
                DeletedBy = LocalPlayer.Name,
                MutationType = "DELETE",
            }

            local cache = (ActiveTab == "GLOBAL") and GlobalCachedMessages or ServerCachedMessages
            for key, value in pairs(cache) do
                if type(value) == "table" and tostring(value.ClientMessageId or "") == tostring(target.ClientMessageId or "") then
                    cache[key] = nil
                end
            end
            for _, card in ipairs((ActiveTab == "GLOBAL") and GlobalUIElements or ServerUIElements) do
                if card and card.Parent and tostring(card:GetAttribute("ClientMessageId") or "") == tostring(target.ClientMessageId or "") then
                    card:Destroy()
                end
            end




            
            task.spawn(function()
                local confirmed = false
                for attempt = 1, 4 do
                    local keys = FindAllFirebaseMessageKeys(endpoint, clientId)
                    if #keys == 0 then
                        confirmed = true
                        break
                    end
                    for _, firebaseKey in ipairs(keys) do
                        pcall(function()
                            HttpRequest(endpoint .. "/" .. tostring(firebaseKey) .. ".json", "DELETE")
                        end)
                    end
                    task.wait(0.35)
                    local remaining = FindAllFirebaseMessageKeys(endpoint, clientId)
                    if #remaining == 0 then
                        confirmed = true
                        break
                    end
                end
                if confirmed then
                    PendingMessageMutations[clientId] = nil
                else
                    ShowLocalSystemMessage("[AI MoD] : Firebase did not confirm deletion. The message is being kept hidden locally while the delete is retried.", true)
                end
            end)

            EditingContextMessage = nil
            EditingContextIsGlobal = false
            ShowLocalSystemMessage("[AI MoD] : Message deleted.")
        end)
    end




    local optionCount = 0
    for _, child in ipairs(ContextMenu:GetChildren()) do
        if child:IsA("TextButton") then
            optionCount = optionCount + 1
        end
    end
    ContextMenu.Size = UDim2.new(0, 170, 0, optionCount * 40)
end

BuildMessageContextMenu()

ContextMenu.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        
    end
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not ContextMenu.Visible or gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        local p = input.Position
        local pos = ContextMenu.AbsolutePosition
        local size = ContextMenu.AbsoluteSize
        if p.X < pos.X or p.X > pos.X + size.X or p.Y < pos.Y or p.Y > pos.Y + size.Y then
            HideMessageContextMenu()
        end
    end
end)


function MakeDraggable(frame, dragHandle)
    local dragging, dragInput, dragStart, startPos
    dragHandle = dragHandle or frame

    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)

    dragHandle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

MakeDraggable(MainFrame, HeaderBar)
MakeDraggable(ToggleButton, ToggleButton)




function SwitchTab(newTab)
    ActiveTab = newTab
    MessageContainer.Visible = (newTab == "GLOBAL" or newTab == "SERVER")
    InputBarFrame.Visible = (newTab == "GLOBAL" or newTab == "SERVER")
    AboutFrame.Visible = (newTab == "ABOUT")
    MusicSessionFrame.Visible = (newTab == "MUSIC")
    ProfileFrame.Visible = (newTab == "PROFILE")
    StickerPanel.Visible = false
    ServerInviteButton.Visible = (newTab == "GLOBAL")
    ProfilePopup.Visible = false
    ProfileSharePopup.Visible = false

    GlobalTabBtn.BackgroundColor3 = (newTab == "GLOBAL") and Color3.fromRGB(200, 30, 30) or Color3.fromRGB(40, 40, 55)
    ServerTabBtn.BackgroundColor3 = (newTab == "SERVER") and Color3.fromRGB(200, 30, 30) or Color3.fromRGB(40, 40, 55)
    AboutTabBtn.BackgroundColor3 = (newTab == "ABOUT") and Color3.fromRGB(200, 30, 30) or Color3.fromRGB(40, 40, 55)
    MusicTabBtn.BackgroundColor3 = (newTab == "MUSIC") and Color3.fromRGB(200, 30, 30) or Color3.fromRGB(40, 40, 55)
    ProfileTabBtn.BackgroundColor3 = (newTab == "PROFILE") and Color3.fromRGB(200, 30, 30) or Color3.fromRGB(40, 40, 55)

    if newTab == "GLOBAL" then
        for _, elem in pairs(ServerUIElements) do elem.Visible = false end
        for _, elem in pairs(GlobalUIElements) do elem.Visible = true end
    elseif newTab == "SERVER" then
        for _, elem in pairs(GlobalUIElements) do elem.Visible = false end
        for _, elem in pairs(ServerUIElements) do elem.Visible = true end
    end
end

GlobalTabBtn.MouseButton1Click:Connect(function() SwitchTab("GLOBAL") end)
ServerTabBtn.MouseButton1Click:Connect(function() SwitchTab("SERVER") end)
AboutTabBtn.MouseButton1Click:Connect(function() SwitchTab("ABOUT") end)
MusicTabBtn.MouseButton1Click:Connect(function() SwitchTab("MUSIC") end)
ProfileTabBtn.MouseButton1Click:Connect(function() SwitchTab("PROFILE") end)


ServerInviteButton.Visible = true




local function AnimateRainbowText(textObject)
    if not textObject then return end
    task.spawn(function()
        local hue = 0
        while textObject and textObject.Parent do
            hue = (hue + 0.008) % 1
            textObject.TextColor3 = Color3.fromHSV(hue, 0.9, 1)
            task.wait(0.03)
        end
    end)
end

CreateMessageCard = function(msgData, isGlobal)
    local frame = Instance.new("Frame")
    frame.Name = "MessageCard"
    frame.Size = UDim2.new(1, 0, 0, 0)
    frame.AutomaticSize = Enum.AutomaticSize.Y
    frame.BackgroundColor3 = msgData.IsPrivate and Color3.fromRGB(120, 20, 28) or Color3.fromRGB(15, 15, 22)
    frame.BackgroundTransparency = 0.4
    frame.BorderSizePixel = 0

    local cardCorner = Instance.new("UICorner")
    cardCorner.CornerRadius = UDim.new(0, 8)
    cardCorner.Parent = frame

    local msgPfpId = msgData.PfpId or DefaultPfpId
    local msgPfpImg = Instance.new("ImageLabel")
    msgPfpImg.Name = "MsgPfp"
    msgPfpImg.Size = UDim2.new(0, 26, 0, 26)
    msgPfpImg.Position = UDim2.new(0, 6, 0, 6)
    msgPfpImg.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    msgPfpImg.Image = "rbxthumb://type=Asset&id=" .. msgPfpId .. "&w=150&h=150"
    msgPfpImg.Parent = frame

    local msgPfpCorner = Instance.new("UICorner")
    msgPfpCorner.CornerRadius = UDim.new(1, 0)
    msgPfpCorner.Parent = msgPfpImg


    local roleName = msgData.Role or GetAssignedOrCalculatedRole(msgData.Username)
    local style = RANK_STYLES[roleName] or RANK_STYLES["VIP"]


    local privatePrefixLabel = nil
    if msgData.IsPrivate then
        privatePrefixLabel = Instance.new("TextLabel")
        privatePrefixLabel.Name = "PrivateWhisperPrefix"
        privatePrefixLabel.AutomaticSize = Enum.AutomaticSize.X
        privatePrefixLabel.Size = UDim2.new(0, 0, 0, 18)
        privatePrefixLabel.Position = UDim2.new(0, 38, 0, 5)
        privatePrefixLabel.BackgroundTransparency = 1
        privatePrefixLabel.Font = Enum.Font.GothamBold
        privatePrefixLabel.Text = "Pvt Whisper from -> "
        privatePrefixLabel.TextColor3 = Color3.fromRGB(255, 105, 180)
        privatePrefixLabel.TextSize = 9
        privatePrefixLabel.Parent = frame
    end

    local tagLabel = Instance.new("TextLabel")
    tagLabel.Name = "RoleTag"
    tagLabel.AutomaticSize = Enum.AutomaticSize.X
    tagLabel.Size = UDim2.new(0, 0, 0, 18)
    tagLabel.Position = UDim2.new(0, 38, 0, 5)
    tagLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    tagLabel.BackgroundTransparency = 0.3
    tagLabel.Font = Enum.Font.GothamBold
    tagLabel.Text = "  " .. string.upper(roleName) .. "  "
    tagLabel.TextColor3 = style.Start
    tagLabel.TextSize = style.TextSize or 9
    tagLabel.Visible = not DisabledTags[msgData.Username]
    tagLabel.Parent = frame

    local tagCorner = Instance.new("UICorner")
    tagCorner.CornerRadius = UDim.new(0, 4)
    tagCorner.Parent = tagLabel

    AnimateRoleFont(tagLabel, roleName, style)

    local isClient = (msgData.Username == LocalPlayer.Name)
    local formattedName = GetFormattedDisplayName(msgData.DisplayName or msgData.Username, isClient)

    local nameLabel = Instance.new("TextLabel")
    nameLabel.Name = "NameLabel"
    nameLabel.AutomaticSize = Enum.AutomaticSize.X
    nameLabel.Size = UDim2.new(0, 0, 0, 18)
    nameLabel.Position = UDim2.new(0, 38, 0, 5)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Text = formattedName .. ":"
    nameLabel.TextColor3 = style.Name
    nameLabel.TextSize = 11
    nameLabel.Parent = frame

    local function UpdatePrivateHeaderLayout()
        local prefixWidth = privatePrefixLabel and privatePrefixLabel.AbsoluteSize.X or 0
        local startX = privatePrefixLabel and (38 + prefixWidth + 4) or 38

        if privatePrefixLabel then
            privatePrefixLabel.Position = UDim2.new(0, 38, 0, 5)
        end

        if tagLabel.Visible then
            tagLabel.Position = UDim2.new(0, startX, 0, 5)
            nameLabel.Position = UDim2.new(0, startX + tagLabel.AbsoluteSize.X + 6, 0, 5)
        else
            tagLabel.Position = UDim2.new(0, startX, 0, 5)
            nameLabel.Position = UDim2.new(0, startX, 0, 5)
        end
    end

    tagLabel:GetPropertyChangedSignal("AbsoluteSize"):Connect(UpdatePrivateHeaderLayout)
    if privatePrefixLabel then
        privatePrefixLabel:GetPropertyChangedSignal("AbsoluteSize"):Connect(UpdatePrivateHeaderLayout)
    end
    UpdatePrivateHeaderLayout()

    if msgData.ReplyToText and msgData.ReplyToText ~= "" then
        local replyBox = Instance.new("Frame")
        replyBox.Name = "ReplyPreview"
        replyBox.Size = UDim2.new(1, -48, 0, 30)
        replyBox.Position = UDim2.new(0, 38, 0, 24)
        replyBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        replyBox.BackgroundTransparency = 0.88
        replyBox.BorderSizePixel = 0
        replyBox.Parent = frame
        local replyCorner = Instance.new("UICorner")
        replyCorner.CornerRadius = UDim.new(0, 6)
        replyCorner.Parent = replyBox
        local replyLabel = Instance.new("TextLabel")
        replyLabel.Size = UDim2.new(1, -10, 1, -4)
        replyLabel.Position = UDim2.new(0, 5, 0, 2)
        replyLabel.BackgroundTransparency = 1
        replyLabel.Font = Enum.Font.Gotham
        local replyTargetText = tostring(msgData.ReplyToText or "")
        local replyIsSticker = string.sub(replyTargetText, 1, 11) == "rbxthumb://" or string.sub(replyTargetText, 1, 13) == "rbxassetid://"
        local replyDisplayText = replyIsSticker and "Sticker⬜" or TruncateText(replyTargetText, 80)
        replyLabel.Text = "↩ " .. tostring(msgData.ReplyToDisplayName or "Reply") .. ": " .. replyDisplayText
        replyLabel.TextColor3 = Color3.fromRGB(210, 210, 220)
        replyLabel.TextSize = 9
        replyLabel.TextXAlignment = Enum.TextXAlignment.Left
        replyLabel.TextYAlignment = Enum.TextYAlignment.Center
        replyLabel.TextWrapped = true
        replyLabel.Parent = replyBox
    end

    local isSticker = string.sub(msgData.Text, 1, 11) == "rbxthumb://" or string.sub(msgData.Text, 1, 13) == "rbxassetid://"

    if msgData.IsMusicSessionInvite and msgData.MusicSessionId then
        local inviteButton = Instance.new("TextButton")
        inviteButton.Name = "MusicSessionInviteButton"
        inviteButton.Size = UDim2.new(1, -48, 0, 34)
        inviteButton.Position = UDim2.new(0, 38, 0, msgData.ReplyToText and 58 or 24)
        inviteButton.BackgroundColor3 = Color3.fromRGB(170, 40, 180)
        inviteButton.BackgroundTransparency = 0.12
        inviteButton.BorderSizePixel = 0
        inviteButton.Font = Enum.Font.GothamBold
        inviteButton.Text = "  🎵 JOIN MUSIC SESSION  "
        inviteButton.TextColor3 = Color3.fromRGB(255,255,255)
        inviteButton.TextSize = 10
        inviteButton.ZIndex = 12
        inviteButton.Parent = frame
        local c = Instance.new("UICorner")
        c.CornerRadius = UDim.new(0,7)
        c.Parent = inviteButton
        local info = Instance.new("TextLabel")
        info.Size = UDim2.new(1,-48,0,28)
        info.Position = UDim2.new(0,38,0,msgData.ReplyToText and 94 or 60)
        info.BackgroundTransparency = 1
        info.Font = Enum.Font.Gotham
        info.Text = tostring(msgData.Text or "Join this Music Session") .. "\nSession ID: " .. tostring(msgData.MusicSessionId) .. "\nHost: " .. tostring(msgData.MusicSessionHostDisplayName or "Unknown")
        info.TextColor3 = Color3.fromRGB(215,215,225)
        info.TextSize = 8
        info.TextXAlignment = Enum.TextXAlignment.Left
        info.TextYAlignment = Enum.TextYAlignment.Center
        info.TextWrapped = true
        info.ZIndex = 12
        info.Parent = frame
        inviteButton.MouseButton1Click:Connect(function()
            JoinMusicSessionById(tostring(msgData.MusicSessionId))
        end)
        local pad = Instance.new("UIPadding")
        pad.PaddingBottom = UDim.new(0, 8)
        pad.Parent = frame
    elseif msgData.IsProfileShare then
        local shareBox = Instance.new("Frame")
        shareBox.Name = "ProfileShareBox"
        shareBox.Size = UDim2.new(1,-48,0,118)
        shareBox.Position = UDim2.new(0,38,0,msgData.ReplyToText and 58 or 24)
        shareBox.BackgroundColor3 = Color3.fromRGB(24,24,36)
        shareBox.BackgroundTransparency = 0.04
        shareBox.BorderSizePixel = 0
        shareBox.ZIndex = 12
        shareBox.Parent = frame
        local sc = Instance.new("UICorner")
        sc.CornerRadius = UDim.new(0,10)
        sc.Parent = shareBox
        local ss = Instance.new("UIStroke")
        ss.Thickness = 1.2
        ss.Transparency = 0.15
        ss.Color = Color3.fromRGB(120,120,145)
        ss.Parent = shareBox

        local socialsTitle = Instance.new("TextLabel")
        socialsTitle.Size = UDim2.new(1,-16,0,22)
        socialsTitle.Position = UDim2.new(0,8,0,5)
        socialsTitle.BackgroundTransparency = 1
        socialsTitle.Font = Enum.Font.GothamBold
        socialsTitle.Text = "~ " .. tostring(msgData.DisplayName or msgData.Username or "User") .. "'s Socials"
        socialsTitle.TextColor3 = Color3.fromRGB(255,255,255)
        socialsTitle.TextSize = 11
        socialsTitle.TextXAlignment = Enum.TextXAlignment.Center
        socialsTitle.ZIndex = 13
        socialsTitle.Parent = shareBox

        local function socialRow(labelText, handle, y, url, buttonText)
            local row = Instance.new("Frame")
            row.Size = UDim2.new(1,-14,0,26)
            row.Position = UDim2.new(0,7,0,y)
            row.BackgroundColor3 = Color3.fromRGB(34,34,48)
            row.BackgroundTransparency = 0
            row.BorderSizePixel = 0
            row.ZIndex = 13
            row.Parent = shareBox
            local rc = Instance.new("UICorner")
            rc.CornerRadius = UDim.new(0,6)
            rc.Parent = row

            local text = Instance.new("TextLabel")
            text.Size = UDim2.new(1,-72,1,0)
            text.Position = UDim2.new(0,8,0,0)
            text.BackgroundTransparency = 1
            text.Font = Enum.Font.GothamMedium
            text.Text = labelText .. "  @" .. (handle ~= "" and handle or "Not set")
            text.TextColor3 = Color3.fromRGB(235,235,242)
            text.TextSize = 8
            text.TextXAlignment = Enum.TextXAlignment.Left
            text.ZIndex = 14
            text.Parent = row

            if url and handle ~= "" then
                local open = Instance.new("TextButton")
                open.Size = UDim2.new(0,58,0,20)
                open.Position = UDim2.new(1,-64,0,3)
                open.BackgroundColor3 = Color3.fromRGB(205,35,45)
                open.BackgroundTransparency = 0
                open.BorderSizePixel = 0
                open.Font = Enum.Font.GothamBold
                open.Text = buttonText or "OPEN"
                open.TextColor3 = Color3.fromRGB(255,255,255)
                open.TextSize = 7
                open.ZIndex = 14
                open.Parent = row
                local oc = Instance.new("UICorner")
                oc.CornerRadius = UDim.new(0,5)
                oc.Parent = open
                open.MouseButton1Click:Connect(function()
                    local opened = false
                    pcall(function()
                        if open_url then open_url(url); opened = true
                        elseif openurl then openurl(url); opened = true
                        elseif syn and syn.open_url then syn.open_url(url); opened = true end
                    end)
                    if not opened and setclipboard then setclipboard(url) end
                    open.Text = opened and "OPENED" or "COPIED"
                    task.delay(1.2,function() if open and open.Parent then open.Text = buttonText or "OPEN" end end)
                end)
            end
        end

        socialRow("Instagram Handle", tostring(msgData.Instagram or ""), 31, "https://www.instagram.com/" .. tostring(msgData.Instagram or ""), "OPEN")
        socialRow("Snapchat Handle", tostring(msgData.Snapchat or ""), 59, "https://www.snapchat.com/add/" .. tostring(msgData.Snapchat or ""), "OPEN")
        socialRow("Discord Username", tostring(msgData.Discord or ""), 87, nil, nil)

        local pad=Instance.new("UIPadding")
        pad.PaddingBottom=UDim.new(0,8)
        pad.Parent=frame
    elseif msgData.IsServerInvite and msgData.PlaceId and msgData.JobId then
        local inviteButton = Instance.new("TextButton")
        inviteButton.Name = "ServerInviteCardButton"
        inviteButton.Size = UDim2.new(1, -48, 0, 34)
        inviteButton.Position = UDim2.new(0, 38, 0, msgData.ReplyToText and 58 or 24)
        inviteButton.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
        inviteButton.BackgroundTransparency = 0.15
        inviteButton.BorderSizePixel = 0
        inviteButton.Font = Enum.Font.GothamBold
        inviteButton.Text = "  ↗  JOIN CURRENT SERVER  "
        inviteButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        inviteButton.TextSize = 11
        inviteButton.TextXAlignment = Enum.TextXAlignment.Center
        inviteButton.ZIndex = 12
        inviteButton.Parent = frame

        local inviteCorner = Instance.new("UICorner")
        inviteCorner.CornerRadius = UDim.new(0, 7)
        inviteCorner.Parent = inviteButton

        local inviteInfo = Instance.new("TextLabel")
        inviteInfo.Name = "ServerInviteInfo"
        inviteInfo.Size = UDim2.new(1, -48, 0, 18)
        inviteInfo.Position = UDim2.new(0, 38, 0, msgData.ReplyToText and 94 or 60)
        inviteInfo.BackgroundTransparency = 1
        inviteInfo.Font = Enum.Font.Gotham
        inviteInfo.Text = tostring(msgData.Text or "invited you to join their current server")
        inviteInfo.TextColor3 = Color3.fromRGB(210, 210, 220)
        inviteInfo.TextSize = 9
        inviteInfo.TextXAlignment = Enum.TextXAlignment.Left
        inviteInfo.TextYAlignment = Enum.TextYAlignment.Center
        inviteInfo.TextWrapped = true
        inviteInfo.ZIndex = 12
        inviteInfo.Parent = frame

        inviteButton.MouseButton1Click:Connect(function()
            local ok, err = pcall(function()
                TeleportService:TeleportToPlaceInstance(
                    tonumber(msgData.PlaceId),
                    tostring(msgData.JobId),
                    LocalPlayer
                )
            end)

            if not ok then
                ShowLocalSystemMessage("[AI MoD] : Unable to join server: " .. tostring(err), true)
            end
        end)

        local pad = Instance.new("UIPadding")
        pad.PaddingBottom = UDim.new(0, 8)
        pad.Parent = frame
    elseif isSticker then
        local stickerImg = Instance.new("ImageButton")
        stickerImg.Name = "StickerMsg"
        stickerImg.Size = UDim2.new(0, 80, 0, 80)
        stickerImg.Position = UDim2.new(0, 38, 0, msgData.ReplyToText and 60 or 26)
        stickerImg.BackgroundTransparency = 1
        stickerImg.BorderSizePixel = 0
        stickerImg.Image = msgData.Text
        stickerImg.ScaleType = Enum.ScaleType.Fit
        stickerImg.AutoButtonColor = false
        stickerImg.ZIndex = 10
        stickerImg.Parent = frame
        stickerImg.Activated:Connect(function()
            OpenStickerViewer(msgData.Text)
        end)

        local pad = Instance.new("UIPadding")
        pad.PaddingBottom = UDim.new(0, 8)
        pad.Parent = frame
    else
        local contentLabel = Instance.new("TextLabel")
        contentLabel.Name = "ContentLabel"
        contentLabel.Size = UDim2.new(1, -48, 0, 0)
        contentLabel.Position = UDim2.new(0, 38, 0, msgData.ReplyToText and 58 or (msgData.IsPrivate and 26 or 24))
        contentLabel.AutomaticSize = Enum.AutomaticSize.Y
        contentLabel.BackgroundTransparency = 1
        contentLabel.Font = Enum.Font.Gotham
        contentLabel.Text = msgData.Text
        contentLabel.TextColor3 = msgData.IsPrivate and Color3.fromRGB(255, 170, 0) or Color3.fromRGB(230, 230, 240)
        if msgData.Announcement then
            
            contentLabel.TextColor3 = ANNOUNCEMENT_TEXT_COLOR
            contentLabel.TextSize = ANNOUNCEMENT_TEXT_SIZE
        else
            contentLabel.TextSize = 11
        end
        contentLabel.TextXAlignment = Enum.TextXAlignment.Left
        contentLabel.TextYAlignment = Enum.TextYAlignment.Top
        contentLabel.TextWrapped = true
        contentLabel.Parent = frame
        if msgData.Edited then
            contentLabel.Text = tostring(msgData.Text or "") .. "  (edited)"
        end

        local pad = Instance.new("UIPadding")
        pad.PaddingBottom = UDim.new(0, 6)
        pad.Parent = frame
    end


    
local interactionButton = Instance.new("TextButton")
    interactionButton.Name = "MessageInteraction"
    interactionButton.Size = UDim2.new(1, 0, 1, 0)
    interactionButton.Position = UDim2.new(0, 0, 0, 0)
    interactionButton.BackgroundTransparency = 1
    interactionButton.BorderSizePixel = 0
    interactionButton.Text = ""
    interactionButton.AutoButtonColor = false
    interactionButton.ZIndex = 4
    interactionButton.Parent = frame

    local pressToken = 0
    interactionButton.InputBegan:Connect(function(input)
        if input.UserInputType ~= Enum.UserInputType.MouseButton1 and input.UserInputType ~= Enum.UserInputType.Touch then return end
        pressToken = pressToken + 1
        local token = pressToken
        local pressPosition = input.Position

        task.delay(0.65, function()
            if token ~= pressToken or not interactionButton.Parent then return end
            ShowMessageContextMenu(msgData, pressPosition)
        end)

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                pressToken = pressToken + 1
            end
        end)
    end)

    frame.Parent = MessageContainer
    frame:SetAttribute("MessageTimestamp", msgData.Timestamp or 0)
    frame:SetAttribute("MessageUsername", msgData.Username or "")
    frame:SetAttribute("ClientMessageId", tostring(msgData.ClientMessageId or ""))

    if msgData.ReplyToTimestamp then
        task.delay(0.15, function()
            for _, child in ipairs(MessageContainer:GetChildren()) do
                if child:IsA("Frame") and child:GetAttribute("MessageTimestamp") == msgData.ReplyToTimestamp and child:GetAttribute("MessageUsername") == msgData.ReplyToUsername then
                    local stroke = child:FindFirstChild("ReplyHighlight")
                    if not stroke then
                        stroke = Instance.new("UIStroke")
                        stroke.Name = "ReplyHighlight"
                        stroke.Thickness = 2
                        stroke.Color = Color3.fromRGB(255, 70, 70)
                        stroke.Transparency = 0.1
                        stroke.Parent = child
                    end
                    task.delay(2.5, function()
                        if stroke and stroke.Parent then
                            TweenService:Create(stroke, TweenInfo.new(0.5), {Transparency = 1}):Play()
                        end
                    end)
                    break
                end
            end
        end)
    end
    return frame
end




local function ShowLocalSystemMessage(text, isError)
    local msg = {
        Username = "AI_MoD",
        DisplayName = "AI MoD",
        Text = tostring(text),
        Timestamp = os.time() + (math.random() / 1000),
        Role = "BOT",
        PfpId = 78588114275746,
        IsPrivate = false,
        SystemMessage = true,
        LocalOnly = true,
    }

    local card = CreateMessageCard(msg, ActiveTab == "GLOBAL")
    card:SetAttribute("LocalOnly", true)
    card.Visible = true

    local content = card:FindFirstChild("ContentLabel")
    if content and isError then
        content.TextColor3 = Color3.fromRGB(255, 105, 105)
    end
end

local function ShowAntiSpamMessage()
    ShowLocalSystemMessage("[AI MoD] : Deep breath in, deep breath out. Now, please stop spamming 😣", true)
end

local function TestNotification()
    local testId = "LOCAL_TEST_NOTIFICATION_" .. tostring(os.clock()) .. "_" .. tostring(math.random(1000, 9999))
    DisplayNewMessageNotif({
        Username = "__KronosNotificationTest",
        DisplayName = "Kronos Test",
        Text = "Notification test — if you see this, notifications are working.",
        Timestamp = os.time(),
        Role = "Recruit",
        PfpId = CurrentPfpId,
        IsPrivate = false,
    }, testId)
end

Moderation.PostModerationPayload = function(payload, isGlobal)
    local endpoint = isGlobal and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
    task.spawn(function()
        pcall(function() HttpRequest(endpoint, "POST", payload) end)
    end)
end

Moderation.RenderLocalMessage = function(payload, isGlobal)
    local cache = isGlobal and GlobalCachedMessages or ServerCachedMessages
    local ui = isGlobal and GlobalUIElements or ServerUIElements
    local key = "local_" .. tostring(payload.ClientMessageId or (payload.Timestamp .. "_" .. math.random(100000, 999999)))
    cache[key] = payload
    local card = CreateMessageCard(payload, isGlobal)
    card.Visible = (ActiveTab == (isGlobal and "GLOBAL" or "SERVER"))
    table.insert(ui, card)
end

Moderation.SendAnnouncement = function(body)
    if not IsCommandUserAuthorized("Announce") then
        ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
        return false
    end

    local isGlobal = ActiveTab == "GLOBAL"
    local text = "🚨 Okey Listen Up 📢 : " .. tostring(body)
    local clientMessageId = LocalPlayer.UserId .. "_announcement_" .. tostring(math.floor(os.clock() * 1000000)) .. "_" .. tostring(math.random(100000, 999999))
    local payload = {
        ClientMessageId = clientMessageId,
        Username = ANNOUNCER_NAME,
        DisplayName = ANNOUNCER_NAME,
        Text = text,
        Timestamp = os.time(),
        Role = "BOT",
        PfpId = ANNOUNCER_PFP_ID,
        IsPrivate = false,
        SystemMessage = true,
        Announcement = true,
        AnnouncementBy = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
    }
    Moderation.RenderLocalMessage(payload, isGlobal)
    Moderation.PostModerationPayload(payload, isGlobal)
end

Moderation.SendMute = function(targetUsername, targetDisplayName, durationMinutes)
    if not IsCommandUserAuthorized("Mute") then
        ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
        return false
    end

    local isGlobal = ActiveTab == "GLOBAL"
    local expiresAt = os.time() + (durationMinutes * 60)
    local clientMessageId = LocalPlayer.UserId .. "_mute_" .. tostring(math.floor(os.clock() * 1000000)) .. "_" .. tostring(math.random(100000, 999999))
    local payload = {
        ClientMessageId = clientMessageId,
        Username = "AI_MoD",
        DisplayName = "AI MoD",
        Text = "",
        Timestamp = os.time(),
        Role = "BOT",
        PfpId = 78588114275746,
        IsPrivate = false,
        SystemMessage = true,
        MuteAction = true,
        TargetUsername = targetUsername,
        TargetDisplayName = targetDisplayName,
        DurationMinutes = durationMinutes,
        ExpiresAt = expiresAt,
        ImposedByUsername = LocalPlayer.Name,
        ImposedByDisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        Scope = isGlobal and "GLOBAL" or "SERVER",
    }
    Moderation.ActiveMutes[Moderation.GetMuteKey(payload.Scope, targetUsername)] = payload
    Moderation.PostModerationPayload(payload, isGlobal)
    ShowLocalSystemMessage("[AI MoD] : 🔇 " .. targetDisplayName .. " has been muted for " .. tostring(durationMinutes) .. " minute(s).", true)
end

local function FindAllFirebaseMessageKeys(endpoint, clientMessageId)
    local keys = {}
    local data = HttpRequest(endpoint, "GET")
    if type(data) ~= "table" then return keys end
    for key, remote in pairs(data) do
        if type(remote) == "table" and tostring(remote.ClientMessageId or "") == tostring(clientMessageId or "") then
            table.insert(keys, tostring(key))
        end
    end
    return keys
end

local function FindFirebaseMessageKey(endpoint, clientMessageId)
    local data = HttpRequest(endpoint, "GET")
    if type(data) ~= "table" then return nil end
    for key, remote in pairs(data) do
        if type(remote) == "table" and tostring(remote.ClientMessageId or "") == tostring(clientMessageId or "") then
            return tostring(key)
        end
    end
    return nil
end

local function UpdateLocalMessageCard(clientMessageId, newText, edited)
    local wanted = tostring(clientMessageId or "")
    for _, list in ipairs({GlobalUIElements, ServerUIElements}) do
        for _, card in ipairs(list) do
            if card and card.Parent and tostring(card:GetAttribute("ClientMessageId") or "") == wanted then
                local label = card:FindFirstChild("ContentLabel")
                if label then
                    label.Text = tostring(newText or "") .. (edited and "  (edited)" or "")
                end
            end
        end
    end
end

local function EnterEditMode(message)
    if not message or tostring(message.Username or "") ~= tostring(LocalPlayer.Name) then return end
    EditingContextMessage = message
    EditingContextIsGlobal = (ActiveTab == "GLOBAL")
    MessageInput.Text = tostring(message.Text or "")
    MessageInput.CursorPosition = #MessageInput.Text + 1
    MessageInput:CaptureFocus()
end

local function SaveEditedMessage(message, newText, isGlobal)
    if not message or tostring(message.Username or "") ~= tostring(LocalPlayer.Name) then return false end
    local clientId = tostring(message.ClientMessageId or "")
    if clientId == "" then return false end
    newText = tostring(newText or "")
    if newText:match("^%s*$") then return false end

    local endpoint = isGlobal and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
    local patch = {
        Text = newText,
        Edited = true,
        EditedAt = os.time(),
        EditedBy = LocalPlayer.Name,
    }


    
    PendingMessageMutations[clientId] = patch
    message.Text = newText
    message.Edited = true
    message.EditedAt = patch.EditedAt
    UpdateLocalMessageCard(clientId, newText, true)

    task.spawn(function()
        local confirmed = false
        for attempt = 1, 4 do
            local firebaseKeys = FindAllFirebaseMessageKeys(endpoint, clientId)
            if #firebaseKeys > 0 then
                for _, firebaseKey in ipairs(firebaseKeys) do
                    pcall(function()
                        HttpRequest(endpoint .. "/" .. tostring(firebaseKey) .. ".json", "PATCH", patch)
                    end)
                end
                task.wait(0.35)
                local latest = HttpRequest(endpoint, "GET")
                if type(latest) == "table" then
                    confirmed = true
                    for _, remote in pairs(latest) do
                        if type(remote) == "table" and tostring(remote.ClientMessageId or "") == clientId then
                            if tostring(remote.Text or "") ~= newText or remote.Edited ~= true then
                                confirmed = false
                                break
                            end
                        end
                    end
                end
                if confirmed then break end
            end
            task.wait(0.35)
        end

        if confirmed then
            PendingMessageMutations[clientId] = nil
        else
            ShowLocalSystemMessage("[AI MoD] : Firebase did not confirm the edit. The edited text will remain protected from stale polling while you are connected.", true)
        end
    end)
    return true
end

SendChatMessage = function()
    local typedText = MessageInput.Text or ""
    if typedText == "" or typedText:match("^%s*$") then return end


    
    if EditingContextMessage then
        local editing = EditingContextMessage
        local isGlobalEdit = EditingContextIsGlobal
        if SaveEditedMessage(editing, typedText, isGlobalEdit) then
            MessageInput.Text = ""
            EditingContextMessage = nil
            EditingContextIsGlobal = false
            ReplyContextMessage = nil
            RefreshReplyUI()
            ShowLocalSystemMessage("[AI MoD] : Message edited ✓")
        end
        return
    end

    local isPrivate = IsPrivateModeActive()
    local privateTargetUsername = PrivateModeTargetUsername
    local privateTargetDisplayName = PrivateModeTargetDisplayName
    local privateBody = typedText






    if not isPrivate then
        local commandToken = typedText:match("^%s*(![%w_]+)")
        if commandToken then
            local commandPermission = {
                ["!testnotif"] = "Announce",
                ["!notify"] = "Announce",
                ["!joined"] = "Announce",
                ["!announce"] = "Announce",
                ["!mute"] = "Mute",
                ["!role"] = "Spoof",
                ["!spoof"] = "Spoof",
                ["!gplymsc"] = "PlayMusic",
                ["!splymsc"] = "PlayMusic",
                ["!stpmsc"] = "PlayMusic",
            }

            local permission = commandPermission[commandToken]
            if permission then
                if not IsCommandUserAuthorized(permission) then
                    ShowLocalSystemMessage(
                        "[AI MoD] : You have not been granted permission to use this command by the Architect.",
                        true
                    )
                    MessageInput.Text = ""
                    return
                end
            end
        end
    end


    
    if isPrivate then
        if not privateTargetUsername then return end
        privateBody = typedText
        if privateBody:match("^%s*$") then return end
    end

    if not isPrivate then
        local currentScope = (ActiveTab == "GLOBAL") and "GLOBAL" or "SERVER"
        local ownMute = Moderation.GetActiveMute(currentScope, LocalPlayer.Name)
        if ownMute then
            local remaining = Moderation.FormatMuteRemaining((tonumber(ownMute.ExpiresAt) or os.time()) - os.time())
            local imposedBy = tostring(ownMute.ImposedByDisplayName or "Moderator")
            ShowLocalSystemMessage("[AI MoD] : 🔇 You can't chat for the next " .. remaining .. " due to a mute enforced by " .. imposedBy .. ".", true)
            MessageInput.Text = ""
            return
        end
    end

    if not isPrivate and typedText == "!testnotif" then
        if IsCommandUserAuthorized("Announce") then
            TestNotification()
            ShowLocalSystemMessage("[AI MoD] : Notification test triggered successfully ✓")
        else
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!notify" then
        if IsCommandUserAuthorized("Announce") then
            TestNotification()
            ShowLocalSystemMessage("[AI MoD] : Notification test triggered successfully ✓")
        else
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!joined" then
        if IsCommandUserAuthorized("Announce") then
            ShowLocalJoinMessage()
            ShowLocalSystemMessage("[AI MoD] : Join-message test triggered successfully ✓")
        else
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 9) == "!announce" then
        if not CanUseAnnouncement(LocalPlayer.Name) then
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
            MessageInput.Text = ""
            return
        end
        local body = typedText:sub(10):gsub("^%s+", "")
        if body == "" then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Usage: !announce [Text].", true)
            MessageInput.Text = ""
            return
        end
        if #body > 900 then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Announcement must be 900 characters or fewer.", true)
            MessageInput.Text = ""
            return
        end
        Moderation.SendAnnouncement(body)
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 6) == "!mute " then
        if not CanUseModerationCommands(LocalPlayer.Name) then
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
            MessageInput.Text = ""
            return
        end
        local argsText = typedText:sub(7)
        local durationText, targetQuery = argsText:match("^(%d+)%s+(.+)$")
        local duration = tonumber(durationText)
        if not duration or duration < 1 or duration > 15 or not targetQuery or targetQuery:match("^%s*$") then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Usage: !mute [1-15 minutes] [DisplayName / Username / prefix].", true)
            MessageInput.Text = ""
            return
        end
        local targetUsername, targetDisplayName = Moderation.ResolveModerationTarget(targetQuery)
        if not targetUsername then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Player not found. Use their DisplayName, Username, or a unique prefix.", true)
            MessageInput.Text = ""
            return
        end
        Moderation.SendMute(targetUsername, targetDisplayName, duration)
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 5) == "!tag " then
        local requestedRole = string.sub(typedText, 6):gsub("^%s+", ""):gsub("%s+$", "")
        local canonical = FindCanonicalRole(requestedRole)
        local style = canonical and RANK_STYLES[canonical] or nil

        if not canonical or type(style) ~= "table" then
            ShowLocalSystemMessage("[AI MoD] : Unknown tag. Please use a valid role.", true)
        elseif not CanSwitchToRole(canonical) then
            ShowLocalSystemMessage("[AI MoD] : You have not been granted the \"" .. canonical .. "\" Tag by the Architect.", true)
        else


            
            AssignedPlayerRoles[LocalPlayer.Name] = canonical
            ActiveSelectedTag = canonical
            DisabledTags[LocalPlayer.Name] = nil
            ShowLocalSystemMessage("[AI MoD] : Tag changed successfully to " .. canonical .. " ✓")
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!tag" then
        DisabledTags[LocalPlayer.Name] = not DisabledTags[LocalPlayer.Name]
        ShowLocalSystemMessage("[AI MoD] : Tag display " .. (DisabledTags[LocalPlayer.Name] and "disabled" or "enabled") .. " ✓")
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 6) == "!role " then
        if not IsCommandUserAuthorized("Spoof") then
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
            MessageInput.Text = ""
            return
        end
        local targetQuery = string.sub(typedText, 7):gsub("^%s+", ""):gsub("%s+$", "")
        local targetUsername, targetDisplayName = Moderation.ResolveModerationTarget(targetQuery)
        if not targetUsername then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Player not found.", true)
        else
            local roles = GetPlayerRoles(targetUsername)
            local highest = GetHighestRole(targetUsername)
            local text = #roles > 0 and table.concat(roles, ", ") or "No assigned roles"
            ShowLocalSystemMessage("[AI MoD] : " .. targetDisplayName .. " — Highest: " .. highest .. " | Roles: " .. text)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!role" then
        if not IsCommandUserAuthorized("Spoof") then
            ShowLocalSystemMessage("[AI MoD] : You have not been granted permission to use this command by the Architect.", true)
            MessageInput.Text = ""
            return
        end
        local roles = GetPlayerRoles(LocalPlayer.Name)
        local highest = GetHighestRole(LocalPlayer.Name)
        ShowLocalSystemMessage("[AI MoD] : Your highest role is " .. highest .. ". Assigned roles: " .. (#roles > 0 and table.concat(roles, ", ") or "none") .. ".")
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 7) == "!spoof " then
        local requested = string.sub(typedText, 8)
        local lowered = string.lower(requested)
        if lowered == "architect" or lowered == "overseer" or lowered == "com-god" then
            ShowLocalSystemMessage("[AI MoD] : Command failed. Architect, Overseer and Com-God cannot be spoofed.", true)
        elseif CanUseSpoof(LocalPlayer.Name) then
            SpoofedDisplayName = requested
            ShowLocalSystemMessage("[AI MoD] : Display name changed successfully ✓")
        else
            ShowLocalSystemMessage("[AI MoD] : Command failed. You do not have permission to use !spoof.", true)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!spoof" then
        ShowLocalSystemMessage("[AI MoD] : Command failed. Usage: !spoof [NewName].", true)
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 9) == "!gplymsc " then
        ShowLocalSystemMessage("[AI MoD] : Roblox Music IDs have been replaced by YouTube Music Sessions. Open the Music Session tab and paste a YouTube URL.", true)
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 9) == "!splymsc " then
        ShowLocalSystemMessage("[AI MoD] : Roblox Music IDs have been replaced by YouTube Music Sessions. Open the Music Session tab and paste a YouTube URL.", true)
        MessageInput.Text = ""
        return
    elseif not isPrivate and typedText == "!stpmsc" then
        if ActiveMusicSessionId and MusicSessionIsHost() then
            HostStopMusic()
        else
            ShowLocalSystemMessage("[AI MoD] : Join/host a Music Session first.", true)
        end
        MessageInput.Text = ""
        return
    elseif not isPrivate and string.sub(typedText, 1, 1) == "!" then
        ShowLocalSystemMessage("[AI MoD] : Unknown command. Command failed.", true)
        MessageInput.Text = ""
        return
    end


    local spamText = privateBody
    local now = tick()
    if spamText == LastSentMessageText and (now - LastMessageSendTime) <= 30 then
        ConsecutiveSpamCount = ConsecutiveSpamCount + 1
    else
        ConsecutiveSpamCount = 1
    end

    if ConsecutiveSpamCount >= 3 then
        ShowAntiSpamMessage()
        MessageInput.Text = ""
        return
    end

    LastSentMessageText = spamText
    LastMessageSendTime = now

    local reply = ReplyContextMessage
    local clientMessageId = LocalPlayer.UserId .. "_" .. tostring(math.floor(os.clock() * 1000000)) .. "_" .. tostring(math.random(100000, 999999))

    local payload = {
        ClientMessageId = clientMessageId,
        Username = LocalPlayer.Name,
        UserId = LocalPlayer.UserId,
        DisplayName = GetFormattedDisplayName(LocalPlayer.DisplayName, true),
        Text = privateBody,
        Timestamp = os.time(),


        
        Role = GetLocalChatRole(),
        PfpId = CurrentPfpId,
        IsPrivate = isPrivate,
        TargetUsername = privateTargetUsername,
        TargetDisplayName = privateTargetDisplayName,
        ReplyToUsername = reply and reply.Username or nil,
        ReplyToDisplayName = reply and reply.DisplayName or nil,
        ReplyToText = reply and reply.Text or nil,
        ReplyToTimestamp = reply and reply.Timestamp or nil
    }


    local localCache = (ActiveTab == "GLOBAL") and GlobalCachedMessages or ServerCachedMessages
    local localUI = (ActiveTab == "GLOBAL") and GlobalUIElements or ServerUIElements
    local localKey = "local_" .. clientMessageId

    localCache[localKey] = payload
    local immediateCard = CreateMessageCard(payload, ActiveTab == "GLOBAL")
    immediateCard.Visible = true
    table.insert(localUI, immediateCard)

    MessageInput.Text = ""
    ReplyContextMessage = nil
    RefreshReplyUI()




    local targetUrl = (ActiveTab == "GLOBAL") and GLOBAL_MESSAGES_ENDPOINT or SERVER_MESSAGES_ENDPOINT
    task.spawn(function()
        local ok = pcall(function()
            HttpRequest(targetUrl, "POST", payload)
        end)
        if not ok then


        end
    end)
end

MessageInput:GetPropertyChangedSignal("Text"):Connect(function()
    ShowCommandAutocomplete()
end)

MessageInput.FocusLost:Connect(function()


    task.defer(function()
        if not MessageInput then return end
        local current = string.lower(MessageInput.Text or "")
        local isCommand = current == "!tag"
            or current:sub(1, 5) == "!tag "
            or current == "!spoof"
            or current:sub(1, 7) == "!spoof "
            or current == "!mute"
            or current:sub(1, 6) == "!mute "
            or current == "!announce"
            or current:sub(1, 9) == "!announce "

        if not isCommand then
            HideCommandAutocomplete()
        end
    end)
end)

SendButton.MouseButton1Click:Connect(SendChatMessage)
MessageInput.FocusLost:Connect(function(enterPressed)
    if enterPressed then SendChatMessage() end
end)

function BuildJoinMessagePayload()
    return {
        Username = "AI_MoD",
        DisplayName = "AI MoD",
        JoinedDisplayName = LocalPlayer.DisplayName,
        Text = LocalPlayer.DisplayName .. " has Joined the Server Chat",
        Timestamp = os.time(),
        Role = "BOT",
        PfpId = 78588114275746,
        IsPrivate = false,
        IsJoinMessage = true,
        JoinEventId = JoinEventId,
    }
end

function ShowLocalJoinMessage()
    local payload = BuildJoinMessagePayload()
    local localId = "local_join_" .. JoinEventId


    ServerCachedMessages[localId] = payload
    local card = CreateMessageCard(payload, false)
    card.Visible = (ActiveTab == "SERVER")
    table.insert(ServerUIElements, card)
    DisplayNewMessageNotif(payload, localId)
end

function PostServerJoinMessage()
    local payload = BuildJoinMessagePayload()
    task.spawn(function()
        HttpRequest(SERVER_MESSAGES_ENDPOINT, "POST", payload)
    end)
end

function PollMessages()
    while true do
        FetchRemoteRankStyles()
        FetchRemoteRoles()

        local globalData = HttpRequest(GLOBAL_MESSAGES_ENDPOINT, "GET")
        if globalData and type(globalData) == "table" then
            for id, msg in pairs(globalData) do
                if type(msg) == "table" then msg._FirebaseKey = tostring(id) end
                if type(msg) == "table" and msg.MuteAction then
                    if tonumber(msg.ExpiresAt or 0) > os.time() and msg.TargetUsername and msg.Scope == "GLOBAL" then
                        Moderation.ActiveMutes[Moderation.GetMuteKey("GLOBAL", msg.TargetUsername)] = msg
                    end
                elseif type(msg) == "table" and msg.Timestamp and msg.Timestamp >= ScriptStartTime and (not msg.IsPrivate or msg.Username == LocalPlayer.Name or msg.TargetUsername == LocalPlayer.Name) then
                    local blockedByUser = IsMessageUserBlocked(msg.Username)
                    local blockedByMute = Moderation.GetActiveMute("GLOBAL", msg.Username) ~= nil
                    if blockedByUser and msg.Username ~= LocalPlayer.Name then


                        
                    elseif blockedByMute and msg.Username ~= LocalPlayer.Name then
                        
                    else
                    local clientIdForMutation = tostring(msg.ClientMessageId or "")
                    local pendingMutation = PendingMessageMutations[clientIdForMutation]
                    if pendingMutation then
                        for mutationKey, mutationValue in pairs(pendingMutation) do
                            if mutationKey == "Deleted" then
                                msg.Deleted = mutationValue
                                msg.DeletedAt = pendingMutation.DeletedAt
                                msg.DeletedBy = pendingMutation.DeletedBy
                            else
                                msg[mutationKey] = mutationValue
                            end
                        end






                        if pendingMutation.MutationType ~= "DELETE"
                            and pendingMutation.Edited
                            and tostring(msg.Text or "") == tostring(pendingMutation.Text or "")
                            and msg.Edited == true then
                            PendingMessageMutations[clientIdForMutation] = nil
                        end
                    end
                    local clientKey = msg.ClientMessageId and ("local_" .. tostring(msg.ClientMessageId)) or nil
                    if msg.Deleted then
                        local targetId = tostring(msg.ClientMessageId or "")
                        GlobalCachedMessages[id] = msg
                        for _, card in ipairs(GlobalUIElements) do
                            if card and card.Parent and tostring(card:GetAttribute("ClientMessageId") or "") == targetId then
                                card:Destroy()
                            end
                        end
                    elseif clientKey and GlobalCachedMessages[clientKey] then
                        GlobalCachedMessages[clientKey] = msg
                        GlobalCachedMessages[id] = msg
                        UpdateLocalMessageCard(msg.ClientMessageId, msg.Text, msg.Edited)
                    elseif GlobalCachedMessages[id] then
                        GlobalCachedMessages[id] = msg
                        UpdateLocalMessageCard(msg.ClientMessageId, msg.Text, msg.Edited)
                    else
                        GlobalCachedMessages[id] = msg
                        local card = CreateMessageCard(msg, true)
                        card.Visible = (ActiveTab == "GLOBAL")
                        table.insert(GlobalUIElements, card)
                        DisplayNewMessageNotif(msg)
                    end
                    end
                end
            end
        end

        local serverData = HttpRequest(SERVER_MESSAGES_ENDPOINT, "GET")
        if serverData and type(serverData) == "table" then
            for id, msg in pairs(serverData) do
                if type(msg) == "table" then msg._FirebaseKey = tostring(id) end
                if type(msg) == "table" and msg.JoinEventId == JoinEventId then
                    
                elseif type(msg) == "table" and msg.MuteAction then
                    if tonumber(msg.ExpiresAt or 0) > os.time() and msg.TargetUsername and msg.Scope == "SERVER" then
                        Moderation.ActiveMutes[Moderation.GetMuteKey("SERVER", msg.TargetUsername)] = msg
                    end
                elseif type(msg) == "table" and msg.Timestamp and msg.Timestamp >= ScriptStartTime and (not msg.IsPrivate or msg.Username == LocalPlayer.Name or msg.TargetUsername == LocalPlayer.Name) then
                    local blockedByUser = IsMessageUserBlocked(msg.Username)
                    local blockedByMute = Moderation.GetActiveMute("SERVER", msg.Username) ~= nil
                    if blockedByUser and msg.Username ~= LocalPlayer.Name then


                        
                    elseif blockedByMute and msg.Username ~= LocalPlayer.Name then
                        
                    else
                    local clientIdForMutation = tostring(msg.ClientMessageId or "")
                    local pendingMutation = PendingMessageMutations[clientIdForMutation]
                    if pendingMutation then
                        for mutationKey, mutationValue in pairs(pendingMutation) do
                            if mutationKey == "Deleted" then
                                msg.Deleted = mutationValue
                                msg.DeletedAt = pendingMutation.DeletedAt
                                msg.DeletedBy = pendingMutation.DeletedBy
                            else
                                msg[mutationKey] = mutationValue
                            end
                        end






                        if pendingMutation.MutationType ~= "DELETE"
                            and pendingMutation.Edited
                            and tostring(msg.Text or "") == tostring(pendingMutation.Text or "")
                            and msg.Edited == true then
                            PendingMessageMutations[clientIdForMutation] = nil
                        end
                    end
                    local clientKey = msg.ClientMessageId and ("local_" .. tostring(msg.ClientMessageId)) or nil
                    if msg.Deleted then
                        local targetId = tostring(msg.ClientMessageId or "")
                        ServerCachedMessages[id] = msg
                        for _, card in ipairs(ServerUIElements) do
                            if card and card.Parent and tostring(card:GetAttribute("ClientMessageId") or "") == targetId then
                                card:Destroy()
                            end
                        end
                    elseif clientKey and ServerCachedMessages[clientKey] then
                        ServerCachedMessages[clientKey] = msg
                        ServerCachedMessages[id] = msg
                        UpdateLocalMessageCard(msg.ClientMessageId, msg.Text, msg.Edited)
                    elseif ServerCachedMessages[id] then
                        ServerCachedMessages[id] = msg
                        UpdateLocalMessageCard(msg.ClientMessageId, msg.Text, msg.Edited)
                    else
                        ServerCachedMessages[id] = msg
                        local card = CreateMessageCard(msg, false)
                        card.Visible = (ActiveTab == "SERVER")
                        table.insert(ServerUIElements, card)
                        DisplayNewMessageNotif(msg)
                    end
                    end
                end
            end
        end

        task.wait(2)
    end
end

task.spawn(function()
    task.wait(0.5)
    ShowLocalJoinMessage()
    task.wait(0.25)
    PostServerJoinMessage()
end)

task.spawn(PollMessages)



function KronosProtectedSpoofRole(name)
    name = tostring(name or ""):lower()
    return name == "architect" or name == "overseer"
end
